# Gemini review — `v0.1.0-alpha.10..e05c191`

_Generated by `gemini -p` on 2026-05-09T14:00:49+08:00_

I have reviewed the changes between `v0.1.0-alpha.10` and `v0.1.0-alpha.11` (branch `main`). The diff covers the decommission of the GitHub Action for AI reviews in favor of a maintainer-local CLI workflow, along with safety improvements in GUI error handling and ref-name validation.

### 1. Architecture
*   **Decoupled Review Strategy:** The shift from API-based CI to CLI-based maintainer-local reviews (`docs/triple_review.md`, `scripts/triple_review.sh`) is a significant architectural decision. It eliminates secrets management for AI providers and leverages existing subscriptions, but at the cost of losing automated "per-PR" signal in the public CI.
*   **GUI Error Propagation:** The addition of `try/except` blocks in `open_wua_q` and `plot_cross_section` (`src/openlimno/gui_core/controller.py:145,188`) correctly implements the "Report, Don't Crash" pattern. This ensures that the alpha.10 change to `_read_wua_parquet` (which now propagates exceptions rather than returning empty lists) does not leak into the global Qt exception handler.

### 2. Bug Risk
*   **Ref Disambiguation:** The logic in `scripts/triple_review.sh:40` to check `git show-ref --verify` for 4–7 character hex strings is a solid improvement over the previous hard block. However, it only checks `refs/heads/` and `refs/tags/`. If a user passes a short hex string that exists only as a remote branch (e.g., `beef` on `origin` but not checked out locally), the script will incorrectly reject it as a "collision-prone SHA."
*   **Broad Exception Catching:** In `controller.py`, catching `ValueError` along with `OSError` and `RuntimeError` (`line 148, 191`) is necessary for `ArrowInvalid` (older pyarrow) but riskily broad. A logic bug in a column transformation that triggers a standard `ValueError` will now be masked by a "Could not read" warning instead of surfacing as a developer error.

### 3. Test Adequacy
*   **Untested GUI Recovery:** The diff does not include unit tests for the newly added `try/except` paths in `open_wua_q` or `plot_cross_section`. While `test_xs_rows_cache.py` exists, it covers the internal caching helper, not the direct-call paths used by these GUI handlers. I recommend adding a smoke test that feeds a corrupt/zero-byte parquet file to these handlers to verify the `QMessageBox` triggers.
*   **Missing Backend Path:** As noted in the alpha.10 residuals (`docs/reviews/v0.1.0-alpha.10/README.md`), the case where neither `pyarrow` nor `gdal` is installed still triggers a `RuntimeError` that `_read_xs_rows_cached` will futilely retry 3 times. This remains unaddressed in alpha.11.

### 4. Performance
*   **GUI Thread Blocking:** The calls to `_read_wua_parquet` remain on the main GUI thread. For large parquet files (e.g., 500k+ rows), the UI will hang during the read. While the `try/except` prevents a crash, the user experience during the hang is unchanged. Future work should move these reads to a `QThread` or `QRunnable`.

### 5. Packaging / Portability
*   **CLI Dependency Drift:** The `triple_review.sh` script now relies on the interactive `claude` CLI rather than the `--bare` API mode. This makes the review process non-portable to standard CI environments (as documented), cementing it as a maintainer-only gate.

### 6. Security
*   **Shell Injection:** The script uses `printf '%s' "$BASE" | tr 'A-F' 'a-f'` (`scripts/triple_review.sh:43`), which is a safe way to handle potentially malicious branch names (like those starting with `-`). This is an improvement over `echo`.
*   **Symlink Hygiene:** The `controller.py` continues to use `os.path.realpath(path)` for cache keys, which is correct for preventing path-traversal/aliasing issues in the cache.
