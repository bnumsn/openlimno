# Codex review — `v0.1.0-alpha.10..4c6ec27`

_Generated by `codex review --base v0.1.0-alpha.10` on 2026-05-09T14:56:12+08:00_

OpenAI Codex v0.128.0 (research preview)
--------
workdir: /mnt/data/openlimno
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: xhigh
reasoning summaries: none
session id: 019e0b85-c3b9-7393-a692-ae764144ca22
--------
user
changes against 'v0.1.0-alpha.10'
exec
/bin/bash -lc 'git diff 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40 --stat && git diff 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40 --name-only' in /mnt/data/openlimno
 succeeded in 0ms:
 .github/workflows/triple-review.yml    |   99 --
 .gitignore                             |    1 +
 docs/reviews/v0.1.0-alpha.10/README.md |  111 ++
 docs/reviews/v0.1.0-alpha.10/claude.md |   32 +
 docs/reviews/v0.1.0-alpha.10/codex.md  | 2464 ++++++++++++++++++++++++++++++++
 docs/reviews/v0.1.0-alpha.10/gemini.md |   28 +
 docs/triple_review.md                  |  109 +-
 pyproject.toml                         |    2 +-
 scripts/triple_review.sh               |   80 +-
 src/openlimno/gui_core/controller.py   |   79 +-
 10 files changed, 2838 insertions(+), 167 deletions(-)
.github/workflows/triple-review.yml
.gitignore
docs/reviews/v0.1.0-alpha.10/README.md
docs/reviews/v0.1.0-alpha.10/claude.md
docs/reviews/v0.1.0-alpha.10/codex.md
docs/reviews/v0.1.0-alpha.10/gemini.md
docs/triple_review.md
pyproject.toml
scripts/triple_review.sh
src/openlimno/gui_core/controller.py

exec
/bin/bash -lc 'git diff 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40 -- src/openlimno/gui_core/controller.py scripts/triple_review.sh pyproject.toml .github/workflows/triple-review.yml docs/triple_review.md .gitignore' in /mnt/data/openlimno
 succeeded in 0ms:
diff --git a/.github/workflows/triple-review.yml b/.github/workflows/triple-review.yml
deleted file mode 100644
index fa83886..0000000
--- a/.github/workflows/triple-review.yml
+++ /dev/null
@@ -1,99 +0,0 @@
-# Three-AI code review on tag push.
-#
-# Runs Codex / Gemini / Claude against the diff between the new tag and
-# the previous matching tag, attaches the four review files (codex,
-# gemini, claude, summary) to the GitHub Release.
-#
-# Why three? Each reviewer has different blind spots — see
-# scripts/triple_review.sh header for the audit trail of past misses.
-#
-# Required secrets (Settings → Secrets → Actions):
-#   CODEX_API_KEY     OpenAI API key with Codex access
-#   GEMINI_API_KEY    Google AI Studio key
-#   ANTHROPIC_API_KEY Claude API key
-name: triple-review
-
-on:
-  push:
-    tags:
-      - 'v*'
-
-permissions:
-  contents: write   # for attaching assets to the release
-
-jobs:
-  review:
-    runs-on: ubuntu-24.04
-    # Round-3 review (Claude) caught that without timeout-minutes a hung
-    # CLI burns the default 6h GHA budget. 15 min is enough for ~50k
-    # diff lines × 3 reviewers in parallel.
-    timeout-minutes: 15
-    steps:
-      - uses: actions/checkout@v4
-        with:
-          # We need the full history so git diff against the previous
-          # tag works.
-          fetch-depth: 0
-
-      - name: Identify previous tag
-        id: prev
-        run: |
-          # Most recent tag in HEAD's ANCESTRY (not just chronological).
-          # Round-3 review caught that the previous `git tag --sort=-creatordate`
-          # could pick a sibling-branch tag created later, leading to a
-          # nonsensical diff. `git tag --merged HEAD` filters to ancestors.
-          CURRENT_TAG=${GITHUB_REF##refs/tags/}
-          PREV_TAG=$(git tag --merged HEAD --sort=-creatordate \
-                       | grep -v "^${CURRENT_TAG}$" | head -1)
-          if [ -z "$PREV_TAG" ]; then
-            # No prior tag in this branch's history → review against the
-            # initial commit so the user gets *some* signal on a v0.x.0
-            # first release.
-            PREV_TAG=$(git rev-list --max-parents=0 HEAD | head -1)
-          fi
-          echo "previous=${PREV_TAG}" >> "$GITHUB_OUTPUT"
-          echo "current=${CURRENT_TAG}" >> "$GITHUB_OUTPUT"
-
-      - uses: actions/setup-node@v4
-        with: { node-version: '22' }
-
-      - name: Install reviewer CLIs
-        run: |
-          # Pin to known-good majors so a CLI breaking change doesn't
-          # suddenly fail CI. Round-3 review caught that the comment
-          # used to claim "pin major versions" while shipping bare
-          # @latest — promise/code mismatch.
-          npm install -g \
-            @openai/codex@^0.130 \
-            @google/gemini-cli@^0.38 \
-            @anthropic-ai/claude-code@^2
-
-      - name: Run three-AI review
-        env:
-          OPENAI_API_KEY: ${{ secrets.CODEX_API_KEY }}
-          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
-          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
-        run: |
-          bash scripts/triple_review.sh ${{ steps.prev.outputs.previous }}
-
-      - name: Attach reviews to release
-        env:
-          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
-        run: |
-          # Upload every reviews/<sha>.* file to the release matching
-          # the just-pushed tag. --clobber lets re-runs replace prior
-          # uploads.
-          for f in reviews/*.md; do
-            [ -f "$f" ] && gh release upload \
-              "${{ steps.prev.outputs.current }}" \
-              "$f" --clobber
-          done
-
-      - name: Stash reviews as workflow artefact
-        # Independent of release attachment so we keep history even when
-        # someone re-tags / replaces a release.
-        uses: actions/upload-artifact@v4
-        with:
-          name: reviews-${{ steps.prev.outputs.current }}
-          path: reviews/
-          retention-days: 90
diff --git a/.gitignore b/.gitignore
index 844fb0d..400524b 100644
--- a/.gitignore
+++ b/.gitignore
@@ -76,3 +76,4 @@ tmp/
 # docs/reviews/ and ARE committed (the negation below).
 /reviews/
 !docs/reviews/
+.claude/
diff --git a/docs/triple_review.md b/docs/triple_review.md
index aae32ad..62ff773 100644
--- a/docs/triple_review.md
+++ b/docs/triple_review.md
@@ -21,8 +21,31 @@ A single AI reviewer is structurally insufficient:
   consistently correlated. Two reviewers with similar blind spots produce
   false-positive consensus.
 
-See [docs/reviews/v0.1.0-alpha.4/](reviews/v0.1.0-alpha.4/) for a worked
-example where the three diverged sharply on a 92-line diff.
+See [docs/reviews/v0.1.0-alpha.4/](reviews/v0.1.0-alpha.4/) for the worked
+example where the three diverged sharply on a 92-line diff, and
+[docs/reviews/v0.1.0-alpha.10/](reviews/v0.1.0-alpha.10/) for the
+8-round audit trail and convergence proof.
+
+## Why CLI subscription, not API
+
+Each reviewer is invoked through its **CLI's own logged-in subscription**
+(ChatGPT for Codex, Google AI for Gemini, Anthropic Console for Claude
+Code). API keys (`OPENAI_API_KEY` / `GEMINI_API_KEY` / `ANTHROPIC_API_KEY`)
+are **not used** anywhere — not in the orchestrator, not in CI.
+
+Reasons:
+
+- The maintainer already pays for the three subscriptions; per-call API
+  billing on top of that is double-pay.
+- Subscription auth has higher rate limits and saner quota behaviour than
+  API tier.
+- It removes a class of secrets-management risk: no keys to rotate,
+  leak, or expire.
+
+The trade-off is that the review is **maintainer-local**: it cannot run on
+a headless CI runner, since CLI auth requires interactive OAuth login that
+GitHub-hosted runners can't perform. This is intentional; see
+[When to invoke](#when-to-invoke).
 
 ## When to invoke
 
@@ -30,7 +53,12 @@ example where the three diverged sharply on a 92-line diff.
 |---|---|---|
 | **Pre-commit on a non-trivial PR** | `bash scripts/triple_review.sh origin/main` | `reviews/<sha>.*.md` (gitignored) |
 | **Pre-tag locally** | `bash scripts/triple_review.sh <previous-tag>` | `reviews/<sha>.*.md` |
-| **Tag push (any v* tag)** | GitHub Action `triple-review.yml` (auto) | release assets + 90-day artefact |
+| **Post-tag manual upload** | `gh release create <tag> reviews/<sha>.*.md ...` | release assets |
+
+There is **no GitHub Action** for this — it was tried in alpha.5..alpha.9
+and removed in alpha.10 once the API-vs-CLI trade-off was resolved
+(see [`feedback_review_cli_only`](#why-cli-subscription-not-api) above
+for the rationale).
 
 ## Local invocation
 
@@ -48,24 +76,32 @@ BASE=v0.1.0-alpha.3 bash scripts/triple_review.sh
 Outputs land in `reviews/<sha>.codex.md`, `reviews/<sha>.gemini.md`,
 `reviews/<sha>.claude.md`, plus a `<sha>.summary.md` index. The `reviews/`
 directory is gitignored — outputs are per-build artefacts, attached to
-GitHub Releases by the Action rather than committed to history.
+GitHub Releases manually via `gh release create` rather than committed
+to history.
 
-## CI invocation (GitHub Actions)
+## Prerequisites
 
-`.github/workflows/triple-review.yml` triggers on every `v*` tag push.
-Required secrets:
+The maintainer's local environment must have all three CLIs installed and
+each logged into its own subscription account:
 
-- `CODEX_API_KEY` — OpenAI API key with Codex access
-- `GEMINI_API_KEY` — Google AI Studio key
-- `ANTHROPIC_API_KEY` — Anthropic API key (for `claude -p --bare`)
+```bash
+# One-time setup, done once per machine:
+codex auth login    # uses ChatGPT subscription
+gemini auth         # uses Google account
+claude              # interactive setup with Anthropic Console
+```
 
-The workflow:
+Verify:
 
-1. Identifies the previous tag (chronological).
-2. Installs the three CLIs via npm.
-3. Runs `scripts/triple_review.sh <prev-tag>`.
-4. Attaches every `reviews/*.md` to the just-published GitHub Release.
-5. Uploads the same files as a 90-day workflow artefact.
+```bash
+codex --version
+gemini --version
+claude --version
+```
+
+If any of the three are missing, the orchestrator script will refuse to
+run that reviewer and exit non-zero. Don't paper over by skipping; the
+whole point is the three-way coverage.
 
 ## How to read the output
 
@@ -79,6 +115,11 @@ matters is the *intersection* and *symmetric difference* of findings:
 - **No concerns from any** → suspicious. Either the diff is genuinely
   trivial, or the prompt scope was too narrow.
 
+Round 8 (alpha.9..alpha.10) provides the canonical false-positive example:
+Claude confidently asserted that `pyarrow.lib.ArrowInvalid` does not
+subclass `ValueError`, prescribing a fix that would have been merged in
+isolation. Codex's runtime probe (`ArrowInvalid.__mro__`) overruled it.
+
 The summary file (`reviews/<sha>.summary.md`) lists which files each
 reviewer touched, but does NOT consolidate findings. That's deliberate:
 a meta-AI summary would re-introduce the single-reviewer bias the whole
@@ -107,6 +148,20 @@ The release commit message should call out each fixed concern by reviewer
 This makes the audit trail readable to future maintainers (and to the
 next round of reviewers).
 
+## Convergence criterion
+
+A release converges when **Codex reports zero P0 and zero P1** by its
+explicit P-tag taxonomy. Gemini and Claude do not use P-tags consistently;
+their content-level concerns are documented as known limitations rather
+than triggering further fix-rounds.
+
+The pre-committed budget is **3 fix-rounds maximum** between any two
+adjacent releases. If round 4+ would be needed, ship with documented
+residuals instead. This protects against the failure mode of iterating
+reviewer feedback indefinitely — where each round's fix introduces enough
+new surface for the next round to find another concern, with no
+convergence proof.
+
 ## What this process is NOT
 
 - **Not a substitute for human review.** The triple-review surfaces
@@ -115,22 +170,8 @@ next round of reviewers).
 - **Not a substitute for tests.** Reviewers can identify missing test
   coverage but can't write the tests for you (or rather, they can, but
   the tests they suggest still need human-judged correctness).
-- **Not free.** Each review uses paid LLM API tokens. Budget accordingly;
-  the GitHub Action is rate-limited to one run per tag push.
-
-## Cost back-of-envelope
-
-A 3000-line diff review costs approximately:
-
-| reviewer | tokens in | tokens out | cost |
-|---|---|---|---|
-| Codex | ~30k | ~3k | $0.50 |
-| Gemini | ~30k | ~3k | $0.10 |
-| Claude | ~30k | ~3k | $0.45 |
-| total / tag | | | **~$1.05** |
-
-Acceptable for the audit-trail value. Cheaper than fixing alpha.2 in
-production after beta users found the broken AppImage.
+- **Not free** — but cheap, since it runs on existing subscriptions
+  rather than per-call API billing.
 
 ## Origin
 
@@ -141,4 +182,6 @@ in one paragraph. Gemini-then-Codex-then-Claude rounds since have each
 caught what the others missed.
 
 The first triple-review was on v0.1.0-alpha.4 — see
-[docs/reviews/v0.1.0-alpha.4/](reviews/v0.1.0-alpha.4/).
+[docs/reviews/v0.1.0-alpha.4/](reviews/v0.1.0-alpha.4/). The convergence
+proof and 8-round audit trail are in
+[docs/reviews/v0.1.0-alpha.10/](reviews/v0.1.0-alpha.10/).
diff --git a/pyproject.toml b/pyproject.toml
index 8771462..79950be 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -4,7 +4,7 @@ build-backend = "hatchling.build"
 
 [project]
 name = "openlimno"
-version = "0.1.0a10"
+version = "0.1.0a13"
 description = "Open-source water ecology modeling platform — PHABSIM/IFIM modernization with SCHISM 2D wrapping"
 readme = "README.md"
 requires-python = ">=3.11,<3.13"
diff --git a/scripts/triple_review.sh b/scripts/triple_review.sh
index 477b0f5..dcf2ab8 100755
--- a/scripts/triple_review.sh
+++ b/scripts/triple_review.sh
@@ -19,7 +19,10 @@
 #     reviews/<sha>.claude.md
 #     reviews/<sha>.summary.md   (cross-references all three)
 #
-# Requires: codex, gemini, claude CLIs on PATH; git working tree clean.
+# Requires: codex, gemini, claude CLIs on PATH AND each logged in via
+# its subscription (ChatGPT / Google AI / Anthropic Console). API-key
+# auth is intentionally not used — see docs/triple_review.md for why.
+# Git working tree must be clean.
 
 set -euo pipefail
 
@@ -27,23 +30,45 @@ REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
 cd "$REPO_ROOT"
 
 BASE="${1:-${BASE:-origin/main}}"
+# Round-3 review (Claude): defend against ``-``-prefixed BASE values
+# being interpreted as flags by downstream git commands. (``--``
+# after ``--verify`` doesn't work — it makes git treat the arg as a
+# pathspec — and ``--end-of-options`` isn't honored uniformly across
+# rev-parse modes. A simple upfront pattern check is the most
+# portable form of belt-and-suspenders.)
+case "$BASE" in
+    -*)
+        echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
+        exit 2
+        ;;
+esac
 # Round-4 review (Gemini) caught that the previous `case` blacklist
 # missed parens, braces, newlines. ``git check-ref-format`` is git's
 # own canonical validator — strictly stricter and standard. We allow
 # 40-char SHAs in addition since ``check-ref-format`` rejects bare
 # hex strings. Refnames containing slashes (e.g. ``origin/main``) need
 # ``--allow-onelevel``.
-# Round-7 review (Codex+Gemini): the previous check rejected ALL hex
-# strings under 8 chars, including legitimate branches named "cafe",
-# "dead", "beef" (4 chars). It also missed UPPERCASE 7-char SHAs
-# like 'A72350F' since the regex was lowercase-only. Now reject
-# ONLY the git-default 7-char short SHA length, case-insensitive.
-# Branches with hex-shaped names of other lengths still allowed,
-# 8+ char SHAs allowed.
-base_lower="$(echo "$BASE" | tr 'A-F' 'a-f')"
-if [[ "$base_lower" =~ ^[0-9a-f]{7}$ ]]; then
-    echo "ERROR: 7-char short SHAs are collision-prone — pass full SHA or 8+ chars" >&2
-    exit 2
+# Round-1 review (Codex P2 + Gemini + Claude consensus, post-alpha.10):
+# the previous "reject only 7-char hex" rule still let 4-6 char
+# abbreviated SHAs (e.g. ``git rev-parse --short=6``) bypass collision
+# protection — they pass ``check-ref-format --allow-onelevel`` as
+# valid one-level refnames. Disambiguate by checking whether the
+# hex-shaped value is *actually* a real branch/tag in this repo. If
+# it is, allow (legit ``cafe`` branch). If it's not, reject as an
+# abbreviated SHA. Round-7's case-insensitive lowercasing preserved.
+# 8+ char SHAs and refnames-with-slashes unchanged.
+base_lower="$(printf '%s' "$BASE" | tr 'A-F' 'a-f')"
+if [[ "$base_lower" =~ ^[0-9a-f]{4,7}$ ]]; then
+    # Round-2 review (Claude P1 #4): the previous version checked
+    # ``show-ref --verify`` against the *original-case* $BASE while
+    # the regex tested $base_lower, so a user passing ``CAFE`` for a
+    # real ``cafe`` branch hit the false-reject path. ``rev-parse
+    # --symbolic-full-name`` is case-sensitive (correctly) and covers
+    # heads/tags/remotes/HEAD-relative in one call.
+    if [ -z "$(git rev-parse --symbolic-full-name "$BASE" 2>/dev/null)" ]; then
+        echo "ERROR: '$BASE' looks like an abbreviated SHA (collision-prone). Pass the full 40-char SHA or use the actual ref name." >&2
+        exit 2
+    fi
 fi
 if ! [[ "$base_lower" =~ ^[0-9a-f]{8,40}$ ]] \
         && ! git check-ref-format --allow-onelevel "$BASE" 2>/dev/null; then
@@ -150,18 +175,22 @@ PID_GEMINI=$!
         echo
         echo "_Generated by \`claude -p\` on $(date -Iseconds)_"
         echo
-        # Two modes:
-        #   * CI-style (ANTHROPIC_API_KEY set): --bare for hermetic, stateless
-        #     invocation. Skips keychain / hooks / plugin sync. Required for
-        #     reproducible review on a clean GitHub Actions runner.
-        #   * Local-style (no API key): rely on the user's logged-in session.
-        # Either way we feed the prompt + diff via stdin (NOT argv) so we
-        # don't hit ARG_MAX on big diffs.
-        if [ -n "${ANTHROPIC_API_KEY:-}" ]; then
-            CLAUDE_FLAGS=(-p --bare --add-dir "$REPO_ROOT")
-        else
-            CLAUDE_FLAGS=(-p --add-dir "$REPO_ROOT")
-        fi
+        # CLI-only: rely on the user's logged-in `claude` session
+        # (Anthropic Console subscription). API-key mode (--bare with
+        # ANTHROPIC_API_KEY) is intentionally not supported — see
+        # docs/triple_review.md "Why CLI subscription, not API". Feed
+        # prompt + diff via stdin (NOT argv) to dodge ARG_MAX on big
+        # diffs.
+        # Round-2 review (Codex P2): `claude -p` silently honors
+        # ANTHROPIC_API_KEY whenever it's set. A stale env var from
+        # an old API-based setup would either fail before reaching
+        # the subscription session, or worse, bill the API silently
+        # despite the "CLI-only" promise. Strip it for this invocation
+        # via `env -u`.
+        # Round-3 review (Claude): also strip ANTHROPIC_AUTH_TOKEN
+        # and ANTHROPIC_BASE_URL — claude-code honors the whole
+        # family. Stripping only one of the three would still let
+        # API auth sneak in.
         {
             cat "$PROMPT_FILE"
             echo
@@ -169,7 +198,8 @@ PID_GEMINI=$!
             echo '```diff'
             cat "$DIFF_FILE"
             echo '```'
-        } | claude "${CLAUDE_FLAGS[@]}" 2>&1
+        } | env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_BASE_URL \
+                claude -p --add-dir "$REPO_ROOT" 2>&1
     } > "$OUT_CLAUDE"
 ) &
 PID_CLAUDE=$!
diff --git a/src/openlimno/gui_core/controller.py b/src/openlimno/gui_core/controller.py
index 8acd0f5..ff8cae1 100644
--- a/src/openlimno/gui_core/controller.py
+++ b/src/openlimno/gui_core/controller.py
@@ -38,6 +38,19 @@ def _read_wua_csv(path: str) -> list[dict[str, str]]:
     return list(csv.DictReader(data_lines))
 
 
+class MissingParquetBackend(RuntimeError):
+    """Neither pyarrow nor GDAL/OGR is importable.
+
+    Inherits from ``RuntimeError`` so the GUI direct-call sites'
+    existing ``except (..., RuntimeError)`` still surfaces a friendly
+    QMessageBox. The cache retry loop in
+    ``Controller._read_xs_rows_cached`` checks for this subtype
+    *before* the broader RuntimeError clause so it short-circuits
+    instead of burning the retry budget on a permanent install
+    problem (round-2 review — Claude P1 #3 + Gemini).
+    """
+
+
 def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
     """Read parquet via pyarrow (preferred) with GDAL/OGR fallback.
 
@@ -51,10 +64,21 @@ def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
     """
     try:
         import pyarrow.parquet as pq
+        from pyarrow.lib import ArrowException
     except ImportError:
         pq = None
+        ArrowException = None
     if pq is not None:
-        t = pq.read_table(path)  # raises ArrowInvalid on torn footer, etc.
+        try:
+            t = pq.read_table(path)
+        except ArrowException as e:
+            # Round-3 review (Claude): only ArrowInvalid subclasses
+            # ``ValueError`` and only ArrowIOError subclasses ``OSError``;
+            # ArrowCapacityError / ArrowKeyError / ArrowNotImplemented
+            # would escape the cache wrapper's exception tuple and crash
+            # the GUI. Normalise the whole family to ``OSError`` so
+            # callers don't need to track pyarrow's MRO.
+            raise OSError(f"parquet read failed: {e}") from e
         return [
             dict(zip(t.column_names, row, strict=False))
             for row in zip(*[c.to_pylist() for c in t.columns], strict=False)
@@ -63,16 +87,28 @@ def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
     try:
         from osgeo import ogr
     except ImportError as e:
-        raise RuntimeError(
+        raise MissingParquetBackend(
             "neither pyarrow nor GDAL available to read parquet"
         ) from e
     ds = ogr.Open(path)
     if ds is None:
         raise OSError(f"GDAL/OGR failed to open {path}")
-    layer = ds.GetLayer(0)
-    defn = layer.GetLayerDefn()
-    field_names = [defn.GetFieldDefn(i).GetName() for i in range(defn.GetFieldCount())]
-    return [{fn: feat.GetField(fn) for fn in field_names} for feat in layer]
+    try:
+        # Round-2 review (Claude P1 #1): malformed datasets can return
+        # ``None`` from ``GetLayer(0)``; without this guard the next
+        # ``GetLayerDefn()`` raises ``AttributeError`` which the cache
+        # retry loop classifies as a programmer bug and propagates uncaught.
+        layer = ds.GetLayer(0)
+        if layer is None:
+            raise OSError(f"GDAL/OGR returned no layer in {path}")
+        defn = layer.GetLayerDefn()
+        field_names = [defn.GetFieldDefn(i).GetName() for i in range(defn.GetFieldCount())]
+        return [{fn: feat.GetField(fn) for fn in field_names} for feat in layer]
+    finally:
+        # OGR's idiomatic close: assigning ``None`` releases the C++
+        # reference and frees the file handle. Long PyQt sessions
+        # otherwise accumulate open fds across repeated WUA-Q opens.
+        ds = None  # noqa: F841
 
 
 
@@ -130,7 +166,8 @@ class Controller:
 
     def open_wua_q(self) -> None:
         from qgis.PyQt.QtWidgets import (
-            QDialog, QFileDialog, QTableWidget, QTableWidgetItem, QVBoxLayout,
+            QDialog, QFileDialog, QMessageBox, QTableWidget, QTableWidgetItem,
+            QVBoxLayout,
         )
 
         path, _ = QFileDialog.getOpenFileName(
@@ -141,7 +178,16 @@ class Controller:
         )
         if not path:
             return
-        rows = _read_wua_csv(path) if path.endswith(".csv") else _read_wua_parquet(path)
+        # Round-1 review (Codex P2 + Claude): _read_wua_parquet now raises
+        # on torn parquet (alpha.10 round-7 change); without this guard,
+        # selecting a corrupt file in the dialog crashes the GUI thread
+        # via the unhandled exception path.
+        try:
+            rows = _read_wua_csv(path) if path.endswith(".csv") else _read_wua_parquet(path)
+        except (OSError, EOFError, ValueError, RuntimeError) as e:
+            QMessageBox.warning(self.host.main_window(), "OpenLimno",
+                                f"Could not read {path}:\n{e}")
+            return
         if not rows:
             return
         dlg = QDialog(self.host.main_window())
@@ -171,7 +217,16 @@ class Controller:
         )
         if not xs_path:
             return
-        rows = _read_wua_parquet(xs_path)
+        # Round-1 review: _read_wua_parquet raises on torn parquet
+        # (alpha.10 round-7); collapse exception + empty-rows into one
+        # error UX path so corrupt files don't escape into Qt's default
+        # exception handler.
+        try:
+            rows = _read_wua_parquet(xs_path)
+        except (OSError, EOFError, ValueError, RuntimeError) as e:
+            QMessageBox.warning(self.host.main_window(), "OpenLimno",
+                                f"Could not read {xs_path}:\n{e}")
+            return
         if not rows:
             QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                   f"Could not read {xs_path}")
@@ -878,6 +933,12 @@ class Controller:
                 continue
             try:
                 rows = _read_wua_parquet(path)
+            except MissingParquetBackend:
+                # Round-2 review (Claude P1 #3 + Gemini): permanent
+                # install failure — no point retrying with backoff.
+                # Propagate immediately so the caller sees a fast,
+                # clear error rather than 150 ms of UI freeze.
+                raise
             except (OSError, EOFError, RuntimeError) as e:
                 # Round-7 review (all 3): narrow from `except Exception`
                 # to the transient I/O / read-failure family. Don't

exec
/bin/bash -lc "git diff 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40 -- src/openlimno/gui_core/controller.py | sed -n '1,260p'" in /mnt/data/openlimno
 succeeded in 0ms:
diff --git a/src/openlimno/gui_core/controller.py b/src/openlimno/gui_core/controller.py
index 8acd0f5..ff8cae1 100644
--- a/src/openlimno/gui_core/controller.py
+++ b/src/openlimno/gui_core/controller.py
@@ -38,6 +38,19 @@ def _read_wua_csv(path: str) -> list[dict[str, str]]:
     return list(csv.DictReader(data_lines))
 
 
+class MissingParquetBackend(RuntimeError):
+    """Neither pyarrow nor GDAL/OGR is importable.
+
+    Inherits from ``RuntimeError`` so the GUI direct-call sites'
+    existing ``except (..., RuntimeError)`` still surfaces a friendly
+    QMessageBox. The cache retry loop in
+    ``Controller._read_xs_rows_cached`` checks for this subtype
+    *before* the broader RuntimeError clause so it short-circuits
+    instead of burning the retry budget on a permanent install
+    problem (round-2 review — Claude P1 #3 + Gemini).
+    """
+
+
 def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
     """Read parquet via pyarrow (preferred) with GDAL/OGR fallback.
 
@@ -51,10 +64,21 @@ def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
     """
     try:
         import pyarrow.parquet as pq
+        from pyarrow.lib import ArrowException
     except ImportError:
         pq = None
+        ArrowException = None
     if pq is not None:
-        t = pq.read_table(path)  # raises ArrowInvalid on torn footer, etc.
+        try:
+            t = pq.read_table(path)
+        except ArrowException as e:
+            # Round-3 review (Claude): only ArrowInvalid subclasses
+            # ``ValueError`` and only ArrowIOError subclasses ``OSError``;
+            # ArrowCapacityError / ArrowKeyError / ArrowNotImplemented
+            # would escape the cache wrapper's exception tuple and crash
+            # the GUI. Normalise the whole family to ``OSError`` so
+            # callers don't need to track pyarrow's MRO.
+            raise OSError(f"parquet read failed: {e}") from e
         return [
             dict(zip(t.column_names, row, strict=False))
             for row in zip(*[c.to_pylist() for c in t.columns], strict=False)
@@ -63,16 +87,28 @@ def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
     try:
         from osgeo import ogr
     except ImportError as e:
-        raise RuntimeError(
+        raise MissingParquetBackend(
             "neither pyarrow nor GDAL available to read parquet"
         ) from e
     ds = ogr.Open(path)
     if ds is None:
         raise OSError(f"GDAL/OGR failed to open {path}")
-    layer = ds.GetLayer(0)
-    defn = layer.GetLayerDefn()
-    field_names = [defn.GetFieldDefn(i).GetName() for i in range(defn.GetFieldCount())]
-    return [{fn: feat.GetField(fn) for fn in field_names} for feat in layer]
+    try:
+        # Round-2 review (Claude P1 #1): malformed datasets can return
+        # ``None`` from ``GetLayer(0)``; without this guard the next
+        # ``GetLayerDefn()`` raises ``AttributeError`` which the cache
+        # retry loop classifies as a programmer bug and propagates uncaught.
+        layer = ds.GetLayer(0)
+        if layer is None:
+            raise OSError(f"GDAL/OGR returned no layer in {path}")
+        defn = layer.GetLayerDefn()
+        field_names = [defn.GetFieldDefn(i).GetName() for i in range(defn.GetFieldCount())]
+        return [{fn: feat.GetField(fn) for fn in field_names} for feat in layer]
+    finally:
+        # OGR's idiomatic close: assigning ``None`` releases the C++
+        # reference and frees the file handle. Long PyQt sessions
+        # otherwise accumulate open fds across repeated WUA-Q opens.
+        ds = None  # noqa: F841
 
 
 
@@ -130,7 +166,8 @@ class Controller:
 
     def open_wua_q(self) -> None:
         from qgis.PyQt.QtWidgets import (
-            QDialog, QFileDialog, QTableWidget, QTableWidgetItem, QVBoxLayout,
+            QDialog, QFileDialog, QMessageBox, QTableWidget, QTableWidgetItem,
+            QVBoxLayout,
         )
 
         path, _ = QFileDialog.getOpenFileName(
@@ -141,7 +178,16 @@ class Controller:
         )
         if not path:
             return
-        rows = _read_wua_csv(path) if path.endswith(".csv") else _read_wua_parquet(path)
+        # Round-1 review (Codex P2 + Claude): _read_wua_parquet now raises
+        # on torn parquet (alpha.10 round-7 change); without this guard,
+        # selecting a corrupt file in the dialog crashes the GUI thread
+        # via the unhandled exception path.
+        try:
+            rows = _read_wua_csv(path) if path.endswith(".csv") else _read_wua_parquet(path)
+        except (OSError, EOFError, ValueError, RuntimeError) as e:
+            QMessageBox.warning(self.host.main_window(), "OpenLimno",
+                                f"Could not read {path}:\n{e}")
+            return
         if not rows:
             return
         dlg = QDialog(self.host.main_window())
@@ -171,7 +217,16 @@ class Controller:
         )
         if not xs_path:
             return
-        rows = _read_wua_parquet(xs_path)
+        # Round-1 review: _read_wua_parquet raises on torn parquet
+        # (alpha.10 round-7); collapse exception + empty-rows into one
+        # error UX path so corrupt files don't escape into Qt's default
+        # exception handler.
+        try:
+            rows = _read_wua_parquet(xs_path)
+        except (OSError, EOFError, ValueError, RuntimeError) as e:
+            QMessageBox.warning(self.host.main_window(), "OpenLimno",
+                                f"Could not read {xs_path}:\n{e}")
+            return
         if not rows:
             QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                   f"Could not read {xs_path}")
@@ -878,6 +933,12 @@ class Controller:
                 continue
             try:
                 rows = _read_wua_parquet(path)
+            except MissingParquetBackend:
+                # Round-2 review (Claude P1 #3 + Gemini): permanent
+                # install failure — no point retrying with backoff.
+                # Propagate immediately so the caller sees a fast,
+                # clear error rather than 150 ms of UI freeze.
+                raise
             except (OSError, EOFError, RuntimeError) as e:
                 # Round-7 review (all 3): narrow from `except Exception`
                 # to the transient I/O / read-failure family. Don't

exec
/bin/bash -lc "git diff 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40 -- src/openlimno/gui_core/controller.py | sed -n '260,520p'" in /mnt/data/openlimno
 succeeded in 0ms:
exec
/bin/bash -lc "sed -n '1,260p' src/openlimno/gui_core/controller.py && sed -n '820,960p' src/openlimno/gui_core/controller.py" in /mnt/data/openlimno
 succeeded in 0ms:
"""Controller — the single source of truth for plugin + Studio actions.

Both the QGIS plugin and OpenLimno Studio instantiate one ``Controller``
per session, passing a ``Host`` adapter that exposes the QGIS-iface-shaped
methods the handlers need. Handlers don't import ``iface`` directly.
"""
from __future__ import annotations

import csv
import math
import os
import time
from pathlib import Path
from typing import Any, Protocol


class Host(Protocol):
    """Adapter interface implemented by both PluginHost and Studio MainWindow.

    The four methods mirror QGIS iface, so the handler bodies (which were
    originally written against ``self.iface``) port over almost verbatim.
    """

    def main_window(self) -> Any: ...
    def map_canvas(self) -> Any: ...
    def message_bar(self) -> Any: ...
    def status_bar(self) -> Any: ...


# ---------------------------------------------------------------------------
# Helpers (no Qt / iface dependency)
# ---------------------------------------------------------------------------
def _read_wua_csv(path: str) -> list[dict[str, str]]:
    with open(path, encoding="utf-8") as f:
        data_lines = [line for line in f if not line.lstrip().startswith("#")]
    if not data_lines:
        return []
    return list(csv.DictReader(data_lines))


class MissingParquetBackend(RuntimeError):
    """Neither pyarrow nor GDAL/OGR is importable.

    Inherits from ``RuntimeError`` so the GUI direct-call sites'
    existing ``except (..., RuntimeError)`` still surfaces a friendly
    QMessageBox. The cache retry loop in
    ``Controller._read_xs_rows_cached`` checks for this subtype
    *before* the broader RuntimeError clause so it short-circuits
    instead of burning the retry budget on a permanent install
    problem (round-2 review — Claude P1 #3 + Gemini).
    """


def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
    """Read parquet via pyarrow (preferred) with GDAL/OGR fallback.

    Round-7 review (Codex P1): the previous implementation caught
    ``Exception`` at both backends and returned ``[]`` on failure.
    That made ``Controller._read_xs_rows_cached``'s retry-on-exception
    code unreachable in production — torn reads silently produced
    empty rows that got cached. Now only ``ImportError`` falls through
    between backends; read failures propagate so the cache layer can
    act on them.
    """
    try:
        import pyarrow.parquet as pq
        from pyarrow.lib import ArrowException
    except ImportError:
        pq = None
        ArrowException = None
    if pq is not None:
        try:
            t = pq.read_table(path)
        except ArrowException as e:
            # Round-3 review (Claude): only ArrowInvalid subclasses
            # ``ValueError`` and only ArrowIOError subclasses ``OSError``;
            # ArrowCapacityError / ArrowKeyError / ArrowNotImplemented
            # would escape the cache wrapper's exception tuple and crash
            # the GUI. Normalise the whole family to ``OSError`` so
            # callers don't need to track pyarrow's MRO.
            raise OSError(f"parquet read failed: {e}") from e
        return [
            dict(zip(t.column_names, row, strict=False))
            for row in zip(*[c.to_pylist() for c in t.columns], strict=False)
        ]
    # No pyarrow: try GDAL OGR.
    try:
        from osgeo import ogr
    except ImportError as e:
        raise MissingParquetBackend(
            "neither pyarrow nor GDAL available to read parquet"
        ) from e
    ds = ogr.Open(path)
    if ds is None:
        raise OSError(f"GDAL/OGR failed to open {path}")
    try:
        # Round-2 review (Claude P1 #1): malformed datasets can return
        # ``None`` from ``GetLayer(0)``; without this guard the next
        # ``GetLayerDefn()`` raises ``AttributeError`` which the cache
        # retry loop classifies as a programmer bug and propagates uncaught.
        layer = ds.GetLayer(0)
        if layer is None:
            raise OSError(f"GDAL/OGR returned no layer in {path}")
        defn = layer.GetLayerDefn()
        field_names = [defn.GetFieldDefn(i).GetName() for i in range(defn.GetFieldCount())]
        return [{fn: feat.GetField(fn) for fn in field_names} for feat in layer]
    finally:
        # OGR's idiomatic close: assigning ``None`` releases the C++
        # reference and frees the file handle. Long PyQt sessions
        # otherwise accumulate open fds across repeated WUA-Q opens.
        ds = None  # noqa: F841




# ---------------------------------------------------------------------------
# Controller
# ---------------------------------------------------------------------------
class Controller:
    """Holds session state and exposes handler methods.

    State lifecycle:
    - ``_xs_parquet`` / ``_hyd_nc``: auto-discovery cache, reset by
      build_case_from_osm and refreshed by activate_pick_tool
    - ``_pick_tool``: active QgsMapTool (None when click-tool is off)
    - ``_run_case_proc``: keeps QProcess alive while CLI runs
    """

    def __init__(self, host: Host) -> None:
        self.host = host
        self._xs_parquet: str | None = None
        self._hyd_nc: str | None = None
        self._pick_tool: Any = None
        self._pick_action: Any = None  # set by UI when an action is checkable
        self._run_case_proc: Any = None
        self._run_case_log: bytearray = bytearray()

    # ------------------------------------------------------------------
    # Open existing results
    # ------------------------------------------------------------------
    def open_hydraulic_nc(self) -> None:
        from qgis.core import QgsMeshLayer, QgsProject, QgsRasterLayer
        from qgis.PyQt.QtWidgets import QFileDialog, QMessageBox

        path, _ = QFileDialog.getOpenFileName(
            self.host.main_window(),
            "Open OpenLimno hydraulics NetCDF",
            "",
            "OpenLimno NetCDF (*.nc);;All files (*)",
        )
        if not path:
            return
        layer_name = os.path.basename(path)
        mesh = QgsMeshLayer(path, layer_name, "mdal")
        if mesh.isValid():
            QgsProject.instance().addMapLayer(mesh)
            return
        rast = QgsRasterLayer(f'NETCDF:"{path}":water_depth', f"{layer_name}:water_depth")
        if rast.isValid():
            QgsProject.instance().addMapLayer(rast)
        else:
            QMessageBox.warning(
                self.host.main_window(), "OpenLimno",
                f"Could not load {path} as mesh or raster.",
            )

    def open_wua_q(self) -> None:
        from qgis.PyQt.QtWidgets import (
            QDialog, QFileDialog, QMessageBox, QTableWidget, QTableWidgetItem,
            QVBoxLayout,
        )

        path, _ = QFileDialog.getOpenFileName(
            self.host.main_window(),
            "Open WUA-Q file",
            "",
            "WUA-Q (*.csv *.parquet);;All files (*)",
        )
        if not path:
            return
        # Round-1 review (Codex P2 + Claude): _read_wua_parquet now raises
        # on torn parquet (alpha.10 round-7 change); without this guard,
        # selecting a corrupt file in the dialog crashes the GUI thread
        # via the unhandled exception path.
        try:
            rows = _read_wua_csv(path) if path.endswith(".csv") else _read_wua_parquet(path)
        except (OSError, EOFError, ValueError, RuntimeError) as e:
            QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                f"Could not read {path}:\n{e}")
            return
        if not rows:
            return
        dlg = QDialog(self.host.main_window())
        dlg.setWindowTitle(f"WUA-Q: {os.path.basename(path)}")
        layout = QVBoxLayout(dlg)
        headers = list(rows[0].keys())
        table = QTableWidget(len(rows), len(headers), dlg)
        table.setHorizontalHeaderLabels(headers)
        for i, row in enumerate(rows):
            for j, h in enumerate(headers):
                table.setItem(i, j, QTableWidgetItem(str(row.get(h, ""))))
        layout.addWidget(table)
        dlg.resize(640, 480)
        dlg.exec()

    def plot_cross_section(self) -> None:
        from qgis.PyQt.QtWidgets import (
            QComboBox, QDialog, QDialogButtonBox, QFileDialog,
            QFormLayout, QMessageBox, QVBoxLayout,
        )

        xs_path, _ = QFileDialog.getOpenFileName(
            self.host.main_window(),
            "Open cross_section.parquet",
            "",
            "OpenLimno cross-sections (*.parquet);;All files (*)",
        )
        if not xs_path:
            return
        # Round-1 review: _read_wua_parquet raises on torn parquet
        # (alpha.10 round-7); collapse exception + empty-rows into one
        # error UX path so corrupt files don't escape into Qt's default
        # exception handler.
        try:
            rows = _read_wua_parquet(xs_path)
        except (OSError, EOFError, ValueError, RuntimeError) as e:
            QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                f"Could not read {xs_path}:\n{e}")
            return
        if not rows:
            QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                  f"Could not read {xs_path}")
            return
        stations = sorted({float(r["station_m"]) for r in rows})

        hyd_path, _ = QFileDialog.getOpenFileName(
            self.host.main_window(),
            "Open hydraulics.nc (cancel for bed-only plot)",
            "",
            "OpenLimno hydraulics (*.nc);;All files (*)",
        )

        dlg = QDialog(self.host.main_window())
        dlg.setWindowTitle("Plot cross-section")
        form = QFormLayout(dlg)
        cb_station = QComboBox(); cb_station.addItems([f"{s:g}" for s in stations])
        form.addRow("station_m:", cb_station)
        cb_q = QComboBox()
        discharges: list[float] = []
        if hyd_path:
            try:
                from netCDF4 import Dataset
                ds = Dataset(hyd_path)
                if "discharge" in ds.variables:
                    discharges = [float(v) for v in ds.variables["discharge"][:]]
                ds.close()
                cb_q.addItems([f"{q:g}" for q in discharges])
                form.addRow("Q (m³/s):", cb_q)
            except Exception as e:
                QMessageBox.warning(self.host.main_window(), "OpenLimno",
                        except Exception:
                            continue
                        # Approximate tolerance in layer units by transforming
                        # a small offset point and taking the resulting delta.
                        try:
                            offset = xform.transform(QgsPointXY(pt.x() + tol, pt.y()))
                            tol_l = abs(offset.x() - pt_l.x()) or tol
                        except Exception:
                            tol_l = tol
                    else:
                        pt_l = pt
                        tol_l = tol
                    bb = QgsRectangle(pt_l.x() - tol_l, pt_l.y() - tol_l,
                                        pt_l.x() + tol_l, pt_l.y() + tol_l)
                    req = QgsFeatureRequest().setFilterRect(bb)
                    feats = list(layer.getFeatures(req))
                    if not feats:
                        continue
                    pt_geom = QgsGeometry.fromPointXY(pt_l)
                    f = min(feats, key=lambda ff: ff.geometry().distance(pt_geom))
                    fields = [fd.name() for fd in layer.fields()]
                    if "station_m" in fields:
                        try:
                            station = float(f["station_m"]); hit_layer = layer.name(); break
                        except (TypeError, ValueError):
                            pass
                    if "node_id" in fields:
                        try:
                            station = float(f["node_id"]) * 100.0
                            hit_layer = layer.name(); break
                        except (TypeError, ValueError):
                            pass
                if station is None:
                    controller.host.message_bar().pushMessage(
                        "OpenLimno",
                        f"No mesh-node / cross-section feature within {tol:.6f}° of click",
                        level=1, duration=5,
                    )
                    return
                controller.host.message_bar().pushMessage(
                    "OpenLimno", f"hit {hit_layer} → station {station:g} m",
                    level=0, duration=3,
                )
                controller._plot_at_station(station)

        self._pick_tool = _ClickTool(canvas)
        canvas.setMapTool(self._pick_tool)
        self.host.message_bar().pushMessage(
            "OpenLimno",
            "Click any mesh node or cross-section to view its profile. "
            "Click the toolbar button again to deactivate.",
            level=0, duration=10,
        )

    def _read_xs_rows_cached(self, path: str, max_retries: int = 3) -> list:
        """Read cross_section.parquet rows with a stat→read→stat guard.

        Round-3 review (codex+gemini+claude unanimous) found the previous
        cache implementation cemented torn rows when the file was rewritten
        during the read: post-read stat agreed with new on-disk metadata,
        so subsequent clicks served the corrupt cache forever. The correct
        pattern is:
            pre = stat()
            rows = read()
            post = stat()
            if pre == post: cache (rows, pre); return rows
            else: retry (file was rewritten mid-read)

        On stat failure (file deleted/unmounted), prefer serving the
        previously-cached rows over crashing — better UX, and the user
        will see the next legitimate read fail with a real error.
        """
        # Normalize so cache keys are stable across cwd changes (Qt file
        # dialogs sometimes ``chdir``).
        path = os.path.realpath(path)
        cache = getattr(self, "_xs_rows_cache", {})

        def _stat(p):
            try:
                return (os.path.getmtime(p), os.path.getsize(p))
            except OSError:
                return None

        cur = _stat(path)
        # Cache hit — fast path
        if (cur is not None
                and cache.get("path") == path
                and cache.get("stat") == cur):
            return cache["rows"]
        # Stat failed — keep stale cache rather than crash
        if cur is None and cache.get("path") == path and cache.get("rows"):
            return cache["rows"]
        # Cache miss: read with TOCTOU guard. Each transient failure
        # mode (stat-fails, parquet-read-raises, post-stat-None, pre/post
        # disagree) records last_error and continues into the next
        # attempt — only when retries are exhausted do we fall back to
        # the prior cache or raise. Round-6 fixes:
        #   - Codex P2 / Claude P1: ``post is None`` was raising on
        #     attempt 0, defeating the retry budget for `mv -f`-style
        #     transient unlinks. Now it `continue`s.
        #   - Gemini P0: parquet readers raise on torn input
        #     (ArrowInvalid, EOFError, OSError). The previous code
        #     let those propagate out, bypassing retry. Wrap.
        #   - Claude P1: ``cache.get("rows")`` was falsy on empty
        #     rows; an empty parquet would be treated as no-cache.
        #     Use ``"rows" in cache`` instead.
        last_error = None
        for attempt in range(max_retries):
            if attempt > 0:
                time.sleep(0.05 * attempt)
            pre = _stat(path)
            if pre is None:
                last_error = OSError(f"cannot stat {path}")
                continue
            try:
                rows = _read_wua_parquet(path)
            except MissingParquetBackend:
                # Round-2 review (Claude P1 #3 + Gemini): permanent
                # install failure — no point retrying with backoff.
                # Propagate immediately so the caller sees a fast,
                # clear error rather than 150 ms of UI freeze.
                raise
            except (OSError, EOFError, RuntimeError) as e:
                # Round-7 review (all 3): narrow from `except Exception`
                # to the transient I/O / read-failure family. Don't
                # swallow programmer bugs (TypeError, AttributeError,
                # KeyError) under the retry banner. ArrowInvalid is a
                # subclass of OSError in newer pyarrow, ValueError in
                # older — we add ValueError as a safety net below.
                last_error = e
                continue
            except ValueError as e:
                # pyarrow.lib.ArrowInvalid on torn footers used to be
                # ValueError. Treat as transient.
                last_error = e
                continue
            post = _stat(path)
            if post is None:
                last_error = FileNotFoundError(
                    f"{path} vanished during read")
                continue

exec
/bin/bash -lc "sed -n '960,1040p' src/openlimno/gui_core/controller.py" in /mnt/data/openlimno
 succeeded in 0ms:
                continue
            if pre == post:
                self._xs_rows_cache = {"path": path, "stat": pre, "rows": rows}
                return rows
            last_error = RuntimeError(f"{path} changed during read")
        # Retries exhausted. Prefer prior cache (from a clean read)
        # over raising — caller's downstream logic handles "rows looks
        # familiar" better than "file disappeared". Use ``"rows" in
        # cache`` so an empty list (legitimate) still hits.
        if cache.get("path") == path and "rows" in cache:
            return cache["rows"]
        raise last_error or RuntimeError(f"unable to read {path} stably")

    def _plot_at_station(self, station: float) -> None:
        from qgis.PyQt.QtWidgets import (
            QComboBox, QDialog, QDialogButtonBox, QFormLayout, QMessageBox,
        )

        if not self._xs_parquet:
            QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                  "No cross_section.parquet selected.")
            return
        cache_key = self._xs_parquet
        rows = self._read_xs_rows_cached(cache_key)
        stations = sorted({float(r["station_m"]) for r in rows})
        station = min(stations, key=lambda s: abs(s - station))

        target_Q = None
        if self._hyd_nc:
            try:
                from netCDF4 import Dataset
                ds = Dataset(self._hyd_nc)
                discharges = [float(v) for v in ds.variables["discharge"][:]]
                ds.close()
            except Exception:
                discharges = []
            if discharges:
                dlg = QDialog(self.host.main_window())
                dlg.setWindowTitle(f"Q for station {station:g} m")
                form = QFormLayout(dlg)
                cb = QComboBox(); cb.addItems([f"{q:g}" for q in discharges])
                idx = min(range(len(discharges)),
                            key=lambda i: abs(discharges[i] - 7.0))
                cb.setCurrentIndex(idx)
                form.addRow("Q (m³/s):", cb)
                bb = QDialogButtonBox(QDialogButtonBox.StandardButton.Ok |
                                        QDialogButtonBox.StandardButton.Cancel)
                bb.accepted.connect(dlg.accept); bb.rejected.connect(dlg.reject)
                form.addRow(bb)
                if dlg.exec() == QDialog.DialogCode.Accepted:
                    target_Q = float(cb.currentText())

        self._render_profile_dialog(rows, stations, station, target_Q)

    def _render_profile_dialog(self, rows, stations, target_station, target_Q) -> None:
        from qgis.PyQt.QtWidgets import QDialog, QMessageBox, QVBoxLayout

        sec = sorted(
            (r for r in rows if float(r["station_m"]) == target_station),
            key=lambda r: int(r["point_index"]),
        )
        d = [float(r["distance_m"]) for r in sec]
        z = [float(r["elevation_m"]) for r in sec]

        wse = None
        if target_Q is not None and self._hyd_nc:
            try:
                from netCDF4 import Dataset
                ds = Dataset(self._hyd_nc)
                qs = list(ds.variables["discharge"][:])
                iq = min(range(len(qs)), key=lambda i: abs(float(qs[i]) - target_Q))
                node_idx = stations.index(target_station)
                wse = float(ds.variables["water_surface"][iq, node_idx])
                ds.close()
            except Exception as e:
                QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                      f"WSE extract failed: {e}")

        try:
            from matplotlib.backends.backend_qtagg import FigureCanvasQTAgg
            from matplotlib.figure import Figure

exec
/bin/bash -lc 'python -m py_compile src/openlimno/gui_core/controller.py scripts/triple_review.sh 2>&1 || true' in /mnt/data/openlimno
 succeeded in 0ms:
  File "scripts/triple_review.sh", line 40
    -*)
      ^
SyntaxError: unmatched ')'

exec
/bin/bash -lc 'python -m py_compile src/openlimno/gui_core/controller.py' in /mnt/data/openlimno
 succeeded in 0ms:
exec
/bin/bash -lc "sed -n '1,280p' scripts/triple_review.sh" in /mnt/data/openlimno
 succeeded in 0ms:
#!/usr/bin/env bash
#
# Three-AI code review: runs Codex, Gemini, and Claude Code in parallel
# against the same diff, lands per-reviewer outputs as Markdown.
#
# Why three? alpha.2 shipped non-functional because Claude alone tested
# only the dev-venv path. Codex caught that. Round-2 Codex flagged a
# Gemini-shaped concern. Round-2 Gemini flagged a Codex-shaped concern.
# No single reviewer is sufficient — they have different blind spots.
#
# Usage:
#     scripts/triple_review.sh                # diff against origin/main
#     scripts/triple_review.sh v0.1.0-alpha.3 # diff against given ref
#     BASE=v0.1.0-alpha.3 scripts/triple_review.sh
#
# Outputs:
#     reviews/<sha>.codex.md
#     reviews/<sha>.gemini.md
#     reviews/<sha>.claude.md
#     reviews/<sha>.summary.md   (cross-references all three)
#
# Requires: codex, gemini, claude CLIs on PATH AND each logged in via
# its subscription (ChatGPT / Google AI / Anthropic Console). API-key
# auth is intentionally not used — see docs/triple_review.md for why.
# Git working tree must be clean.

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

BASE="${1:-${BASE:-origin/main}}"
# Round-3 review (Claude): defend against ``-``-prefixed BASE values
# being interpreted as flags by downstream git commands. (``--``
# after ``--verify`` doesn't work — it makes git treat the arg as a
# pathspec — and ``--end-of-options`` isn't honored uniformly across
# rev-parse modes. A simple upfront pattern check is the most
# portable form of belt-and-suspenders.)
case "$BASE" in
    -*)
        echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
        exit 2
        ;;
esac
# Round-4 review (Gemini) caught that the previous `case` blacklist
# missed parens, braces, newlines. ``git check-ref-format`` is git's
# own canonical validator — strictly stricter and standard. We allow
# 40-char SHAs in addition since ``check-ref-format`` rejects bare
# hex strings. Refnames containing slashes (e.g. ``origin/main``) need
# ``--allow-onelevel``.
# Round-1 review (Codex P2 + Gemini + Claude consensus, post-alpha.10):
# the previous "reject only 7-char hex" rule still let 4-6 char
# abbreviated SHAs (e.g. ``git rev-parse --short=6``) bypass collision
# protection — they pass ``check-ref-format --allow-onelevel`` as
# valid one-level refnames. Disambiguate by checking whether the
# hex-shaped value is *actually* a real branch/tag in this repo. If
# it is, allow (legit ``cafe`` branch). If it's not, reject as an
# abbreviated SHA. Round-7's case-insensitive lowercasing preserved.
# 8+ char SHAs and refnames-with-slashes unchanged.
base_lower="$(printf '%s' "$BASE" | tr 'A-F' 'a-f')"
if [[ "$base_lower" =~ ^[0-9a-f]{4,7}$ ]]; then
    # Round-2 review (Claude P1 #4): the previous version checked
    # ``show-ref --verify`` against the *original-case* $BASE while
    # the regex tested $base_lower, so a user passing ``CAFE`` for a
    # real ``cafe`` branch hit the false-reject path. ``rev-parse
    # --symbolic-full-name`` is case-sensitive (correctly) and covers
    # heads/tags/remotes/HEAD-relative in one call.
    if [ -z "$(git rev-parse --symbolic-full-name "$BASE" 2>/dev/null)" ]; then
        echo "ERROR: '$BASE' looks like an abbreviated SHA (collision-prone). Pass the full 40-char SHA or use the actual ref name." >&2
        exit 2
    fi
fi
if ! [[ "$base_lower" =~ ^[0-9a-f]{8,40}$ ]] \
        && ! git check-ref-format --allow-onelevel "$BASE" 2>/dev/null; then
    echo "ERROR: '$BASE' is not a valid ref name or SHA" >&2
    exit 2
fi
HEAD_SHA="$(git rev-parse --short HEAD)"
HEAD_REF="$(git rev-parse --abbrev-ref HEAD)"
REVIEWS_DIR="reviews"
mkdir -p "$REVIEWS_DIR"

OUT_CODEX="$REVIEWS_DIR/${HEAD_SHA}.codex.md"
OUT_GEMINI="$REVIEWS_DIR/${HEAD_SHA}.gemini.md"
OUT_CLAUDE="$REVIEWS_DIR/${HEAD_SHA}.claude.md"
OUT_SUMMARY="$REVIEWS_DIR/${HEAD_SHA}.summary.md"

DIFF_FILE="$(mktemp /tmp/review-diff.XXXXXX.patch)"
trap 'rm -f "$DIFF_FILE"' EXIT

# --- Generate the diff ---------------------------------------------------
echo "==> Generating diff: $BASE..HEAD ($HEAD_SHA on $HEAD_REF)"
# Resolve $BASE to a SHA so we fail loudly on misspelled refs (rather
# than silently producing an empty diff). Round-3 review caught the
# `git diff || true` swallowing this exact failure.
if ! BASE_SHA="$(git rev-parse --verify "${BASE}^{commit}" 2>/dev/null)"; then
    echo "ERROR: can't resolve BASE='$BASE' — fetch it or pass a valid ref" >&2
    exit 2
fi
# No path filter: every changed file gets reviewed, including
# scripts/, .github/workflows/, docs/, pyproject.toml. Round-3 review
# caught the original filter excluding the very files of the orchestrator
# itself.
# Triple-dot semantics: diff against the merge-base, not the raw range.
# Codex round-3 caught that on a feature branch behind main, the
# two-dot form would include the *inverse* of unrelated main commits.
git diff "$BASE_SHA...HEAD" > "$DIFF_FILE"
DIFF_LINES=$(wc -l < "$DIFF_FILE")
echo "    diff: $DIFF_LINES lines"
if [ "$DIFF_LINES" -eq 0 ]; then
    echo "    no changes — nothing to review"
    exit 0
fi

# --- Build the shared review prompt --------------------------------------
PROMPT_FILE="$(mktemp /tmp/review-prompt.XXXXXX.txt)"
trap 'rm -f "$DIFF_FILE" "$PROMPT_FILE"' EXIT

cat > "$PROMPT_FILE" <<EOF
You are reviewing a Git diff on the OpenLimno project (open-source water-
ecology modeling, successor to PHABSIM/River2D/FishXing). The diff range
is $BASE..$HEAD_SHA on branch $HEAD_REF.

Read the unified diff supplied below (~$DIFF_LINES lines), then evaluate.
Cover:

1. Architecture — is the change consistent with what the rest of the
   codebase does? Any unjustified new abstraction?
2. Bug risk — what corner cases are missed? Race conditions, path
   traversal, integer overflow, CRS mismatches, file-handle leaks?
3. Test adequacy — what's NOT covered? Could a regression here pass
   the new tests? What minimum extra coverage would you require?
4. Performance — any O(n^2) hot paths, memory leaks, blocking I/O on
   the GUI thread?
5. Packaging / portability — anything in the build that won't reproduce
   on a clean GitHub Actions runner?
6. Security — anything user-supplied that gets concatenated into shell,
   SQL, or HTTP? Privileged file overwrites? Symlink TOCTOU?

Be specific. Cite file:line where relevant. Do NOT praise. List real
concerns only. Cap at ~600 words.
EOF

# --- Run all three reviewers in parallel ---------------------------------
echo "==> Launching Codex, Gemini, Claude in parallel..."

(
    set +e
    {
        echo "# Codex review — \`$BASE..$HEAD_SHA\`"
        echo
        echo "_Generated by \`codex review --base $BASE\` on $(date -Iseconds)_"
        echo
        codex review --base "$BASE" --title "Triple-review: $HEAD_REF $HEAD_SHA" 2>&1
    } > "$OUT_CODEX"
) &
PID_CODEX=$!

(
    set +e
    {
        echo "# Gemini review — \`$BASE..$HEAD_SHA\`"
        echo
        echo "_Generated by \`gemini -p\` on $(date -Iseconds)_"
        echo
        gemini -p "$(cat "$PROMPT_FILE")" < "$DIFF_FILE" 2>&1
    } > "$OUT_GEMINI"
) &
PID_GEMINI=$!

(
    set +e
    {
        echo "# Claude review — \`$BASE..$HEAD_SHA\`"
        echo
        echo "_Generated by \`claude -p\` on $(date -Iseconds)_"
        echo
        # CLI-only: rely on the user's logged-in `claude` session
        # (Anthropic Console subscription). API-key mode (--bare with
        # ANTHROPIC_API_KEY) is intentionally not supported — see
        # docs/triple_review.md "Why CLI subscription, not API". Feed
        # prompt + diff via stdin (NOT argv) to dodge ARG_MAX on big
        # diffs.
        # Round-2 review (Codex P2): `claude -p` silently honors
        # ANTHROPIC_API_KEY whenever it's set. A stale env var from
        # an old API-based setup would either fail before reaching
        # the subscription session, or worse, bill the API silently
        # despite the "CLI-only" promise. Strip it for this invocation
        # via `env -u`.
        # Round-3 review (Claude): also strip ANTHROPIC_AUTH_TOKEN
        # and ANTHROPIC_BASE_URL — claude-code honors the whole
        # family. Stripping only one of the three would still let
        # API auth sneak in.
        {
            cat "$PROMPT_FILE"
            echo
            echo "Diff to review:"
            echo '```diff'
            cat "$DIFF_FILE"
            echo '```'
        } | env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_BASE_URL \
                claude -p --add-dir "$REPO_ROOT" 2>&1
    } > "$OUT_CLAUDE"
) &
PID_CLAUDE=$!

# Wait for all three; record exit codes. Round-3 review (Codex) caught
# that with `set -e` active, a non-zero `wait` immediately terminates
# the script — so the prior aggregation logic was DEAD CODE that never
# ran when any reviewer failed. Now we explicitly disable errexit
# around the waits (the `|| true` form is too easy to mis-edit), then
# re-enable below for the rest of the script.
set +e
wait $PID_CODEX; CODEX_RC=$?
wait $PID_GEMINI; GEMINI_RC=$?
wait $PID_CLAUDE; CLAUDE_RC=$?
set -e

# Detect content-level failures too: a review file containing only the
# script-side header (no actual review body) is a failure even if the
# CLI returned 0. We use a simple heuristic — body must be > N lines.
HEADER_LINES=4   # script writes 4 header lines per file
declare -A REVIEW_BODY
for f in "$OUT_CODEX" "$OUT_GEMINI" "$OUT_CLAUDE"; do
    if [ -s "$f" ]; then
        body=$(($(wc -l < "$f") - HEADER_LINES))
        REVIEW_BODY[$f]=$body
    else
        REVIEW_BODY[$f]=0
    fi
done

# Aggregate failures across exit-code AND body-length checks. Use
# `(... || true)` on each test so `set -u` doesn't trip when an entry
# is absent; ${FAILURES[@]:-} below provides the same safety on read.
EXIT_CODE=0
FAILURES=()
[ "$CODEX_RC" -ne 0 ] && FAILURES+=("codex(rc=$CODEX_RC)") || true
[ "$GEMINI_RC" -ne 0 ] && FAILURES+=("gemini(rc=$GEMINI_RC)") || true
[ "$CLAUDE_RC" -ne 0 ] && FAILURES+=("claude(rc=$CLAUDE_RC)") || true
for f in "$OUT_CODEX" "$OUT_GEMINI" "$OUT_CLAUDE"; do
    if [ "${REVIEW_BODY[$f]}" -le 1 ]; then
        FAILURES+=("$(basename "$f")(empty-body)")
    fi
done

echo "==> Reviews written:"
for f in "$OUT_CODEX" "$OUT_GEMINI" "$OUT_CLAUDE"; do
    if [ -s "$f" ] && [ "${REVIEW_BODY[$f]}" -gt 1 ]; then
        echo "    ✓ $f ($(wc -l < "$f") lines)"
    elif [ -s "$f" ]; then
        echo "    ⚠ $f (header-only — reviewer didn't produce content)"
    else
        echo "    ✗ $f (empty)"
    fi
done

if [ "${#FAILURES[@]}" -gt 0 ]; then
    # `${FAILURES[*]}` is safe here — we already gated on length.
    echo "==> FAILED reviewers: ${FAILURES[*]}"
    EXIT_CODE=1
fi

# --- Summary ------------------------------------------------------------
{
    echo "# Triple-AI review — $BASE..$HEAD_SHA"
    echo
    echo "_$(git log -1 --pretty=format:'%s' HEAD)_"
    echo
    echo "_Generated $(date -Iseconds) on \`$(uname -s) $(uname -r)\`_"
    echo
    echo "## Reviewers"
    echo
    echo "| AI | output | lines |"
    echo "| --- | --- | --- |"
    for f in "$OUT_CODEX" "$OUT_GEMINI" "$OUT_CLAUDE"; do
        ai=$(echo "$f" | sed 's|.*\.\([^.]*\)\.md|\1|')
        if [ -s "$f" ]; then
            echo "| ${ai^} | [\`${f#reviews/}\`](${f#reviews/}) | $(wc -l < "$f") |"
        else

exec
/bin/bash -lc "sed -n '280,560p' scripts/triple_review.sh" in /mnt/data/openlimno
 succeeded in 0ms:
        else
            echo "| ${ai^} | _(failed)_ | 0 |"
        fi
    done
    echo
    echo "## Diff scope"
    echo
    echo "- Range: \`$BASE..$HEAD_SHA\`"
    echo "- Lines: $DIFF_LINES"
    echo "- Files touched:"
    # Round-4 review (Codex) caught the summary previously used a
    # path-filtered two-dot diff, while reviewers see triple-dot
    # unfiltered. Use the same revspec/scope so the artifact lists
    # exactly what was reviewed.
    git diff --name-only "$BASE_SHA...HEAD" \
        | sed 's/^/  - /'
    echo
    echo "## Reading order"
    echo
    echo "1. Each reviewer above sees the same diff with the same prompt."
    echo "2. Read all three before triaging — they have different blind spots."
    echo "3. Lessons from past rounds: Claude tested only dev-venv → Codex caught"
    echo "   bundle regressions. Codex assumed live network → Gemini caught it."
    echo "   Three reviewers ≠ overkill, it's structurally necessary."
} > "$OUT_SUMMARY"

echo "==> Summary: $OUT_SUMMARY"
echo
echo "Done. Skim: cat $OUT_SUMMARY"

# Exit non-zero if any reviewer failed (so CI fails the workflow rather
# than uploading auth-error output as a "successful" review). Round-3
# review caught that swallowing failures here masked exactly the
# alpha.5 case where all three reviewers got auth errors but the
# release showed "review attached".
exit "$EXIT_CODE"

exec
/bin/bash -lc "grep -R \"_read_wua_parquet\\|_read_xs_rows_cached\\|open_wua_q\\|triple_review\" -n tests src scripts | head -200" in /mnt/data/openlimno
 succeeded in 1260ms:
grep: tests/unit/__pycache__/test_qgis_plugin_smoke.cpython-312-pytest-9.0.2.pyc: binary file matches
grep: tests/unit/__pycache__/test_qgis_plugin_smoke.cpython-312-pytest-9.0.3.pyc: binary file matches
grep: tests/unit/__pycache__/test_xs_rows_cache.cpython-312-pytest-9.0.2.pyc: binary file matches
grep: tests/integration/__pycache__/test_qgis_plugin_shim.cpython-312-pytest-9.0.2.pyc: binary file matches
tests/unit/test_xs_rows_cache.py:1:"""Unit tests for ``Controller._read_xs_rows_cached``.
tests/unit/test_xs_rows_cache.py:51:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:53:        rows = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:65:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:67:        subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:68:        subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:79:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:81:        rows1 = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:84:        rows2 = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:99:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:101:        subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:105:        subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:127:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:129:        rows = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:141:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:143:        subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:145:    with patch("openlimno.gui_core.controller._read_wua_parquet") as mock:
tests/unit/test_xs_rows_cache.py:146:        rows = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:160:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:167:            subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
tests/unit/test_xs_rows_cache.py:175:    """Round-6 fix (Gemini P0): if `_read_wua_parquet` raises (e.g.,
tests/unit/test_xs_rows_cache.py:187:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:190:        rows = subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
tests/unit/test_xs_rows_cache.py:206:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:208:        rows1 = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:209:        rows2 = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:228:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:231:        rows1 = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:239:        rows2 = subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:259:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:263:            subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
tests/unit/test_xs_rows_cache.py:289:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:292:            subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
tests/unit/test_xs_rows_cache.py:307:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:311:            subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
tests/unit/test_xs_rows_cache.py:328:    with patch("openlimno.gui_core.controller._read_wua_parquet",
tests/unit/test_xs_rows_cache.py:330:        subject._read_xs_rows_cached(str(parquet_file))
tests/unit/test_xs_rows_cache.py:334:            subject._read_xs_rows_cached(parquet_file.name)
tests/unit/test_qgis_plugin_smoke.py:73:    rows = OpenLimnoPlugin._read_wua_parquet(str(tmp_path / "wua.parquet"))
tests/integration/studio_workflows_smoke.py:401:    from openlimno.gui_core.controller import _read_wua_parquet
tesgrep: src/openlimno/qgis/openlimno_qgis_plugin/__pycache__/plugin.cpython-312.pyc: binary file matches
grep: src/openlimno/gui_core/__pycache__/controller.cpython-312.pyc: binary file matches
grep: src/openlimno/studio/__pycache__/main_window.cpython-312.pyc: binary file matches
ts/integration/studio_workflows_smoke.py:402:    rows = _read_wua_parquet(str(xs))
tests/integration/test_qgis_plugin_shim.py:120:    for method in ("open_hydraulic_nc", "open_wua_q", "plot_cross_section",
src/openlimno/qgis/openlimno_qgis_plugin/plugin.py:95:        _menu_only("Open WUA-Q curve…", self.ctl.open_wua_q)
src/openlimno/gui_core/controller.py:47:    ``Controller._read_xs_rows_cached`` checks for this subtype
src/openlimno/gui_core/controller.py:54:def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
src/openlimno/gui_core/controller.py:59:    That made ``Controller._read_xs_rows_cached``'s retry-on-exception
src/openlimno/gui_core/controller.py:167:    def open_wua_q(self) -> None:
src/openlimno/gui_core/controller.py:181:        # Round-1 review (Codex P2 + Claude): _read_wua_parquet now raises
src/openlimno/gui_core/controller.py:186:            rows = _read_wua_csv(path) if path.endswith(".csv") else _read_wua_parquet(path)
src/openlimno/gui_core/controller.py:220:        # Round-1 review: _read_wua_parquet raises on torn parquet
src/openlimno/gui_core/controller.py:225:            rows = _read_wua_parquet(xs_path)
src/openlimno/gui_core/controller.py:874:    def _read_xs_rows_cached(self, path: str, max_retries: int = 3) -> list:
src/openlimno/gui_core/controller.py:935:                rows = _read_wua_parquet(path)
src/openlimno/gui_core/controller.py:983:        rows = self._read_xs_rows_cached(cache_key)
src/openlimno/studio/main_window.py:251:        _ol_action("Open WUA-Q curve…", self.ctl.open_wua_q, on_toolbar=False)
scripts/triple_review.sh:12:#     scripts/triple_review.sh                # diff against origin/main
scripts/triple_review.sh:13:#     scripts/triple_review.sh v0.1.0-alpha.3 # diff against given ref
scripts/triple_review.sh:14:#     BASE=v0.1.0-alpha.3 scripts/triple_review.sh
scripts/triple_review.sh:24:# auth is intentionally not used — see docs/triple_review.md for why.
scripts/triple_review.sh:181:        # docs/triple_review.md "Why CLI subscription, not API". Feed

exec
/bin/bash -lc "git show 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40:src/openlimno/gui_core/controller.py | sed -n '1,120p;120,240p;850,950p'" in /mnt/data/openlimno
 succeeded in 0ms:
"""Controller — the single source of truth for plugin + Studio actions.

Both the QGIS plugin and OpenLimno Studio instantiate one ``Controller``
per session, passing a ``Host`` adapter that exposes the QGIS-iface-shaped
methods the handlers need. Handlers don't import ``iface`` directly.
"""
from __future__ import annotations

import csv
import math
import os
import time
from pathlib import Path
from typing import Any, Protocol


class Host(Protocol):
    """Adapter interface implemented by both PluginHost and Studio MainWindow.

    The four methods mirror QGIS iface, so the handler bodies (which were
    originally written against ``self.iface``) port over almost verbatim.
    """

    def main_window(self) -> Any: ...
    def map_canvas(self) -> Any: ...
    def message_bar(self) -> Any: ...
    def status_bar(self) -> Any: ...


# ---------------------------------------------------------------------------
# Helpers (no Qt / iface dependency)
# ---------------------------------------------------------------------------
def _read_wua_csv(path: str) -> list[dict[str, str]]:
    with open(path, encoding="utf-8") as f:
        data_lines = [line for line in f if not line.lstrip().startswith("#")]
    if not data_lines:
        return []
    return list(csv.DictReader(data_lines))


def _read_wua_parquet(path: str) -> list[dict[str, Any]]:
    """Read parquet via pyarrow (preferred) with GDAL/OGR fallback.

    Round-7 review (Codex P1): the previous implementation caught
    ``Exception`` at both backends and returned ``[]`` on failure.
    That made ``Controller._read_xs_rows_cached``'s retry-on-exception
    code unreachable in production — torn reads silently produced
    empty rows that got cached. Now only ``ImportError`` falls through
    between backends; read failures propagate so the cache layer can
    act on them.
    """
    try:
        import pyarrow.parquet as pq
    except ImportError:
        pq = None
    if pq is not None:
        t = pq.read_table(path)  # raises ArrowInvalid on torn footer, etc.
        return [
            dict(zip(t.column_names, row, strict=False))
            for row in zip(*[c.to_pylist() for c in t.columns], strict=False)
        ]
    # No pyarrow: try GDAL OGR.
    try:
        from osgeo import ogr
    except ImportError as e:
        raise RuntimeError(
            "neither pyarrow nor GDAL available to read parquet"
        ) from e
    ds = ogr.Open(path)
    if ds is None:
        raise OSError(f"GDAL/OGR failed to open {path}")
    layer = ds.GetLayer(0)
    defn = layer.GetLayerDefn()
    field_names = [defn.GetFieldDefn(i).GetName() for i in range(defn.GetFieldCount())]
    return [{fn: feat.GetField(fn) for fn in field_names} for feat in layer]




# ---------------------------------------------------------------------------
# Controller
# ---------------------------------------------------------------------------
class Controller:
    """Holds session state and exposes handler methods.

    State lifecycle:
    - ``_xs_parquet`` / ``_hyd_nc``: auto-discovery cache, reset by
      build_case_from_osm and refreshed by activate_pick_tool
    - ``_pick_tool``: active QgsMapTool (None when click-tool is off)
    - ``_run_case_proc``: keeps QProcess alive while CLI runs
    """

    def __init__(self, host: Host) -> None:
        self.host = host
        self._xs_parquet: str | None = None
        self._hyd_nc: str | None = None
        self._pick_tool: Any = None
        self._pick_action: Any = None  # set by UI when an action is checkable
        self._run_case_proc: Any = None
        self._run_case_log: bytearray = bytearray()

    # ------------------------------------------------------------------
    # Open existing results
    # ------------------------------------------------------------------
    def open_hydraulic_nc(self) -> None:
        from qgis.core import QgsMeshLayer, QgsProject, QgsRasterLayer
        from qgis.PyQt.QtWidgets import QFileDialog, QMessageBox

        path, _ = QFileDialog.getOpenFileName(
            self.host.main_window(),
            "Open OpenLimno hydraulics NetCDF",
            "",
            "OpenLimno NetCDF (*.nc);;All files (*)",
        )
        if not path:
            return
        layer_name = os.path.basename(path)
        mesh = QgsMeshLayer(path, layer_name, "mdal")
        if mesh.isValid():
            QgsProject.instance().addMapLayer(mesh)
            QgsProject.instance().addMapLayer(mesh)
            return
        rast = QgsRasterLayer(f'NETCDF:"{path}":water_depth', f"{layer_name}:water_depth")
        if rast.isValid():
            QgsProject.instance().addMapLayer(rast)
        else:
            QMessageBox.warning(
                self.host.main_window(), "OpenLimno",
                f"Could not load {path} as mesh or raster.",
            )

    def open_wua_q(self) -> None:
        from qgis.PyQt.QtWidgets import (
            QDialog, QFileDialog, QTableWidget, QTableWidgetItem, QVBoxLayout,
        )

        path, _ = QFileDialog.getOpenFileName(
            self.host.main_window(),
            "Open WUA-Q file",
            "",
            "WUA-Q (*.csv *.parquet);;All files (*)",
        )
        if not path:
            return
        rows = _read_wua_csv(path) if path.endswith(".csv") else _read_wua_parquet(path)
        if not rows:
            return
        dlg = QDialog(self.host.main_window())
        dlg.setWindowTitle(f"WUA-Q: {os.path.basename(path)}")
        layout = QVBoxLayout(dlg)
        headers = list(rows[0].keys())
        table = QTableWidget(len(rows), len(headers), dlg)
        table.setHorizontalHeaderLabels(headers)
        for i, row in enumerate(rows):
            for j, h in enumerate(headers):
                table.setItem(i, j, QTableWidgetItem(str(row.get(h, ""))))
        layout.addWidget(table)
        dlg.resize(640, 480)
        dlg.exec()

    def plot_cross_section(self) -> None:
        from qgis.PyQt.QtWidgets import (
            QComboBox, QDialog, QDialogButtonBox, QFileDialog,
            QFormLayout, QMessageBox, QVBoxLayout,
        )

        xs_path, _ = QFileDialog.getOpenFileName(
            self.host.main_window(),
            "Open cross_section.parquet",
            "",
            "OpenLimno cross-sections (*.parquet);;All files (*)",
        )
        if not xs_path:
            return
        rows = _read_wua_parquet(xs_path)
        if not rows:
            QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                  f"Could not read {xs_path}")
            return
        stations = sorted({float(r["station_m"]) for r in rows})

        hyd_path, _ = QFileDialog.getOpenFileName(
            self.host.main_window(),
            "Open hydraulics.nc (cancel for bed-only plot)",
            "",
            "OpenLimno hydraulics (*.nc);;All files (*)",
        )

        dlg = QDialog(self.host.main_window())
        dlg.setWindowTitle("Plot cross-section")
        form = QFormLayout(dlg)
        cb_station = QComboBox(); cb_station.addItems([f"{s:g}" for s in stations])
        form.addRow("station_m:", cb_station)
        cb_q = QComboBox()
        discharges: list[float] = []
        if hyd_path:
            try:
                from netCDF4 import Dataset
                ds = Dataset(hyd_path)
                if "discharge" in ds.variables:
                    discharges = [float(v) for v in ds.variables["discharge"][:]]
                ds.close()
                cb_q.addItems([f"{q:g}" for q in discharges])
                form.addRow("Q (m³/s):", cb_q)
            except Exception as e:
                QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                      f"Failed to read discharges: {e}")
        bb = QDialogButtonBox(QDialogButtonBox.StandardButton.Ok |
                                QDialogButtonBox.StandardButton.Cancel)
        bb.accepted.connect(dlg.accept); bb.rejected.connect(dlg.reject)
        form.addRow(bb)
        if dlg.exec() != QDialog.DialogCode.Accepted:
            return

        target_station = float(cb_station.currentText())
        target_Q = float(cb_q.currentText()) if discharges else None
        # Stash the hydraulics path so the renderer can reach it
        self._hyd_nc = hyd_path or None
        self._render_profile_dialog(rows, stations, target_station, target_Q)

    # ------------------------------------------------------------------
    # Build a brand-new case from OSM
    # ------------------------------------------------------------------
    def build_case_from_osm(self) -> None:
        from qgis.PyQt.QtWidgets import (
            QButtonGroup, QDialog, QDialogButtonBox, QDoubleSpinBox,
            QFileDialog, QFormLayout, QHBoxLayout, QLabel, QLineEdit,
            QMessageBox, QPushButton, QRadioButton, QSpinBox, QWidget,
        )

        dlg = QDialog(self.host.main_window())
        dlg.setWindowTitle("Build OpenLimno case from OSM")
        form = QFormLayout(dlg)

        canvas_extent = self.host.map_canvas().extent()
        bbox_str = (
            f"{canvas_extent.xMinimum():.6f},{canvas_extent.yMinimum():.6f},"
            f"{canvas_extent.xMaximum():.6f},{canvas_extent.yMaximum():.6f}"
        )

        rb_bbox = QRadioButton("Use current map view (bbox) — recommended")
        if (cur is not None
                and cache.get("path") == path
                and cache.get("stat") == cur):
            return cache["rows"]
        # Stat failed — keep stale cache rather than crash
        if cur is None and cache.get("path") == path and cache.get("rows"):
            return cache["rows"]
        # Cache miss: read with TOCTOU guard. Each transient failure
        # mode (stat-fails, parquet-read-raises, post-stat-None, pre/post
        # disagree) records last_error and continues into the next
        # attempt — only when retries are exhausted do we fall back to
        # the prior cache or raise. Round-6 fixes:
        #   - Codex P2 / Claude P1: ``post is None`` was raising on
        #     attempt 0, defeating the retry budget for `mv -f`-style
        #     transient unlinks. Now it `continue`s.
        #   - Gemini P0: parquet readers raise on torn input
        #     (ArrowInvalid, EOFError, OSError). The previous code
        #     let those propagate out, bypassing retry. Wrap.
        #   - Claude P1: ``cache.get("rows")`` was falsy on empty
        #     rows; an empty parquet would be treated as no-cache.
        #     Use ``"rows" in cache`` instead.
        last_error = None
        for attempt in range(max_retries):
            if attempt > 0:
                time.sleep(0.05 * attempt)
            pre = _stat(path)
            if pre is None:
                last_error = OSError(f"cannot stat {path}")
                continue
            try:
                rows = _read_wua_parquet(path)
            except (OSError, EOFError, RuntimeError) as e:
                # Round-7 review (all 3): narrow from `except Exception`
                # to the transient I/O / read-failure family. Don't
                # swallow programmer bugs (TypeError, AttributeError,
                # KeyError) under the retry banner. ArrowInvalid is a
                # subclass of OSError in newer pyarrow, ValueError in
                # older — we add ValueError as a safety net below.
                last_error = e
                continue
            except ValueError as e:
                # pyarrow.lib.ArrowInvalid on torn footers used to be
                # ValueError. Treat as transient.
                last_error = e
                continue
            post = _stat(path)
            if post is None:
                last_error = FileNotFoundError(
                    f"{path} vanished during read")
                continue
            if pre == post:
                self._xs_rows_cache = {"path": path, "stat": pre, "rows": rows}
                return rows
            last_error = RuntimeError(f"{path} changed during read")
        # Retries exhausted. Prefer prior cache (from a clean read)
        # over raising — caller's downstream logic handles "rows looks
        # familiar" better than "file disappeared". Use ``"rows" in
        # cache`` so an empty list (legitimate) still hits.
        if cache.get("path") == path and "rows" in cache:
            return cache["rows"]
        raise last_error or RuntimeError(f"unable to read {path} stably")

    def _plot_at_station(self, station: float) -> None:
        from qgis.PyQt.QtWidgets import (
            QComboBox, QDialog, QDialogButtonBox, QFormLayout, QMessageBox,
        )

        if not self._xs_parquet:
            QMessageBox.warning(self.host.main_window(), "OpenLimno",
                                  "No cross_section.parquet selected.")
            return
        cache_key = self._xs_parquet
        rows = self._read_xs_rows_cached(cache_key)
        stations = sorted({float(r["station_m"]) for r in rows})
        station = min(stations, key=lambda s: abs(s - station))

        target_Q = None
        if self._hyd_nc:
            try:
                from netCDF4 import Dataset
                ds = Dataset(self._hyd_nc)
                discharges = [float(v) for v in ds.variables["discharge"][:]]
                ds.close()
            except Exception:
                discharges = []
            if discharges:
                dlg = QDialog(self.host.main_window())
                dlg.setWindowTitle(f"Q for station {station:g} m")
                form = QFormLayout(dlg)
                cb = QComboBox(); cb.addItems([f"{q:g}" for q in discharges])
                idx = min(range(len(discharges)),
                            key=lambda i: abs(discharges[i] - 7.0))
                cb.setCurrentIndex(idx)
                form.addRow("Q (m³/s):", cb)
                bb = QDialogButtonBox(QDialogButtonBox.StandardButton.Ok |
                                        QDialogButtonBox.StandardButton.Cancel)
                bb.accepted.connect(dlg.accept); bb.rejected.connect(dlg.reject)
                form.addRow(bb)
                if dlg.exec() == QDialog.DialogCode.Accepted:
                    target_Q = float(cb.currentText())


exec
/bin/bash -lc "python - <<'PY'
try:
 import pyarrow.lib as lib
 print('pyarrow', lib.ArrowException, hasattr(lib,'ArrowException'))
 print(lib.ArrowInvalid.__mro__)
except Exception as e:
 print(type(e).__name__, e)
PY" in /mnt/data/openlimno
 succeeded in 278ms:
pyarrow <class 'pyarrow.lib.ArrowException'> True
(<class 'pyarrow.lib.ArrowInvalid'>, <class 'ValueError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)

exec
/bin/bash -lc "sed -n '1,220p' pyproject.toml" in /mnt/data/openlimno
 succeeded in 0ms:
[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "openlimno"
version = "0.1.0a13"
description = "Open-source water ecology modeling platform — PHABSIM/IFIM modernization with SCHISM 2D wrapping"
readme = "README.md"
requires-python = ">=3.11,<3.13"
license = { file = "LICENSE" }
authors = [
    { name = "OpenLimno Contributors", email = "hello@openlimno.org" },
]
keywords = [
    "ecology", "hydrology", "fish-habitat", "PHABSIM", "IFIM",
    "ecological-flow", "WUA", "HSI", "river2d", "SCHISM",
]
classifiers = [
    "Development Status :: 1 - Planning",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Hydrology",
    "Topic :: Scientific/Engineering :: GIS",
]
dependencies = [
    "numpy>=1.26",
    "pandas>=2.2",
    "xarray>=2024.1",
    "netCDF4>=1.6",
    "pyarrow>=15",
    "pyproj>=3.6",
    "shapely>=2.0",
    "geopandas>=0.14",
    "rasterio>=1.3",
    "matplotlib>=3.8",
    "pyyaml>=6",
    "jsonschema>=4.21",
    "click>=8.1",
    "rich>=13",
    "scipy>=1.12",
    "requests>=2.31",  # openlimno.preprocess.osm_builder Overpass calls
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-cov>=5",
    "pytest-regressions>=2",
    "hypothesis>=6",
    "ruff>=0.4",
    "mypy>=1.10",
]
docs = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.25",
]
schism = [
    "pyschism",
]

[project.urls]
Homepage = "https://openlimno.org"
Documentation = "https://docs.openlimno.org"
Repository = "https://github.com/openlimno/openlimno"
Issues = "https://github.com/openlimno/openlimno/issues"
Changelog = "https://github.com/openlimno/openlimno/blob/main/CHANGELOG.md"

[project.scripts]
openlimno = "openlimno.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/openlimno"]

[tool.hatch.build.targets.wheel.shared-data]
"src/openlimno/wedm/schemas" = "openlimno/wedm/schemas"

# ---------------------------------------------------------------
# ruff
# ---------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "F", "W",          # pycodestyle / pyflakes
    "I",                      # isort
    "N",                      # naming
    "UP",                     # pyupgrade
    "B",                      # bugbear
    "SIM",                    # simplify
    "RUF",                    # ruff-specific
    "PT",                     # pytest style
]
ignore = [
    "E501",   # line too long (formatter handles)
    "N802",   # function name lowercase (math operators: Q_at_h, T_of_x, ...)
    "N803",   # argument lowercase (Q, A, U_burst — physics symbols)
    "N806",   # var name in func (same)
    "N815",   # mixed-case class attribute (hatch_temp_C_mean: °C convention)
    "N816",   # mixed-case module-level (LTS_VERSION ok)
    "RUF001", # ambiguous unicode (× / – are unambiguous in scientific text)
    "RUF002", # same in docstrings
    "RUF003", # same in comments
    "RUF043", # match= regex without raw-string (test plumbing, not security)
    "RUF046", # cast(int, x) where x is already int (clarity > redundancy)
    "RUF100", # unused noqa (downstream ruff version drift)
    "SIM105", # contextlib.suppress vs try/except pass (taste)
    "SIM108", # ternary preference (taste)
    "SIM117", # combined with-statements (taste)
    "PT018",  # multi-part assert (clarity > brevity in tests)
]

[tool.ruff.lint.per-file-ignores]
# B023: closure-over-loop-var inside numerical iteration is intentional
# (the closure is consumed within the same iteration via brentq/minimize)
"src/openlimno/hydro/builtin_1d.py" = ["B023"]
"src/openlimno/preprocess/legacy.py" = ["F841"]  # cur_npts kept for parser symmetry
"tests/**" = ["N806", "B011", "B023", "F841", "RUF059"]
"benchmarks/**" = ["N806", "B011", "F841", "RUF059"]
"examples/**" = ["N806"]

[tool.ruff.format]
quote-style = "double"

# ---------------------------------------------------------------
# mypy
# ---------------------------------------------------------------
[tool.mypy]
python_version = "3.11"
# Pragmatic stance for a 1.0-rc science codebase: catch real type bugs without
# fighting numpy/pandas dynamic typing. Strict mode is the goal post-1.0 once
# upstream stubs (yaml, geopandas, rasterio) stabilise.
strict = false
ignore_missing_imports = true
warn_return_any = false
warn_unused_configs = true
warn_unused_ignores = false
disable_error_code = [
    "import-untyped",  # yaml/geopandas/rasterio still ship without stubs
    "no-any-return",   # numpy/pandas return Any pervasively
    "type-arg",        # `dict` without parameters is fine for this code
    "assignment",      # union assignments in regulatory_export switch
    "misc",            # numpy callable? edge cases
    "unused-ignore",   # tolerated drift across mypy versions
    "attr-defined",    # studyplan dynamic dict-as-object access (works at runtime)
    "arg-type",        # numpy/pandas list[str] vs list[Literal] friction
    "call-arg",        # jsonschema-referencing API surface drift
]
plugins = []

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true

# ---------------------------------------------------------------
# pytest
# ---------------------------------------------------------------
[tool.pytest.ini_options]
testpaths = ["tests", "benchmarks"]
addopts = "-ra --strict-markers"
markers = [
    "slow: tests that take >5s",
    "schism: requires external SCHISM binary",
    "qgis: requires QGIS environment",
    "benchmark: validation benchmark, not unit test",
]

[tool.coverage.run]
source = ["src/openlimno"]
omit = ["src/openlimno/qgis/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]

exec
/bin/bash -lc "sed -n '1,380p' tests/unit/test_xs_rows_cache.py" in /mnt/data/openlimno
 succeeded in 0ms:
"""Unit tests for ``Controller._read_xs_rows_cached``.

The third review round (codex + gemini + claude) found that the previous
cache implementation cemented torn rows when the parquet was rewritten
during the read. Claude's verdict was unambiguous: 'the only behaviorally
new code path is untested'. This file fixes that.

These tests exercise the caching logic in pure Python without instantiating
a Host. ``Controller.__init__`` only needs an attribute access (host),
which we satisfy with a sentinel.
"""
from __future__ import annotations

import os
import time
from pathlib import Path
from unittest.mock import patch

import pytest

from openlimno.gui_core.controller import Controller


@pytest.fixture
def subject():
    """A Controller instance whose ``_xs_parquet`` we set per-test.
    We pass None for the host since these tests don't touch any host
    methods.
    """
    ctl = Controller(host=None)  # type: ignore[arg-type]
    return ctl


@pytest.fixture
def parquet_file(tmp_path):
    p = tmp_path / "xs.parquet"
    p.write_text("v1")
    return p


# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------
def test_first_read_caches_rows(subject, parquet_file):
    calls = []

    def fake_read(p):
        calls.append(p)
        return [{"row": 1}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read):
        rows = subject._read_xs_rows_cached(str(parquet_file))
    assert rows == [{"row": 1}]
    assert len(calls) == 1


def test_cache_hit_skips_re_read(subject, parquet_file):
    calls = []

    def fake_read(p):
        calls.append(p)
        return [{"row": 1}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read):
        subject._read_xs_rows_cached(str(parquet_file))
        subject._read_xs_rows_cached(str(parquet_file))
    assert len(calls) == 1, "cache should have served the second call"


def test_mtime_change_invalidates(subject, parquet_file):
    calls = []

    def fake_read(p):
        calls.append(p)
        return [{"v": len(calls)}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read):
        rows1 = subject._read_xs_rows_cached(str(parquet_file))
        future = time.time() + 100
        os.utime(parquet_file, (future, future))
        rows2 = subject._read_xs_rows_cached(str(parquet_file))
    assert rows1 != rows2, "mtime change should invalidate cache"
    assert len(calls) == 2


def test_size_change_invalidates_at_same_mtime(subject, parquet_file):
    """The FAT/SMB scenario the cache claims to protect against: mtime
    granularity is 1-2 s, so a same-second rewrite ties on mtime. Size
    differing must still trigger an invalidation."""
    calls = []

    def fake_read(p):
        calls.append(p)
        return [{"v": Path(p).read_text()}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read):
        subject._read_xs_rows_cached(str(parquet_file))
        original_mtime = os.path.getmtime(parquet_file)
        parquet_file.write_text("v2_extended_payload")
        os.utime(parquet_file, (original_mtime, original_mtime))
        subject._read_xs_rows_cached(str(parquet_file))
    assert len(calls) == 2, "size change must invalidate even at same mtime"


def test_mid_read_rewrite_does_not_cache_torn_rows(subject, parquet_file):
    """The bug all three reviewers caught. Simulate a rewrite DURING the
    read: pre-stat (T1, S1), read produces torn rows, post-stat (T2, S2).
    The cache MUST NOT store (T2, S2, torn_rows) — that would cement
    corrupt data forever."""
    state = {"call_count": 0}

    def fake_read_with_concurrent_rewrite(p):
        state["call_count"] += 1
        if state["call_count"] == 1:
            # First call: simulate writer touching file mid-read
            future = time.time() + 50
            Path(p).write_text("post-read-content-different-size-XX")
            os.utime(p, (future, future))
            return [{"row": "TORN"}]
        # Subsequent retry: file quiescent
        return [{"row": "CLEAN"}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read_with_concurrent_rewrite):
        rows = subject._read_xs_rows_cached(str(parquet_file))
    assert rows == [{"row": "CLEAN"}], (
        "After mid-read rewrite, the retry must serve clean rows, "
        "not the original torn read"
    )
    assert state["call_count"] >= 2, "expected at least one retry"


def test_stat_failure_serves_stale_cache(subject, parquet_file):
    """If the file is moved/unmounted between clicks, the user shouldn't
    see a crash — they should get the previously-cached rows. The next
    legitimate read can surface the real error."""
    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 return_value=[{"row": "ok"}]):
        subject._read_xs_rows_cached(str(parquet_file))
    parquet_file.unlink()
    with patch("openlimno.gui_core.controller._read_wua_parquet") as mock:
        rows = subject._read_xs_rows_cached(str(parquet_file))
    assert rows == [{"row": "ok"}]
    mock.assert_not_called()


def test_vanished_during_read_retries_then_raises(subject, parquet_file):
    """Round-6 fix (Codex P0): a vanished file on attempt 0 should
    re-enter the retry loop (a transient ``mv -f`` may complete
    before retries exhaust). Only when ALL retries fail do we raise.
    Previously we raised immediately, defeating the backoff design."""
    def fake_read_then_delete(p):
        Path(p).unlink()
        return [{"row": "TORN"}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read_then_delete), \
         patch("openlimno.gui_core.controller.time.sleep") as mock_sleep:
        # Either FileNotFoundError (post-stat None) or its parent OSError
        # (subsequent pre-stat None) is acceptable — both signal the same
        # condition.
        with pytest.raises(OSError):
            subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
    # Backoff must have run for retries 1 and 2.
    assert mock_sleep.call_count == 2, (
        f"vanished-during-read must retry, got {mock_sleep.call_count} sleeps"
    )


def test_parquet_read_exception_triggers_retry(subject, parquet_file):
    """Round-6 fix (Gemini P0): if `_read_wua_parquet` raises (e.g.,
    pyarrow's ArrowInvalid on a torn footer, or OSError on a locked
    file), the retry loop must catch and retry instead of propagating
    immediately."""
    state = {"calls": 0}

    def flaky_read(p):
        state["calls"] += 1
        if state["calls"] < 3:
            raise OSError("torn read - file locked by writer")
        return [{"row": "OK_AT_ATTEMPT_3"}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=flaky_read), \
         patch("openlimno.gui_core.controller.time.sleep"):
        rows = subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
    assert rows == [{"row": "OK_AT_ATTEMPT_3"}]
    assert state["calls"] == 3


def test_empty_rows_treated_as_valid_cache(subject, parquet_file):
    """Round-6 fix (Claude P1): an empty parquet (legitimate WUA
    with no observations yet) must produce a cacheable empty list.
    Previous ``cache.get("rows")`` truthy-check treated [] as
    no-cache, forcing re-read every click."""
    calls = []

    def fake_read(p):
        calls.append(p)
        return []

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read):
        rows1 = subject._read_xs_rows_cached(str(parquet_file))
        rows2 = subject._read_xs_rows_cached(str(parquet_file))
    assert rows1 == [] and rows2 == []
    assert len(calls) == 1, "empty rows must still be cached"


def test_vanished_during_read_serves_prior_cache(subject, parquet_file):
    """Round-5 fix (Gemini P0): if a prior good cache exists and the
    file vanishes mid-read on a subsequent call, serve the prior
    cached rows (which were read from a quiescent file) rather than
    the torn read."""
    state = {"reads": 0, "should_delete": False}

    def fake_read(p):
        state["reads"] += 1
        if state["should_delete"]:
            Path(p).unlink()
            return [{"row": "TORN"}]
        return [{"row": "GOOD"}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read):
        # First call: clean read, populates cache.
        rows1 = subject._read_xs_rows_cached(str(parquet_file))
        assert rows1 == [{"row": "GOOD"}]

        # Force cache miss by making the cache see a different stat.
        future = time.time() + 100
        os.utime(parquet_file, (future, future))
        # Second call: rewrite triggers cache miss; mid-read file vanishes.
        state["should_delete"] = True
        rows2 = subject._read_xs_rows_cached(str(parquet_file))
    # Must serve the prior good cache, NOT the torn read.
    assert rows2 == [{"row": "GOOD"}], (
        "must prefer prior cache over torn rows when file vanishes mid-read"
    )


def test_backoff_called_with_correct_durations(subject, parquet_file):
    """Round-5 fix (Claude P1): the `time.sleep` calls in the retry
    loop must use linear backoff. A regression that drops the sleep
    or uses a fixed duration must fail."""
    state = {"call_count": 0}

    def always_rewrite(p):
        state["call_count"] += 1
        future = time.time() + 100 + state["call_count"]
        Path(p).write_text("x" * (10 + state["call_count"]))
        os.utime(p, (future, future))
        return [{"row": f"torn_{state['call_count']}"}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=always_rewrite), \
         patch("openlimno.gui_core.controller.time.sleep") as mock_sleep:
        with pytest.raises((RuntimeError, OSError)):
            subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
    # First attempt: no sleep. Subsequent attempts: 0.05*1, 0.05*2.
    assert mock_sleep.call_count == 2, (
        f"expected 2 backoff sleeps for max_retries=3, got {mock_sleep.call_count}"
    )
    durations = [c.args[0] for c in mock_sleep.call_args_list]
    assert durations == [0.05, 0.10], (
        f"expected linear backoff [0.05, 0.10], got {durations}"
    )


def test_retry_exhaustion_raises(subject, parquet_file):
    """Round-4 fix: a writer rewriting on every read attempt must
    eventually trigger a clear error rather than silently returning
    the last torn read (Claude P1 + Gemini P1)."""
    state = {"call_count": 0}

    def always_rewrite(p):
        state["call_count"] += 1
        # Bump mtime forward by a different amount each time so pre/post
        # stats always disagree
        future = time.time() + 100 + state["call_count"]
        Path(p).write_text("x" * (10 + state["call_count"]))
        os.utime(p, (future, future))
        return [{"row": f"torn_{state['call_count']}"}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=always_rewrite):
        with pytest.raises((RuntimeError, OSError)):
            subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
    assert state["call_count"] >= 2, "should have retried at least once"


def test_typeerror_propagates_does_not_retry(subject, parquet_file):
    """Round-7 fix (all 3 reviewers): narrow ``except`` so programmer
    bugs (TypeError, AttributeError) propagate immediately rather than
    being misdiagnosed as transient I/O issues that burn the retry
    budget."""
    state = {"calls": 0}

    def fake_read_typeerror(p):
        state["calls"] += 1
        raise TypeError("simulated programmer bug in parquet reader")

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read_typeerror), \
         patch("openlimno.gui_core.controller.time.sleep") as mock_sleep:
        with pytest.raises(TypeError):
            subject._read_xs_rows_cached(str(parquet_file), max_retries=3)
    # Should NOT retry on TypeError.
    assert state["calls"] == 1, (
        f"TypeError should not trigger retry, but read was called {state['calls']}×"
    )
    assert mock_sleep.call_count == 0


def test_realpath_normalises_cache_key(subject, parquet_file, tmp_path):
    """Qt file dialogs sometimes change cwd; cache keys must not be
    sensitive to relative-vs-absolute spelling."""
    calls = []

    def fake_read(p):
        calls.append(p)
        return [{"row": "ok"}]

    with patch("openlimno.gui_core.controller._read_wua_parquet",
                 side_effect=fake_read):
        subject._read_xs_rows_cached(str(parquet_file))
        cwd = os.getcwd()
        os.chdir(tmp_path)
        try:
            subject._read_xs_rows_cached(parquet_file.name)
        finally:
            os.chdir(cwd)
    assert len(calls) == 1, (
        "realpath should make both spellings hit the same cache"
    )

exec
/bin/bash -lc "sed -n '380,760p' tests/unit/test_xs_rows_cache.py" in /mnt/data/openlimno
 succeeded in 0ms:
exec
/bin/bash -lc "sed -n '1,160p' docs/reviews/v0.1.0-alpha.10/README.md && sed -n '1,120p' docs/reviews/v0.1.0-alpha.10/claude.md && sed -n '1,120p' docs/reviews/v0.1.0-alpha.10/gemini.md" in /mnt/data/openlimno
 succeeded in 0ms:
# Triple-AI review of v0.1.0-alpha.10 — convergence round

_Diff range: [v0.1.0-alpha.9..v0.1.0-alpha.10](../../../compare/v0.1.0-alpha.9...v0.1.0-alpha.10) (4 files, +73/-25)_

This is the eighth and final review round of the v0.1.0-alpha series. It
documents **convergence by the explicit-tag criterion** and three
documented residuals shipped as known limitations rather than attempting
a ninth fix-round.

## Why this round matters

The pre-commitment was: "3 more rounds (alpha.7 → alpha.10) OR until a
round produces zero P0/P1, whichever comes first." That budget protects
against the failure mode of *iterating reviewer feedback indefinitely* —
where each round's fix introduces enough new surface for the next round
to find another concern, with no convergence proof.

Round 8 hit the budget *and* the criterion at the same time:

| reviewer  | P0 | P1 | P2 | total | verdict                                    |
|-----------|----|----|----|-------|--------------------------------------------|
| Codex     | 0  | 0  | 2  | 2     | "regressions but P2"                       |
| Gemini    | —  | —  | —  | 6     | untagged; 1 content-level P1 concern       |
| Claude    | —  | —  | —  | 7     | untagged; 1 content-level P1, 1 false-pos. |

Codex (the only reviewer that uses explicit P-tags) reported **zero P0
and zero P1**. By the documented convergence criterion, this is the
clean round.

## Three residuals shipped as known limitations

These are real concerns, not P0/P1-tagged by any reviewer, documented
here for v0.1.0 final:

1. **Missing-backend retry futility** (Gemini #2 + Claude #1, consensus)
   `_read_wua_parquet` raises `RuntimeError` when neither pyarrow nor
   GDAL is importable. The controller's retry loop catches `RuntimeError`
   as transient and burns 3 attempts × 50 ms backoff before giving up.
   Cosmetic latency on a guaranteed-failure path; no data corruption.
   **Filed as issue:** consider a `MissingBackendError` subclass that the
   controller short-circuits on.

2. **GUI direct-call paths bypass the cache wrapper** (Codex P2)
   `Controller.open_wua_q` and `plot_cross_section` call
   `_read_wua_parquet` directly rather than `_read_xs_rows_cached`.
   Round-7 changed the helper to propagate read failures, so a torn
   parquet now raises through to the GUI handler. Acceptable for an
   alpha — Qt's default exception handler shows a dialog rather than
   crashing — but the cache wrapper should be the single entry point.

3. **OGR fallback resource hygiene** (Claude #3)
   The GDAL/OGR branch never explicitly closes the `Dataset`, and
   `GetLayer(0)` returning `None` on malformed input crashes with
   `AttributeError` (not retried under round-7's narrowed exception
   list). Long-running GUI sessions could leak file handles. Mitigation:
   pyarrow is the bundled-default path; OGR is the fallback that few
   users will hit in practice.

## Round-8 false positive worth recording

Claude flagged as concern #2:

> `pyarrow.lib.ArrowInvalid` inherits from `Exception` in essentially
> every released pyarrow. Neither `OSError` nor `ValueError` catches it.

This is wrong. Codex's review session ran a runtime probe:

```
ArrowInvalid (<class 'pyarrow.lib.ArrowInvalid'>, <class 'ValueError'>,
              <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, ...)
```

`ArrowInvalid` *is* a subclass of `ValueError`, so the existing
`except ValueError` catches it. This is a textbook example of why
single-reviewer review is dangerous: in isolation Claude's claim looks
authoritative, and the prescribed fix (add `pyarrow.lib.ArrowException`
to the tuple) would have been merged. Codex's empirical probe broke
the false certainty.

## What the 8-round audit trail looks like

| round | tag        | reviewer who caught the round-defining bug | bug class                                                  |
|-------|------------|--------------------------------------------|------------------------------------------------------------|
| 1     | a.2        | (none — shipped broken)                    | scipy.optimize / WEDM schemas excluded from bundle         |
| 2     | a.3        | Codex                                      | non-functional AppImage diagnosed in one paragraph         |
| 3     | a.4        | Claude                                     | TOCTOU between stat and read (load-bearing find)           |
| 4     | a.5        | Codex                                      | `${#FAILURES[@]:-0}` invalid bash; `set -e` killed waits   |
| 5     | a.7        | Claude+Codex                               | torn rows cemented in cache; sibling-tag selection         |
| 6     | a.8        | Codex+Gemini                               | post-stat None bypass; 7-char SHA bypass                   |
| 7     | a.9        | Gemini                                     | `_read_wua_parquet` swallowed Exception → retry unreachable|
| 8     | a.10       | (consensus residuals; no P0/P1)            | convergence                                                |

Every round caught at least one issue invisible to the others. No two
adjacent rounds had the same load-bearing reviewer.

## Reading order for the round-8 reviews

1. [`codex.md`](codex.md) — runtime-probe verification (the
   `ArrowInvalid` MRO check), 2× P2 regression catches.
2. [`gemini.md`](gemini.md) — architectural framing of the
   `RuntimeError`-as-transient design smell.
3. [`claude.md`](claude.md) — priority-ordered concerns with one
   confident false positive that the other two reviewers' empirical
   evidence overruled.

## Lesson for future rounds

Convergence is *defined* by the explicit-tag criterion, not by
"all reviewers happy." Reviewers will always find more concerns —
that's not a failure mode, it's the methodology working. The maintainer's
job is to triage, not to chase clean reviews indefinitely.
# Claude review — `v0.1.0-alpha.9..86ff7a7`

_Generated by `claude -p` on 2026-05-09T13:28:40+08:00_

Concerns, in priority order:

**1. `RuntimeError` retried for permanent config failure (controller.py:881)**
The refactored `_read_wua_parquet` raises `RuntimeError("neither pyarrow nor GDAL available...")` when both backends are missing. The retry loop now catches `RuntimeError` and burns the full retry budget (with `time.sleep` backoff) on what is a permanent install problem. Should special-case missing-backend (or use a distinct exception type) and propagate immediately.

**2. The `ArrowInvalid` claim looks wrong (controller.py:880-887 comment)**
The comment asserts "ArrowInvalid is a subclass of OSError in newer pyarrow, ValueError in older." In reality `pyarrow.lib.ArrowInvalid` inherits from `pyarrow.lib.ArrowException` → `Exception` in essentially every released pyarrow. Neither `OSError` nor `ValueError` catches it. So the very corruption case the change exists to handle (torn parquet footer) likely escapes the new `except` list and surfaces as an uncaught crash. Verify against the bundled pyarrow version and add `pyarrow.lib.ArrowException` (or import-guarded equivalent) to the `except` tuple. Without this fix the cache-bypass-on-torn-read goal is not met.

**3. OGR fallback resource leaks and None-layer crash (controller.py:_read_wua_parquet GDAL branch)**
`ds = ogr.Open(path)` is never closed; in a long-running PyQt GUI session this leaks file handles. `ds.GetLayer(0)` may return `None` for malformed datasets — `defn = layer.GetLayerDefn()` then raises `AttributeError`, which the new exception-narrowing classifies as a "programmer bug" and refuses to retry, even though it is actually corrupt-input. Wrap in `try/finally` and explicit None-check that raises `OSError`.

**4. Test coverage is thin (tests/unit/test_xs_rows_cache.py)**
The added `test_typeerror_propagates_does_not_retry` only patches `_read_wua_parquet` to raise `TypeError`. The change comment names `TypeError, AttributeError, KeyError` — only one is exercised. Also missing:
- A test that a real torn parquet file (bad footer / truncated bytes) actually hits the retry path through the *unmocked* `_read_wua_parquet`. Mocking sidesteps issue #2 entirely; a regression where ArrowException leaks past the `except` would still pass these tests.
- A test that the missing-backend `RuntimeError` does NOT retry (it currently does — issue #1).
- A test that `ValueError` / `OSError` / `EOFError` each trigger a retry (positive case).
A regression in the new exception list would not be caught.

**5. Shell `echo` portability (scripts/triple_review.sh:46)**
`base_lower="$(echo "$BASE" | tr 'A-F' 'a-f')"`: if `BASE` happens to be `-n` or contains backslashes, `echo` behavior is shell-dependent (bash builtin vs `/bin/echo` vs dash). Use `printf '%s' "$BASE" | tr ...`. Minor on GitHub Actions today but not portable.

**6. Hex-named branches still bypass `check-ref-format`**
`^[0-9a-f]{8,40}$` accepts e.g. `deadbeef`, `feedface`, `cafebabe1` as "SHA" without ever reaching the ref-format fallback. If such branches exist locally, git resolution rules apply silently. Pre-existing; called out because the round-7 commit message implies the validation is now correct, and it isn't.

**7. Architecture nit — abstraction churn**
Five rounds of review-driven exception-list re-narrowing on the same 6-line `except` block is itself a smell. The right abstraction is probably "categorize once in `_read_wua_parquet` (transient vs fatal) and let the caller catch one base class," not iterating the tuple membership in the call site.

Not seen: SQL/HTTP injection, integer overflow, CRS handling, GUI-thread blocking changes (retry+sleep on controller is pre-existing).
# Gemini review — `v0.1.0-alpha.9..86ff7a7`

_Generated by `gemini -p` on 2026-05-09T13:28:40+08:00_

### 1. Architecture and Design
The narrowing of exception handling in `src/openlimno/gui_core/controller.py` aligns with robust systems programming by distinguishing between transient I/O failures and programmer errors. However, the implementation of the fallback logic in `_read_wua_parquet` (lines 58–61) introduces a minor architectural flaw: it raises a `RuntimeError` when dependencies are missing, which the caller (`Controller._read_xs_rows_cached`, line 881) then catches and retries. Retrying a missing library installation is futile and adds unnecessary latency before the eventual failure.

### 2. Bug Risk
*   **Ref Validation (`scripts/triple_review.sh:44`):** The logic explicitly rejects any 7-character hex string as a "collision-prone SHA." While this protects against Git's default short-SHA length, it creates a false positive for legitimate branch or tag names that happen to be 7-character hex strings (e.g., `beef123`, `cafebad`). If a user has a naming convention involving hex prefixes, this script will block their refs.
*   **Parquet Reader Fallback (`src/openlimno/gui_core/controller.py:53`):** The OGR backend check uses `if ds is None` but then proceeds to call `ds.GetLayer(0)`. While the code now raises `OSError` if `ds` is `None` (line 72), the error message "GDAL/OGR failed to open..." is generic. It could be a missing file, a permission issue, or an unsupported parquet flavor (e.g., encrypted), yet it is treated as a transient retryable error in the controller.
*   **Exception Narrowing (`src/openlimno/gui_core/controller.py:881`):** You catch `ValueError` to handle `ArrowInvalid` on older pyarrow versions. However, `ValueError` is a very broad built-in exception. If `_read_wua_parquet` or any logic inside the `try` block (like `zip(..., strict=False)` if it were to fail) raises a `ValueError` due to a logic bug, the controller will incorrectly treat it as a transient I/O issue and burn the retry budget.

### 3. Performance
*   **Row Conversion (`src/openlimno/gui_core/controller.py:46`):** The pyarrow path converts every column to a Python list (`c.to_pylist()`) and then zips them. For large hydraulic datasets, this is highly memory-inefficient as it duplicates the entire table in memory before creating the list of dicts. `table.to_pydict()` followed by a transformation or using `to_batches()` would be significantly more performant.
*   **GUI Blocking:** This code resides in `gui_core`. If `_read_xs_rows_cached` is called on the main thread, the combination of multiple I/O retries and `time.sleep` (implied by `mock_sleep` in tests) will hang the UI.

### 4. Test Adequacy
*   **Missing Dependency Path:** There is no test verifying the behavior when *neither* `pyarrow` nor `osgeo` is installed. As noted in section 1, this currently triggers an unnecessary retry loop.
*   **Torn Read Recovery:** The tests mock the exception but don't verify that the *second* attempt (after a simulated transient failure) actually succeeds and returns correct data.
*   **GDAL Path:** The new test only mocks the pyarrow entry point. There is no coverage for the OGR fallback path or its specific failure modes (e.g., `ds is None`).

### 5. Packaging and Portability
*   **Dependency Assumptions:** The `pyproject.toml` bump is standard, but the code assumes `osgeo` (GDAL) might be available as a fallback. GDAL is notoriously difficult to install via `pip` without external C libraries. If the AppImage/distribution strategy (implied by `packaging/` directory) relies on this fallback, it needs rigorous validation on the target runners.

### 6. Recommendations
1.  **Refine Retry Logic:** In `_read_wua_parquet`, raise a specific `DependencyError` (subclass of `Exception`, not `RuntimeError`) when libraries are missing, and ensure `Controller` does **not** catch/retry that specific type.
2.  **Optimize Parquet Read:** Use `t.to_batches()` to iterate rows if memory pressure is a concern.
3.  **Ref Validation:** Instead of a hard block on 7-char hex, consider checking if the ref exists via `git rev-parse --verify "$BASE^{commit}"` to disambiguate.

exec
/bin/bash -lc 'set +e
for x in $(git rev-parse --short=6 HEAD) $(git rev-parse --short=7 HEAD) cafe HEAD v0.1.0-alpha.10 origin/main; do echo X=$x; git rev-parse --symbolic-full-name "$x" >/tmp/out 2>/tmp/err; echo rc=$? out=$(cat /tmp/out) err=$(cat /tmp/err); done' in /mnt/data/openlimno
 succeeded in 0ms:
X=4c6ec2
rc=0 out= err=
X=4c6ec27
rc=0 out= err=
X=cafe
rc=128 out=cafe err=fatal: ambiguous argument 'cafe': unknown revision or path not in the working tree. Use '--' to separate paths from revisions, like this: 'git <command> [<revision>...] -- [<file>...]'
X=HEAD
rc=0 out=refs/heads/main err=
X=v0.1.0-alpha.10
rc=0 out=refs/tags/v0.1.0-alpha.10 err=
X=origin/main
rc=0 out=refs/remotes/origin/main err=

exec
/bin/bash -lc "git show 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40:scripts/triple_review.sh | sed -n '1,260p'" in /mnt/data/openlimno
 succeeded in 0ms:
#!/usr/bin/env bash
#
# Three-AI code review: runs Codex, Gemini, and Claude Code in parallel
# against the same diff, lands per-reviewer outputs as Markdown.
#
# Why three? alpha.2 shipped non-functional because Claude alone tested
# only the dev-venv path. Codex caught that. Round-2 Codex flagged a
# Gemini-shaped concern. Round-2 Gemini flagged a Codex-shaped concern.
# No single reviewer is sufficient — they have different blind spots.
#
# Usage:
#     scripts/triple_review.sh                # diff against origin/main
#     scripts/triple_review.sh v0.1.0-alpha.3 # diff against given ref
#     BASE=v0.1.0-alpha.3 scripts/triple_review.sh
#
# Outputs:
#     reviews/<sha>.codex.md
#     reviews/<sha>.gemini.md
#     reviews/<sha>.claude.md
#     reviews/<sha>.summary.md   (cross-references all three)
#
# Requires: codex, gemini, claude CLIs on PATH; git working tree clean.

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

BASE="${1:-${BASE:-origin/main}}"
# Round-4 review (Gemini) caught that the previous `case` blacklist
# missed parens, braces, newlines. ``git check-ref-format`` is git's
# own canonical validator — strictly stricter and standard. We allow
# 40-char SHAs in addition since ``check-ref-format`` rejects bare
# hex strings. Refnames containing slashes (e.g. ``origin/main``) need
# ``--allow-onelevel``.
# Round-7 review (Codex+Gemini): the previous check rejected ALL hex
# strings under 8 chars, including legitimate branches named "cafe",
# "dead", "beef" (4 chars). It also missed UPPERCASE 7-char SHAs
# like 'A72350F' since the regex was lowercase-only. Now reject
# ONLY the git-default 7-char short SHA length, case-insensitive.
# Branches with hex-shaped names of other lengths still allowed,
# 8+ char SHAs allowed.
base_lower="$(echo "$BASE" | tr 'A-F' 'a-f')"
if [[ "$base_lower" =~ ^[0-9a-f]{7}$ ]]; then
    echo "ERROR: 7-char short SHAs are collision-prone — pass full SHA or 8+ chars" >&2
    exit 2
fi
if ! [[ "$base_lower" =~ ^[0-9a-f]{8,40}$ ]] \
        && ! git check-ref-format --allow-onelevel "$BASE" 2>/dev/null; then
    echo "ERROR: '$BASE' is not a valid ref name or SHA" >&2
    exit 2
fi
HEAD_SHA="$(git rev-parse --short HEAD)"
HEAD_REF="$(git rev-parse --abbrev-ref HEAD)"
REVIEWS_DIR="reviews"
mkdir -p "$REVIEWS_DIR"

OUT_CODEX="$REVIEWS_DIR/${HEAD_SHA}.codex.md"
OUT_GEMINI="$REVIEWS_DIR/${HEAD_SHA}.gemini.md"
OUT_CLAUDE="$REVIEWS_DIR/${HEAD_SHA}.claude.md"
OUT_SUMMARY="$REVIEWS_DIR/${HEAD_SHA}.summary.md"

DIFF_FILE="$(mktemp /tmp/review-diff.XXXXXX.patch)"
trap 'rm -f "$DIFF_FILE"' EXIT

# --- Generate the diff ---------------------------------------------------
echo "==> Generating diff: $BASE..HEAD ($HEAD_SHA on $HEAD_REF)"
# Resolve $BASE to a SHA so we fail loudly on misspelled refs (rather
# than silently producing an empty diff). Round-3 review caught the
# `git diff || true` swallowing this exact failure.
if ! BASE_SHA="$(git rev-parse --verify "${BASE}^{commit}" 2>/dev/null)"; then
    echo "ERROR: can't resolve BASE='$BASE' — fetch it or pass a valid ref" >&2
    exit 2
fi
# No path filter: every changed file gets reviewed, including
# scripts/, .github/workflows/, docs/, pyproject.toml. Round-3 review
# caught the original filter excluding the very files of the orchestrator
# itself.
# Triple-dot semantics: diff against the merge-base, not the raw range.
# Codex round-3 caught that on a feature branch behind main, the
# two-dot form would include the *inverse* of unrelated main commits.
git diff "$BASE_SHA...HEAD" > "$DIFF_FILE"
DIFF_LINES=$(wc -l < "$DIFF_FILE")
echo "    diff: $DIFF_LINES lines"
if [ "$DIFF_LINES" -eq 0 ]; then
    echo "    no changes — nothing to review"
    exit 0
fi

# --- Build the shared review prompt --------------------------------------
PROMPT_FILE="$(mktemp /tmp/review-prompt.XXXXXX.txt)"
trap 'rm -f "$DIFF_FILE" "$PROMPT_FILE"' EXIT

cat > "$PROMPT_FILE" <<EOF
You are reviewing a Git diff on the OpenLimno project (open-source water-
ecology modeling, successor to PHABSIM/River2D/FishXing). The diff range
is $BASE..$HEAD_SHA on branch $HEAD_REF.

Read the unified diff supplied below (~$DIFF_LINES lines), then evaluate.
Cover:

1. Architecture — is the change consistent with what the rest of the
   codebase does? Any unjustified new abstraction?
2. Bug risk — what corner cases are missed? Race conditions, path
   traversal, integer overflow, CRS mismatches, file-handle leaks?
3. Test adequacy — what's NOT covered? Could a regression here pass
   the new tests? What minimum extra coverage would you require?
4. Performance — any O(n^2) hot paths, memory leaks, blocking I/O on
   the GUI thread?
5. Packaging / portability — anything in the build that won't reproduce
   on a clean GitHub Actions runner?
6. Security — anything user-supplied that gets concatenated into shell,
   SQL, or HTTP? Privileged file overwrites? Symlink TOCTOU?

Be specific. Cite file:line where relevant. Do NOT praise. List real
concerns only. Cap at ~600 words.
EOF

# --- Run all three reviewers in parallel ---------------------------------
echo "==> Launching Codex, Gemini, Claude in parallel..."

(
    set +e
    {
        echo "# Codex review — \`$BASE..$HEAD_SHA\`"
        echo
        echo "_Generated by \`codex review --base $BASE\` on $(date -Iseconds)_"
        echo
        codex review --base "$BASE" --title "Triple-review: $HEAD_REF $HEAD_SHA" 2>&1
    } > "$OUT_CODEX"
) &
PID_CODEX=$!

(
    set +e
    {
        echo "# Gemini review — \`$BASE..$HEAD_SHA\`"
        echo
        echo "_Generated by \`gemini -p\` on $(date -Iseconds)_"
        echo
        gemini -p "$(cat "$PROMPT_FILE")" < "$DIFF_FILE" 2>&1
    } > "$OUT_GEMINI"
) &
PID_GEMINI=$!

(
    set +e
    {
        echo "# Claude review — \`$BASE..$HEAD_SHA\`"
        echo
        echo "_Generated by \`claude -p\` on $(date -Iseconds)_"
        echo
        # Two modes:
        #   * CI-style (ANTHROPIC_API_KEY set): --bare for hermetic, stateless
        #     invocation. Skips keychain / hooks / plugin sync. Required for
        #     reproducible review on a clean GitHub Actions runner.
        #   * Local-style (no API key): rely on the user's logged-in session.
        # Either way we feed the prompt + diff via stdin (NOT argv) so we
        # don't hit ARG_MAX on big diffs.
        if [ -n "${ANTHROPIC_API_KEY:-}" ]; then
            CLAUDE_FLAGS=(-p --bare --add-dir "$REPO_ROOT")
        else
            CLAUDE_FLAGS=(-p --add-dir "$REPO_ROOT")
        fi
        {
            cat "$PROMPT_FILE"
            echo
            echo "Diff to review:"
            echo '```diff'
            cat "$DIFF_FILE"
            echo '```'
        } | claude "${CLAUDE_FLAGS[@]}" 2>&1
    } > "$OUT_CLAUDE"
) &
PID_CLAUDE=$!

# Wait for all three; record exit codes. Round-3 review (Codex) caught
# that with `set -e` active, a non-zero `wait` immediately terminates
# the script — so the prior aggregation logic was DEAD CODE that never
# ran when any reviewer failed. Now we explicitly disable errexit
# around the waits (the `|| true` form is too easy to mis-edit), then
# re-enable below for the rest of the script.
set +e
wait $PID_CODEX; CODEX_RC=$?
wait $PID_GEMINI; GEMINI_RC=$?
wait $PID_CLAUDE; CLAUDE_RC=$?
set -e

# Detect content-level failures too: a review file containing only the
# script-side header (no actual review body) is a failure even if the
# CLI returned 0. We use a simple heuristic — body must be > N lines.
HEADER_LINES=4   # script writes 4 header lines per file
declare -A REVIEW_BODY
for f in "$OUT_CODEX" "$OUT_GEMINI" "$OUT_CLAUDE"; do
    if [ -s "$f" ]; then
        body=$(($(wc -l < "$f") - HEADER_LINES))
        REVIEW_BODY[$f]=$body
    else
        REVIEW_BODY[$f]=0
    fi
done

# Aggregate failures across exit-code AND body-length checks. Use
# `(... || true)` on each test so `set -u` doesn't trip when an entry
# is absent; ${FAILURES[@]:-} below provides the same safety on read.
EXIT_CODE=0
FAILURES=()
[ "$CODEX_RC" -ne 0 ] && FAILURES+=("codex(rc=$CODEX_RC)") || true
[ "$GEMINI_RC" -ne 0 ] && FAILURES+=("gemini(rc=$GEMINI_RC)") || true
[ "$CLAUDE_RC" -ne 0 ] && FAILURES+=("claude(rc=$CLAUDE_RC)") || true
for f in "$OUT_CODEX" "$OUT_GEMINI" "$OUT_CLAUDE"; do
    if [ "${REVIEW_BODY[$f]}" -le 1 ]; then
        FAILURES+=("$(basename "$f")(empty-body)")
    fi
done

echo "==> Reviews written:"
for f in "$OUT_CODEX" "$OUT_GEMINI" "$OUT_CLAUDE"; do
    if [ -s "$f" ] && [ "${REVIEW_BODY[$f]}" -gt 1 ]; then
        echo "    ✓ $f ($(wc -l < "$f") lines)"
    elif [ -s "$f" ]; then
        echo "    ⚠ $f (header-only — reviewer didn't produce content)"
    else
        echo "    ✗ $f (empty)"
    fi
done

if [ "${#FAILURES[@]}" -gt 0 ]; then
    # `${FAILURES[*]}` is safe here — we already gated on length.
    echo "==> FAILED reviewers: ${FAILURES[*]}"
    EXIT_CODE=1
fi

# --- Summary ------------------------------------------------------------
{
    echo "# Triple-AI review — $BASE..$HEAD_SHA"
    echo
    echo "_$(git log -1 --pretty=format:'%s' HEAD)_"
    echo
    echo "_Generated $(date -Iseconds) on \`$(uname -s) $(uname -r)\`_"
    echo
    echo "## Reviewers"
    echo
    echo "| AI | output | lines |"
    echo "| --- | --- | --- |"
    for f in "$OUT_CODEX" "$OUT_GEMINI" "$OUT_CLAUDE"; do
        ai=$(echo "$f" | sed 's|.*\.\([^.]*\)\.md|\1|')
        if [ -s "$f" ]; then
            echo "| ${ai^} | [\`${f#reviews/}\`](${f#reviews/}) | $(wc -l < "$f") |"
        else
            echo "| ${ai^} | _(failed)_ | 0 |"
        fi
    done
    echo
    echo "## Diff scope"
    echo
    echo "- Range: \`$BASE..$HEAD_SHA\`"
    echo "- Lines: $DIFF_LINES"
    echo "- Files touched:"
    # Round-4 review (Codex) caught the summary previously used a

exec
/bin/bash -lc 'bash -n scripts/triple_review.sh && echo ok' in /mnt/data/openlimno
 succeeded in 0ms:
ok

exec
/bin/bash -lc 'PYTHONDONTWRITEBYTECODE=1 pytest -q tests/unit/test_xs_rows_cache.py -p no:cacheprovider' in /mnt/data/openlimno
 exited 2 in 1421ms:

==================================== ERRORS ====================================
______________ ERROR collecting tests/unit/test_xs_rows_cache.py _______________
ImportError while importing test module '/mnt/data/openlimno/tests/unit/test_xs_rows_cache.py'.
Hint: make sure your test modules/packages have valid Python names.
Traceback:
/usr/lib/python3.12/importlib/__init__.py:90: in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/unit/test_xs_rows_cache.py:21: in <module>
    from openlimno.gui_core.controller import Controller
E   ModuleNotFoundError: No module named 'openlimno'
=========================== short test summary info ============================
ERROR tests/unit/test_xs_rows_cache.py
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 error in 0.12s

exec
/bin/bash -lc 'PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src pytest -q tests/unit/test_xs_rows_cache.py -p no:cacheprovider' in /mnt/data/openlimno
 succeeded in 1809ms:
..............                                                           [100%]
14 passed in 0.44s

exec
/bin/bash -lc 'git log --oneline --decorate --max-count=20 --graph' in /mnt/data/openlimno
 succeeded in 0ms:
* 4c6ec27 (HEAD -> main, origin/main) fix: round-3 review — flag-injection guard + env-var family + Arrow MRO normalize
* 07b6287 fix: round-2 review — 2 regressions + 2 escalated residuals + env-var bypass
* e05c191 fix: round-1 review — abbreviated-SHA validation + GUI direct-call hardening
* bd925b8 chore(review): drop API-key path, CLI-subscription auth only
* dcdb4d4 docs: v0.1.0-alpha.10 round-8 review showcase + audit trail
* 86ff7a7 (tag: v0.1.0-alpha.10) fix: round-7 review — exception scope + parquet helper + uppercase SHA
* 54ec3d2 (tag: v0.1.0-alpha.9) fix: round-6 review — exception/post-None retry + empty-rows + 7-char SHA gap
* a72350f (tag: v0.1.0-alpha.8) fix: round-5 review — torn rows + backoff timing + SHA collision
* e990c00 (tag: v0.1.0-alpha.7) fix: round-4 review fixes (script + cache torn-cement)
* 8bf011d (tag: v0.1.0-alpha.6) fix: round-3 review fixes (TOCTOU + orchestrator + workflow)
* 14f6616 (tag: v0.1.0-alpha.5) docs: triple-review methodology + alpha.4 showcase
* 45aa79c feat(ci): triple-AI code review orchestrator (codex + gemini + claude)
* 4408c45 (tag: v0.1.0-alpha.4) fix: 3 issues from second-pass code review (codex+gemini)
* 01dc890 (tag: v0.1.0-alpha.3) release: v0.1.0-alpha.3 — fix critical bundle regressions
* 7dd8801 (tag: v0.1.0-alpha.2) release: v0.1.0-alpha.2 — fail-fast OSM, update check, glibc preflight
* c9ae4c9 fix(gui_core): stash xs/hyd paths from built case directly
* 68e369e (tag: v0.1.0-alpha.1) test(qgis): headless plugin-shim integration tests
* 7d5f902 feat(studio): brand icon + .AppImage byproduct gitignore
* 86b048f build(studio): PyInstaller bundle + AppImage script + Phase 5 docs
* 2027766 feat(studio): standalone PyQt5 + PyQGIS desktop application

exec
/bin/bash -lc "grep -R \"P0\\|P1\\|bug\\|ERROR\\|FAILED\\|concern\\|regress\" -n docs/reviews/v0.1.0-alpha.10/codex.md | head -80" in /mnt/data/openlimno
 succeeded in 0ms:
47:-# Round-6 review (Codex P1): a 7-char hex string like 'e990c00' is
53:-    echo "ERROR: short SHAs (<8 chars) rejected — collision risk on busy repos" >&2
63:+    echo "ERROR: 7-char short SHAs are collision-prone — pass full SHA or 8+ chars" >&2
69:     echo "ERROR: '$BASE' is not a valid ref name or SHA" >&2
82:+    Round-7 review (Codex P1): the previous implementation caught
139:+                # swallow programmer bugs (TypeError, AttributeError,
161:+    bugs (TypeError, AttributeError) propagate immediately rather than
168:+        raise TypeError("simulated programmer bug in parquet reader")
232:    Round-7 review (Codex P1): the previous implementation caught
361:        #   - Codex P2 / Claude P1: ``post is None`` was raising on
364:        #   - Gemini P0: parquet readers raise on torn input
367:        #   - Claude P1: ``cache.get("rows")`` was falsy on empty
383:                # swallow programmer bugs (TypeError, AttributeError,
547:   862	        #   - Codex P2 / Claude P1: ``post is None`` was raising on
550:   865	        #   - Gemini P0: parquet readers raise on torn input
553:   868	        #   - Claude P1: ``cache.get("rows")`` was falsy on empty
569:   884	                # swallow programmer bugs (TypeError, AttributeError,
769:    """The bug all three reviewers caught. Simulate a rewrite DURING the
811:    """Round-6 fix (Codex P0): a vanished file on attempt 0 should
834:    """Round-6 fix (Gemini P0): if `_read_wua_parquet` raises (e.g.,
855:    """Round-6 fix (Claude P1): an empty parquet (legitimate WUA
874:    """Round-5 fix (Gemini P0): if a prior good cache exists and the
906:    """Round-5 fix (Claude P1): the `time.sleep` calls in the retry
907:    loop must use linear backoff. A regression that drops the sleep
936:    the last torn read (Claude P1 + Gemini P1)."""
957:    bugs (TypeError, AttributeError) propagate immediately rather than
964:        raise TypeError("simulated programmer bug in parquet reader")
1010:# Gemini-shaped concern. Round-2 Gemini flagged a Codex-shaped concern.
1047:    echo "ERROR: 7-char short SHAs are collision-prone — pass full SHA or 8+ chars" >&2
1052:    echo "ERROR: '$BASE' is not a valid ref name or SHA" >&2
1074:    echo "ERROR: can't resolve BASE='$BASE' — fetch it or pass a valid ref" >&2
1108:3. Test adequacy — what's NOT covered? Could a regression here pass
1118:concerns only. Cap at ~600 words.
1237:    echo "==> FAILED reviewers: ${FAILURES[*]}"
1279:    echo "   bundle regressions. Codex assumed live network → Gemini caught it."
1349:    "pytest-regressions>=2",
1393:    "B",                      # bugbear
1434:# Pragmatic stance for a 1.0-rc science codebase: catch real type bugs without
1506:2026-05-09T05:30:35.907231Z ERROR codex_core::tools::router: error=write_stdin failed: stdin is closed for this session; rerun exec_command with tty=true to keep stdin open
1771: # Gemini-shaped concern. Round-2 Gemini flagged a Codex-shaped concern.
1799:-# Round-6 review (Codex P1): a 7-char hex string like 'e990c00' is
1805:-    echo "ERROR: short SHAs (<8 chars) rejected — collision risk on busy repos" >&2
1815:+    echo "ERROR: 7-char short SHAs are collision-prone — pass full SHA or 8+ chars" >&2
1821:     echo "ERROR: '$BASE' is not a valid ref name or SHA" >&2
1843:     echo "ERROR: can't resolve BASE='$BASE' — fetch it or pass a valid ref" >&2
1877: 3. Test adequacy — what's NOT covered? Could a regression here pass
1887: concerns only. Cap at ~600 words.
1949:+    Round-7 review (Codex P1): the previous implementation caught
2137:         #   - Codex P2 / Claude P1: ``post is None`` was raising on
2140:         #   - Gemini P0: parquet readers raise on torn input
2143:         #   - Claude P1: ``cache.get("rows")`` was falsy on empty
2160:+                # swallow programmer bugs (TypeError, AttributeError,
2274:tests/unit/test_xs_rows_cache.py:175:    """Round-6 fix (Gemini P0): if `_read_wua_parquet` raises (e.g.,
2387:    45	    echo "ERROR: 7-char short SHAs are collision-prone — pass full SHA or 8+ chars" >&2
2392:    50	    echo "ERROR: '$BASE' is not a valid ref name or SHA" >&2
2447:The patch introduces regressions in both the GUI parquet-read path and the review script's SHA validation. These are actionable issues that can cause unhandled GUI exceptions and allow the collision-prone short SHAs the script is trying to block.
2456:The patch introduces regressions in both the GUI parquet-read path and the review script's SHA validation. These are actionable issues that can cause unhandled GUI exceptions and allow the collision-prone short SHAs the script is trying to block.

exec
/bin/bash -lc 'git show --stat --oneline --decorate e05c191 && git show --stat --oneline 07b6287 && git show --stat --oneline 4c6ec27' in /mnt/data/openlimno
 succeeded in 0ms:
e05c191 fix: round-1 review — abbreviated-SHA validation + GUI direct-call hardening
 pyproject.toml                       |  2 +-
 scripts/triple_review.sh             | 27 ++++++++++++++++-----------
 src/openlimno/gui_core/controller.py | 25 ++++++++++++++++++++++---
 3 files changed, 39 insertions(+), 15 deletions(-)
07b6287 fix: round-2 review — 2 regressions + 2 escalated residuals + env-var bypass
 .gitignore                           |  1 +
 pyproject.toml                       |  2 +-
 scripts/triple_review.sh             | 17 +++++++++++---
 src/openlimno/gui_core/controller.py | 45 ++++++++++++++++++++++++++++++------
 4 files changed, 54 insertions(+), 11 deletions(-)
4c6ec27 fix: round-3 review — flag-injection guard + env-var family + Arrow MRO normalize
 pyproject.toml                       |  2 +-
 scripts/triple_review.sh             | 19 ++++++++++++++++++-
 src/openlimno/gui_core/controller.py | 13 ++++++++++++-
 3 files changed, 31 insertions(+), 3 deletions(-)

exec
/bin/bash -lc 'PYTHONPATH=src ruff check src/openlimno/gui_core/controller.py scripts/triple_review.sh pyproject.toml 2>&1 | head -200 || true' in /mnt/data/openlimno
 succeeded in 0ms:
invalid-syntax: Simple statements must be separated by newlines or semicolons
  --> scripts/triple_review.sh:27:10
   |
25 | # Git working tree must be clean.
26 |
27 | set -euo pipefail
   |          ^^^^^^^^
28 |
29 | REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
   |

invalid-syntax: Got unexpected token $
  --> scripts/triple_review.sh:29:18
   |
27 | set -euo pipefail
28 |
29 | REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
   |                  ^
30 | cd "$REPO_ROOT"
   |

invalid-syntax: Expected `)`, found string
  --> scripts/triple_review.sh:29:28
   |
27 | set -euo pipefail
28 |
29 | REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
   |                            ^^^^
30 | cd "$REPO_ROOT"
   |

invalid-syntax: Expected a statement
  --> scripts/triple_review.sh:29:32
   |
27 | set -euo pipefail
28 |
29 | REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
   |                                ^
30 | cd "$REPO_ROOT"
   |

invalid-syntax: Expected a statement
  --> scripts/triple_review.sh:29:33
   |
27 | set -euo pipefail
28 |
29 | REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
   |                                 ^
30 | cd "$REPO_ROOT"
   |

invalid-syntax: Expected a statement
  --> scripts/triple_review.sh:29:34
   |
27 | set -euo pipefail
28 |
29 | REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
   |                                  ^
30 | cd "$REPO_ROOT"
   |

invalid-syntax: Expected a statement
  --> scripts/triple_review.sh:29:35
   |
27 | set -euo pipefail
28 |
29 | REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
   |                                   ^
30 | cd "$REPO_ROOT"
   |

invalid-syntax: Simple statements must be separated by newlines or semicolons
  --> scripts/triple_review.sh:30:4
   |
29 | REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
30 | cd "$REPO_ROOT"
   |    ^^^^^^^^^^^^
31 |
32 | BASE="${1:-${BASE:-origin/main}}"
   |

invalid-syntax: Simple statements must be separated by newlines or semicolons
  --> scripts/triple_review.sh:39:6
   |
37 | # rev-parse modes. A simple upfront pattern check is the most
38 | # portable form of belt-and-suspenders.)
39 | case "$BASE" in
   |      ^^^^^^^
40 |     -*)
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
   |

invalid-syntax: Expected an expression
  --> scripts/triple_review.sh:39:16
   |
37 | # rev-parse modes. A simple upfront pattern check is the most
38 | # portable form of belt-and-suspenders.)
39 | case "$BASE" in
   |                ^
40 |     -*)
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
   |

invalid-syntax: Unexpected indentation
  --> scripts/triple_review.sh:40:1
   |
38 | # portable form of belt-and-suspenders.)
39 | case "$BASE" in
40 |     -*)
   | ^^^^
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
42 |         exit 2
   |

invalid-syntax: Starred expression cannot be used here
  --> scripts/triple_review.sh:40:6
   |
38 | # portable form of belt-and-suspenders.)
39 | case "$BASE" in
40 |     -*)
   |      ^
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
42 |         exit 2
   |

invalid-syntax: Expected an expression
  --> scripts/triple_review.sh:40:7
   |
38 | # portable form of belt-and-suspenders.)
39 | case "$BASE" in
40 |     -*)
   |       ^
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
42 |         exit 2
   |

invalid-syntax: Expected a statement
  --> scripts/triple_review.sh:40:7
   |
38 | # portable form of belt-and-suspenders.)
39 | case "$BASE" in
40 |     -*)
   |       ^
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
42 |         exit 2
   |

invalid-syntax: Expected a statement
  --> scripts/triple_review.sh:40:8
   |
38 | # portable form of belt-and-suspenders.)
39 | case "$BASE" in
40 |     -*)
   |        ^
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
42 |         exit 2
   |

invalid-syntax: Unexpected indentation
  --> scripts/triple_review.sh:41:1
   |
39 | case "$BASE" in
40 |     -*)
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
   | ^^^^^^^^
42 |         exit 2
43 |         ;;
   |

invalid-syntax: Simple statements must be separated by newlines or semicolons
  --> scripts/triple_review.sh:41:14
   |
39 | case "$BASE" in
40 |     -*)
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
   |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
42 |         exit 2
43 |         ;;
   |

invalid-syntax: Expected an expression
  --> scripts/triple_review.sh:41:89
   |
39 | case "$BASE" in
40 |     -*)
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
   |                                                                                         ^
42 |         exit 2
43 |         ;;
   |

invalid-syntax: Simple statements must be separated by newlines or semicolons
  --> scripts/triple_review.sh:42:14
   |
40 |     -*)
41 |         echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
42 |         exit 2
   |              ^
43 |         ;;
44 | esac

exec
/bin/bash -lc 'git diff --check 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40' in /mnt/data/openlimno
 exited 2 in 0ms:
docs/reviews/v0.1.0-alpha.10/codex.md:31: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:76: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:77: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:128: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:129: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:130: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:157: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:158: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:517: trailing whitespace.
+   832	
docs/reviews/v0.1.0-alpha.10/codex.md:526: trailing whitespace.
+   841	
docs/reviews/v0.1.0-alpha.10/codex.md:532: trailing whitespace.
+   847	
docs/reviews/v0.1.0-alpha.10/codex.md:629: trailing whitespace.
+   809	
docs/reviews/v0.1.0-alpha.10/codex.md:638: trailing whitespace.
+   818	
docs/reviews/v0.1.0-alpha.10/codex.md:641: trailing whitespace.
+   821	
docs/reviews/v0.1.0-alpha.10/codex.md:652: trailing whitespace.
+   832	
docs/reviews/v0.1.0-alpha.10/codex.md:1786: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1788: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1791: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1828: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1833: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1836: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1860: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1864: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1869: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1872: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1885: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1889: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1892: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1906: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1912: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1919: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1920: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1923: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1927: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1932: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1933: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1943: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1944: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1995: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1996: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1997: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:1998: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2004: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2011: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2020: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2027: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2049: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2054: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2084: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2093: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2096: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2107: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2116: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2122: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2187: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2192: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2201: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2226: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2228: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2231: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2238: trailing whitespace.
+ 
docs/reviews/v0.1.0-alpha.10/codex.md:2286: trailing whitespace.
+   130	
docs/reviews/v0.1.0-alpha.10/codex.md:2291: trailing whitespace.
+   135	
docs/reviews/v0.1.0-alpha.10/codex.md:2315: trailing whitespace.
+   159	
docs/reviews/v0.1.0-alpha.10/codex.md:2321: trailing whitespace.
+   165	
docs/reviews/v0.1.0-alpha.10/codex.md:2336: trailing whitespace.
+   180	
docs/reviews/v0.1.0-alpha.10/codex.md:2343: trailing whitespace.
+   187	

exec
/bin/bash -lc "grep -R \"sk-\\|AIza\\|ANTHROPIC_API_KEY=.*[A-Za-z0-9]\\|OPENAI_API_KEY=.*[A-Za-z0-9]\\|GEMINI_API_KEY=.*[A-Za-z0-9]\" -n docs/reviews/v0.1.0-alpha.10 .github scripts src pyproject.toml || true" in /mnt/data/openlimno
 succeeded in 0ms:
exec
/bin/bash -lc "sed -n '1,140p' src/openlimno/qgis/openlimno_qgis_plugin/plugin.py" in /mnt/data/openlimno
 succeeded in 0ms:
"""OpenLimno QGIS plugin — thin shim over openlimno.gui_core.

All handler logic lives in ``openlimno.gui_core.Controller``; this file
only:
- declares menu / toolbar entries for QGIS,
- adapts QGIS ``iface`` to the ``Host`` protocol gui_core expects.

The plugin is in maintenance mode: new features land in OpenLimno Studio
(``openlimno.studio``) and are picked up here automatically via the
shared Controller. See memory/project_studio.md.
"""
from __future__ import annotations

import os
import sys
from typing import Any


def _ensure_openlimno_importable() -> None:
    """Splice the OpenLimno repo onto sys.path if it isn't already.

    QGIS runs its own embedded Python; the user's pip-installed
    openlimno may not be visible. Probe the typical dev install
    locations.
    """
    try:
        import openlimno.gui_core  # noqa: F401
        return
    except ImportError:
        pass
    for cand in (
        "/mnt/data/openlimno/src",
        os.path.expanduser("~/openlimno/src"),
        os.path.expanduser("~/openlimno-env/lib/python3.12/site-packages"),
        os.path.expanduser("~/openlimno-env/lib/python3.11/site-packages"),
    ):
        if os.path.isdir(os.path.join(cand, "openlimno", "gui_core")):
            sys.path.insert(0, cand)
            return


class _PluginHost:
    """Adapter from QGIS iface to gui_core.Host protocol."""

    def __init__(self, iface):
        self._iface = iface

    def main_window(self):
        return self._iface.mainWindow()

    def map_canvas(self):
        return self._iface.mapCanvas()

    def message_bar(self):
        return self._iface.messageBar()

    def status_bar(self):
        return self._iface.statusBarIface()


class OpenLimnoPlugin:
    """Plugin object instantiated by QGIS at startup."""

    def __init__(self, iface):
        self.iface = iface
        self.actions: list[Any] = []
        self.menu = "&OpenLimno"
        _ensure_openlimno_importable()
        from openlimno.gui_core import Controller
        self.ctl = Controller(_PluginHost(iface))

    # ------------------------------------------------------------------
    def initGui(self) -> None:  # noqa: N802 (QGIS API)
        try:
            from qgis.PyQt.QtGui import QIcon
            from qgis.PyQt.QtWidgets import QAction
        except ImportError:
            return

        icon = QIcon()

        def _menu_only(text, slot):
            a = QAction(icon, text, self.iface.mainWindow())
            a.triggered.connect(slot)
            self.iface.addPluginToMenu(self.menu, a)
            self.actions.append(a)
            return a

        def _toolbar_too(text, slot):
            a = _menu_only(text, slot)
            self.iface.addToolBarIcon(a)
            return a

        _toolbar_too("Open OpenLimno hydraulic results…", self.ctl.open_hydraulic_nc)
        _menu_only("Open WUA-Q curve…", self.ctl.open_wua_q)
        _menu_only("Plot cross-section profile…", self.ctl.plot_cross_section)

        a_pick = QAction(icon, "Click cross-section to view profile",
                            self.iface.mainWindow())
        a_pick.setCheckable(True)
        a_pick.triggered.connect(self.ctl.activate_pick_tool)
        self.iface.addPluginToMenu(self.menu, a_pick)
        self.iface.addToolBarIcon(a_pick)
        self.actions.append(a_pick)
        self.ctl._pick_action = a_pick  # let controller un-check on cancel

        _toolbar_too("🆕 Build case from OSM river…", self.ctl.build_case_from_osm)
        _toolbar_too("▶ Run case…", self.ctl.run_case)

    def unload(self) -> None:
        try:
            from qgis.PyQt.QtWidgets import QAction  # noqa: F401
        except ImportError:
            return
        for a in self.actions:
            self.iface.removePluginMenu(self.menu, a)
            self.iface.removeToolBarIcon(a)
        self.actions = []

exec
/bin/bash -lc "nl -ba scripts/triple_review.sh | sed -n '18,28p;145,170p;175,205p' && nl -ba docs/triple_review.md | sed -n '21,48p;86,105p'" in /mnt/data/openlimno
 succeeded in 0ms:
    18	#     reviews/<sha>.gemini.md
    19	#     reviews/<sha>.claude.md
    20	#     reviews/<sha>.summary.md   (cross-references all three)
    21	#
    22	# Requires: codex, gemini, claude CLIs on PATH AND each logged in via
    23	# its subscription (ChatGPT / Google AI / Anthropic Console). API-key
    24	# auth is intentionally not used — see docs/triple_review.md for why.
    25	# Git working tree must be clean.
    26	
    27	set -euo pipefail
    28	
   145	echo "==> Launching Codex, Gemini, Claude in parallel..."
   146	
   147	(
   148	    set +e
   149	    {
   150	        echo "# Codex review — \`$BASE..$HEAD_SHA\`"
   151	        echo
   152	        echo "_Generated by \`codex review --base $BASE\` on $(date -Iseconds)_"
   153	        echo
   154	        codex review --base "$BASE" --title "Triple-review: $HEAD_REF $HEAD_SHA" 2>&1
   155	    } > "$OUT_CODEX"
   156	) &
   157	PID_CODEX=$!
   158	
   159	(
   160	    set +e
   161	    {
   162	        echo "# Gemini review — \`$BASE..$HEAD_SHA\`"
   163	        echo
   164	        echo "_Generated by \`gemini -p\` on $(date -Iseconds)_"
   165	        echo
   166	        gemini -p "$(cat "$PROMPT_FILE")" < "$DIFF_FILE" 2>&1
   167	    } > "$OUT_GEMINI"
   168	) &
   169	PID_GEMINI=$!
   170	
   175	        echo
   176	        echo "_Generated by \`claude -p\` on $(date -Iseconds)_"
   177	        echo
   178	        # CLI-only: rely on the user's logged-in `claude` session
   179	        # (Anthropic Console subscription). API-key mode (--bare with
   180	        # ANTHROPIC_API_KEY) is intentionally not supported — see
   181	        # docs/triple_review.md "Why CLI subscription, not API". Feed
   182	        # prompt + diff via stdin (NOT argv) to dodge ARG_MAX on big
   183	        # diffs.
   184	        # Round-2 review (Codex P2): `claude -p` silently honors
   185	        # ANTHROPIC_API_KEY whenever it's set. A stale env var from
   186	        # an old API-based setup would either fail before reaching
   187	        # the subscription session, or worse, bill the API silently
   188	        # despite the "CLI-only" promise. Strip it for this invocation
   189	        # via `env -u`.
   190	        # Round-3 review (Claude): also strip ANTHROPIC_AUTH_TOKEN
   191	        # and ANTHROPIC_BASE_URL — claude-code honors the whole
   192	        # family. Stripping only one of the three would still let
   193	        # API auth sneak in.
   194	        {
   195	            cat "$PROMPT_FILE"
   196	            echo
   197	            echo "Diff to review:"
   198	            echo '```diff'
   199	            cat "$DIFF_FILE"
   200	            echo '```'
   201	        } | env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_BASE_URL \
   202	                claude -p --add-dir "$REPO_ROOT" 2>&1
   203	    } > "$OUT_CLAUDE"
   204	) &
   205	PID_CLAUDE=$!
    21	  consistently correlated. Two reviewers with similar blind spots produce
    22	  false-positive consensus.
    23	
    24	See [docs/reviews/v0.1.0-alpha.4/](reviews/v0.1.0-alpha.4/) for the worked
    25	example where the three diverged sharply on a 92-line diff, and
    26	[docs/reviews/v0.1.0-alpha.10/](reviews/v0.1.0-alpha.10/) for the
    27	8-round audit trail and convergence proof.
    28	
    29	## Why CLI subscription, not API
    30	
    31	Each reviewer is invoked through its **CLI's own logged-in subscription**
    32	(ChatGPT for Codex, Google AI for Gemini, Anthropic Console for Claude
    33	Code). API keys (`OPENAI_API_KEY` / `GEMINI_API_KEY` / `ANTHROPIC_API_KEY`)
    34	are **not used** anywhere — not in the orchestrator, not in CI.
    35	
    36	Reasons:
    37	
    38	- The maintainer already pays for the three subscriptions; per-call API
    39	  billing on top of that is double-pay.
    40	- Subscription auth has higher rate limits and saner quota behaviour than
    41	  API tier.
    42	- It removes a class of secrets-management risk: no keys to rotate,
    43	  leak, or expire.
    44	
    45	The trade-off is that the review is **maintainer-local**: it cannot run on
    46	a headless CI runner, since CLI auth requires interactive OAuth login that
    47	GitHub-hosted runners can't perform. This is intentional; see
    48	[When to invoke](#when-to-invoke).
    86	
    87	```bash
    88	# One-time setup, done once per machine:
    89	codex auth login    # uses ChatGPT subscription
    90	gemini auth         # uses Google account
    91	claude              # interactive setup with Anthropic Console
    92	```
    93	
    94	Verify:
    95	
    96	```bash
    97	codex --version
    98	gemini --version
    99	claude --version
   100	```
   101	
   102	If any of the three are missing, the orchestrator script will refuse to
   103	run that reviewer and exit non-zero. Don't paper over by skipping; the
   104	whole point is the three-way coverage.
   105	

exec
/bin/bash -lc "git check-ref-format --allow-onelevel -- '-foo'; echo rc="'$?' in /mnt/data/openlimno
 succeeded in 0ms:
usage: git check-ref-format [--normalize] [<options>] <refname>
   or: git check-ref-format --branch <branchname-shorthand>
rc=129

exec
/bin/bash -lc 'git check-ref-format --allow-onelevel -foo; echo rc=$?' in /mnt/data/openlimno
 succeeded in 0ms:
usage: git check-ref-format [--normalize] [<options>] <refname>
   or: git check-ref-format --branch <branchname-shorthand>
rc=129

exec
/bin/bash -lc 'set +e
for x in 4C6EC2 4C6EC27; do echo X=$x; git rev-parse --symbolic-full-name "$x" >/tmp/out 2>/tmp/err; echo rc=$? out=$(cat /tmp/out) err=$(cat /tmp/err); done' in /mnt/data/openlimno
 succeeded in 0ms:
X=4C6EC2
rc=0 out= err=
X=4C6EC27
rc=0 out= err=

exec
/bin/bash -lc "python - <<'PY'
try:
 import pyarrow.lib as lib
 for name in ['ArrowInvalid','ArrowIOError','ArrowCapacityError','ArrowKeyError','ArrowNotImplementedError','ArrowTypeError']:
  cls=getattr(lib,name,None)
  if cls: print(name, cls.__mro__)
except Exception as e: print(e)
PY" in /mnt/data/openlimno
 succeeded in 236ms:
ArrowInvalid (<class 'pyarrow.lib.ArrowInvalid'>, <class 'ValueError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowIOError (<class 'OSError'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowCapacityError (<class 'pyarrow.lib.ArrowCapacityError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowKeyError (<class 'pyarrow.lib.ArrowKeyError'>, <class 'KeyError'>, <class 'LookupError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowNotImplementedError (<class 'pyarrow.lib.ArrowNotImplementedError'>, <class 'NotImplementedError'>, <class 'RuntimeError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowTypeError (<class 'pyarrow.lib.ArrowTypeError'>, <class 'TypeError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)

exec
/bin/bash -lc "find .github -maxdepth 3 -type f -print -exec sed -n '1,80p' {} \\;" in /mnt/data/openlimno
 succeeded in 0ms:
.github/ISSUE_TEMPLATE/feature.yml
name: Feature request
description: Propose a new capability
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        OpenLimno 1.0 has a frozen scope (SPEC §0.3). Before filing:
        - If this is in §13 (research roadmap), label your issue `research-roadmap`.
        - If this would expand 1.0 scope, file a [SPEC change proposal](../docs/governance/SPEC_CHANGE_PROPOSAL.md) instead of a feature request.
  - type: dropdown
    id: scope
    attributes:
      label: Scope category
      options:
        - 1.0 scope (already in §4)
        - Research roadmap (§13)
        - Would expand 1.0 scope (need SPEC change)
    validations:
      required: true
  - type: textarea
    id: motivation
    attributes:
      label: Motivation
      description: What concrete user / project need does this solve?
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: Proposal
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered
.github/ISSUE_TEMPLATE/bug.yml
name: Bug report
description: Something doesn't work as documented in SPEC.md
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting. Before filing, please check:
        - This is **not** a §0.3 non-goal (SPEC.md). If it is, this isn't a bug.
        - There's no existing duplicate issue.
  - type: input
    id: openlimno-version
    attributes:
      label: OpenLimno version
      placeholder: "0.X.Y"
    validations:
      required: true
  - type: dropdown
    id: platform
    attributes:
      label: Platform
      options: [Linux, macOS, Windows]
    validations:
      required: true
  - type: input
    id: python-version
    attributes:
      label: Python version
      placeholder: "3.11.x / 3.12.x"
    validations:
      required: true
  - type: textarea
    id: spec-section
    attributes:
      label: SPEC section claimed (if applicable)
      description: Quote the SPEC text the actual behavior contradicts.
  - type: textarea
    id: reproduce
    attributes:
      label: Steps to reproduce
      description: Minimum command + minimum YAML / data
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
    validations:
      required: true
  - type: textarea
    id: actual
    attributes:
      label: Actual behavior
    validations:
      required: true
  - type: textarea
    id: provenance
    attributes:
      label: provenance.json (if available)
      description: Paste the contents; this captures versions / inputs deterministically.
.github/CODEOWNERS
# OpenLimno CODEOWNERS
# Per SPEC §11 governance: ≥ 2 reviewers per core module (bus factor)
# M0 deliverable: replace _TBD_ with real GitHub handles

# Default fallback (PSC reviews)
*                                           @openlimno/psc

# Module ownership
/src/openlimno/wedm/                        @openlimno/wedm-maintainers
/src/openlimno/preprocess/                  @openlimno/preprocess-maintainers
/src/openlimno/hydro/                       @openlimno/hydro-maintainers
/src/openlimno/habitat/                     @openlimno/habitat-maintainers
/src/openlimno/passage/                     @openlimno/passage-maintainers
/src/openlimno/studyplan/                   @openlimno/habitat-maintainers
/src/openlimno/qgis/                        @openlimno/qgis-maintainers

# Documentation
/docs/                                      @openlimno/docs-maintainers
/SPEC.md                                    @openlimno/psc
/docs/governance/                           @openlimno/psc

# CI / build
/.github/                                   @openlimno/release-managers
/pixi.toml                                  @openlimno/release-managers
/pyproject.toml                             @openlimno/release-managers

# Schemas (extra eyes - data model is contract)
/src/openlimno/wedm/schemas/                @openlimno/wedm-maintainers @openlimno/psc

# Benchmarks (must not regress)
/benchmarks/                                @openlimno/psc
.github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main, "release/*"]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: v0.67.2
          environments: default
          cache: true
      - run: pixi run lint
      - run: pixi run -- ruff format --check src/ tests/

  typecheck:
    name: Type check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: v0.67.2
          environments: default
          cache: true
      - run: pixi run typecheck

  schema-validation:
    name: WEDM schema self-validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: v0.67.2
          environments: default
          cache: true
      - run: pixi run validate-schemas

  test:
    name: Test (${{ matrix.os }} / Py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: v0.67.2
          environments: default
          cache: true
      - run: pixi run test-cov
      - uses: codecov/codecov-action@v4
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  benchmark-fast:
    name: Benchmark (fast subset, every PR)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: v0.67.2
          environments: default
.github/workflows/benchmark-weekly.yml
name: Weekly benchmarks

on:
  schedule:
    # Mondays 02:00 UTC
    - cron: "0 2 * * 1"
  workflow_dispatch:

jobs:
  benchmark-all:
    name: Full benchmark suite
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: v0.67.2
          environments: schism
          cache: true
      - run: pixi run -e schism benchmark-all
      - name: Publish benchmark dashboard
        # M2+: push results to gh-pages benchmarks/ for public dashboard
        run: echo "TODO M2: publish to gh-pages"
.github/workflows/schism-container.yml
# Build + push the SCHISM v5.11.0 OCI container to GHCR.
#
# Triggered:
#   - manually via "Run workflow" in GitHub Actions UI
#   - on push to main when containers/schism/** changes (build only, no push)
#   - on tag matching schism-v* (build + push)
#
# Required repository secrets:
#   GHCR_PAT     — Personal Access Token with packages:write scope
#                  (or use GITHUB_TOKEN if the repo allows packages:write)

name: SCHISM container

on:
  workflow_dispatch:
    inputs:
      push:
        description: "Push to ghcr.io after build"
        required: true
        default: "false"
        type: choice
        options: ["true", "false"]
  push:
    branches: [main]
    paths:
      - "containers/schism/**"
      - ".github/workflows/schism-container.yml"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: openlimno/schism
  SCHISM_VERSION: 5.11.0

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    name: Build SCHISM ${{ env.SCHISM_VERSION }}
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Decide whether to push
        id: push_decision
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ inputs.push }}" == "true" ]]; then
            echo "push=true" >> "$GITHUB_OUTPUT"
          else
            echo "push=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Login to GHCR
        if: steps.push_decision.outputs.push == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build (and push if enabled)
        uses: docker/build-push-action@v5
        with:
          context: containers/schism
          file: containers/schism/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ steps.push_decision.outputs.push == 'true' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.SCHISM_VERSION }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image digest
        if: steps.push_decision.outputs.push == 'true'
.github/PULL_REQUEST_TEMPLATE.md
## Summary

<!-- 1-3 sentences. What and why. -->

## Scope discipline (SPEC §0.3)

- [ ] This PR is **in 1.0 scope** (SPEC §4 modules)
- [ ] This PR is **research roadmap** (SPEC §13) and targets a `research-*` branch, not `main`
- [ ] This PR is documentation / CI / chore only

If you ticked "in 1.0 scope" and the work touches a §0.3 non-goal, file a [SPEC change proposal](../docs/governance/SPEC_CHANGE_PROPOSAL.md) first.

## Type

- [ ] Bug fix
- [ ] Feature (in 1.0 scope)
- [ ] Documentation
- [ ] Refactor (no behavior change)
- [ ] Schema change (WEDM)
- [ ] Benchmark / validation
- [ ] CI / build

## Checklist

- [ ] `pixi run check` passes locally
- [ ] If WEDM schema changed: validation example added to `tests/unit/wedm/`
- [ ] If module spec impl: regression case in `tests/`
- [ ] If user-facing: docs updated
- [ ] CHANGELOG.md updated (under "Unreleased")
- [ ] Linked to issue (if applicable): #
- [ ] No new dependency with non-Apache/MIT/BSD/LGPL license

## Reviewers

(CODEOWNERS will auto-assign. ≥ 1 maintainer review for routine, ≥ 2 for security / migration / SPEC-touching code.)

## Related

- SPEC section affected: §
- ADR (if architectural): docs/decisions/00XX-...

exec
/bin/bash -lc "for r in 'foo"'$(touchx)'"' 'foo"'`bar`'"' 'foo(bar)' 'foo{bar}' 'foo"'$bar'"' 'foo;bar'; do printf '%s ' \""'$r"; git check-ref-format --allow-onelevel "$r"; echo rc=$?; done' in /mnt/data/openlimno
 succeeded in 0ms:
foo$(touchx) rc=0
foo`bar` rc=0
foo(bar) rc=0
foo{bar} rc=0
foo$bar rc=0
foo;bar rc=0

exec
/bin/bash -lc 'cat src/openlimno/gui_core/__init__.py' in /mnt/data/openlimno
 succeeded in 0ms:
"""OpenLimno GUI core: shared dialog / handler logic for plugin + Studio.

Handlers live on `Controller`, which receives a `Host` adapter exposing
the four QGIS-iface-shaped methods the handlers need (main_window,
map_canvas, message_bar, status_bar). Both the QGIS plugin and
OpenLimno Studio create one Controller per session.

This module is the only place where build/run/click/plot logic lives;
plugin.py and studio/main_window.py are thin UI-toolkit-only wiring.
"""
from openlimno.gui_core.controller import Controller, Host

__all__ = ["Controller", "Host"]

exec
/bin/bash -lc "grep -n \"lint\\|ruff\" -n pixi.toml | head -80" in /mnt/data/openlimno
 succeeded in 0ms:
74:ruff = ">=0.4"
117:lint = "ruff check src/ tests/"
118:lint-fix = "ruff check --fix src/ tests/"
119:format = "ruff format src/ tests/"
130:check = { depends-on = ["lint", "typecheck", "test", "validate-schemas"] }

exec
/bin/bash -lc 'ruff check src/openlimno/gui_core/controller.py 2>&1 | head -200 || true' in /mnt/data/openlimno
 succeeded in 0ms:
N818 Exception name `MissingParquetBackend` should be named with an Error suffix
  --> src/openlimno/gui_core/controller.py:41:7
   |
41 | class MissingParquetBackend(RuntimeError):
   |       ^^^^^^^^^^^^^^^^^^^^^
42 |     """Neither pyarrow nor GDAL/OGR is importable.
   |

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:168:9
    |
167 |       def open_wua_q(self) -> None:
168 | /         from qgis.PyQt.QtWidgets import (
169 | |             QDialog, QFileDialog, QMessageBox, QTableWidget, QTableWidgetItem,
170 | |             QVBoxLayout,
171 | |         )
    | |_________^
172 |
173 |           path, _ = QFileDialog.getOpenFileName(
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:207:9
    |
206 |       def plot_cross_section(self) -> None:
207 | /         from qgis.PyQt.QtWidgets import (
208 | |             QComboBox, QDialog, QDialogButtonBox, QFileDialog,
209 | |             QFormLayout, QMessageBox, QVBoxLayout,
210 | |         )
    | |_________^
211 |
212 |           xs_path, _ = QFileDialog.getOpenFileName(
    |
help: Organize imports

F401 [*] `qgis.PyQt.QtWidgets.QVBoxLayout` imported but unused
   --> src/openlimno/gui_core/controller.py:209:39
    |
207 |         from qgis.PyQt.QtWidgets import (
208 |             QComboBox, QDialog, QDialogButtonBox, QFileDialog,
209 |             QFormLayout, QMessageBox, QVBoxLayout,
    |                                       ^^^^^^^^^^^
210 |         )
    |
help: Remove unused import: `qgis.PyQt.QtWidgets.QVBoxLayout`

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:246:33
    |
244 |         dlg.setWindowTitle("Plot cross-section")
245 |         form = QFormLayout(dlg)
246 |         cb_station = QComboBox(); cb_station.addItems([f"{s:g}" for s in stations])
    |                                 ^
247 |         form.addRow("station_m:", cb_station)
248 |         cb_q = QComboBox()
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:264:40
    |
262 |         bb = QDialogButtonBox(QDialogButtonBox.StandardButton.Ok |
263 |                                 QDialogButtonBox.StandardButton.Cancel)
264 |         bb.accepted.connect(dlg.accept); bb.rejected.connect(dlg.reject)
    |                                        ^
265 |         form.addRow(bb)
266 |         if dlg.exec() != QDialog.DialogCode.Accepted:
    |

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:279:9
    |
277 |       # ------------------------------------------------------------------
278 |       def build_case_from_osm(self) -> None:
279 | /         from qgis.PyQt.QtWidgets import (
280 | |             QButtonGroup, QDialog, QDialogButtonBox, QDoubleSpinBox,
281 | |             QFileDialog, QFormLayout, QHBoxLayout, QLabel, QLineEdit,
282 | |             QMessageBox, QPushButton, QRadioButton, QSpinBox, QWidget,
283 | |         )
    | |_________^
284 |
285 |           dlg = QDialog(self.host.main_window())
    |
help: Organize imports

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:312:33
    |
310 |         form.addRow(QLabel("<b>Reach location</b>"))
311 |         form.addRow(rb_bbox, e_bbox)
312 |         polyline_row = QWidget(); pl = QHBoxLayout(polyline_row); pl.setContentsMargins(0, 0, 0, 0)
    |                                 ^
313 |         pl.addWidget(e_polyline)
314 |         btn_browse = QPushButton("Browse…")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:312:65
    |
310 |         form.addRow(QLabel("<b>Reach location</b>"))
311 |         form.addRow(rb_bbox, e_bbox)
312 |         polyline_row = QWidget(); pl = QHBoxLayout(polyline_row); pl.setContentsMargins(0, 0, 0, 0)
    |                                                                 ^
313 |         pl.addWidget(e_polyline)
314 |         btn_browse = QPushButton("Browse…")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:320:38
    |
318 |                                                   "", "GeoJSON (*.geojson *.json)")
319 |             if p:
320 |                 e_polyline.setText(p); rb_polyline.setChecked(True)
    |                                      ^
321 |         btn_browse.clicked.connect(_browse)
322 |         pl.addWidget(btn_browse)
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:328:25
    |
326 |         form.addRow("    region:", e_region)
327 |
328 |         e_n = QSpinBox(); e_n.setRange(3, 200); e_n.setValue(11)
    |                         ^
329 |         e_reach = QDoubleSpinBox(); e_reach.setRange(0.1, 100); e_reach.setValue(1.0); e_reach.setSuffix(" km")
330 |         e_w = QDoubleSpinBox(); e_w.setRange(1, 500); e_w.setValue(10); e_w.setSuffix(" m")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:328:47
    |
326 |         form.addRow("    region:", e_region)
327 |
328 |         e_n = QSpinBox(); e_n.setRange(3, 200); e_n.setValue(11)
    |                                               ^
329 |         e_reach = QDoubleSpinBox(); e_reach.setRange(0.1, 100); e_reach.setValue(1.0); e_reach.setSuffix(" km")
330 |         e_w = QDoubleSpinBox(); e_w.setRange(1, 500); e_w.setValue(10); e_w.setSuffix(" m")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:329:35
    |
328 |         e_n = QSpinBox(); e_n.setRange(3, 200); e_n.setValue(11)
329 |         e_reach = QDoubleSpinBox(); e_reach.setRange(0.1, 100); e_reach.setValue(1.0); e_reach.setSuffix(" km")
    |                                   ^
330 |         e_w = QDoubleSpinBox(); e_w.setRange(1, 500); e_w.setValue(10); e_w.setSuffix(" m")
331 |         e_d = QDoubleSpinBox(); e_d.setRange(0.1, 50); e_d.setValue(1.0); e_d.setSuffix(" m")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:329:63
    |
328 |         e_n = QSpinBox(); e_n.setRange(3, 200); e_n.setValue(11)
329 |         e_reach = QDoubleSpinBox(); e_reach.setRange(0.1, 100); e_reach.setValue(1.0); e_reach.setSuffix(" km")
    |                                                               ^
330 |         e_w = QDoubleSpinBox(); e_w.setRange(1, 500); e_w.setValue(10); e_w.setSuffix(" m")
331 |         e_d = QDoubleSpinBox(); e_d.setRange(0.1, 50); e_d.setValue(1.0); e_d.setSuffix(" m")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:329:86
    |
328 |         e_n = QSpinBox(); e_n.setRange(3, 200); e_n.setValue(11)
329 |         e_reach = QDoubleSpinBox(); e_reach.setRange(0.1, 100); e_reach.setValue(1.0); e_reach.setSuffix(" km")
    |                                                                                      ^
330 |         e_w = QDoubleSpinBox(); e_w.setRange(1, 500); e_w.setValue(10); e_w.setSuffix(" m")
331 |         e_d = QDoubleSpinBox(); e_d.setRange(0.1, 50); e_d.setValue(1.0); e_d.setSuffix(" m")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:330:31
    |
328 |         e_n = QSpinBox(); e_n.setRange(3, 200); e_n.setValue(11)
329 |         e_reach = QDoubleSpinBox(); e_reach.setRange(0.1, 100); e_reach.setValue(1.0); e_reach.setSuffix(" km")
330 |         e_w = QDoubleSpinBox(); e_w.setRange(1, 500); e_w.setValue(10); e_w.setSuffix(" m")
    |                               ^
331 |         e_d = QDoubleSpinBox(); e_d.setRange(0.1, 50); e_d.setValue(1.0); e_d.setSuffix(" m")
332 |         e_elev = QDoubleSpinBox(); e_elev.setRange(0, 9000); e_elev.setValue(1500); e_elev.setSuffix(" m")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:330:53
    |
328 |         e_n = QSpinBox(); e_n.setRange(3, 200); e_n.setValue(11)
329 |         e_reach = QDoubleSpinBox(); e_reach.setRange(0.1, 100); e_reach.setValue(1.0); e_reach.setSuffix(" km")
330 |         e_w = QDoubleSpinBox(); e_w.setRange(1, 500); e_w.setValue(10); e_w.setSuffix(" m")
    |                                                     ^
331 |         e_d = QDoubleSpinBox(); e_d.setRange(0.1, 50); e_d.setValue(1.0); e_d.setSuffix(" m")
332 |         e_elev = QDoubleSpinBox(); e_elev.setRange(0, 9000); e_elev.setValue(1500); e_elev.setSuffix(" m")
    |

E702 Multiple statements on one line (semicolon)
   --> src/openlimno/gui_core/controller.py:330:71
    |
328 |         e_n = QSpinBox(); e_n.setRange(3, 200); e_n.setValue(11)
329 |         e_reach = QDoubleSpinBox(); e_reach.setRange(0.1, 100); e_reach.setValue(1.0); e_reach.setSuffix(" km")
330 |         e_w = QDoubleSpinBox(); e_w.setRange(1, 500); e_w.setValue(10); e_w.setSuffix(" m")
    |                                                                       ^
331 |         e_d = QDoubleSpinBox(); e_d.setRange(0.1, 50); e_d.setValue(1.0); e_d.setSuffix(" m")

exec
/bin/bash -lc "git show 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40:src/openlimno/gui_core/controller.py > /tmp/controller_base.py && ruff check /tmp/controller_base.py --select N818,I001,F401 2>&1 || true && echo '--- current subset' && ruff check src/openlimno/gui_core/controller.py --select N818,I001,F401 2>&1 || true" in /mnt/data/openlimno
 succeeded in 0ms:
I001 [*] Import block is un-sorted or un-formatted
   --> /tmp/controller_base.py:132:9
    |
131 |       def open_wua_q(self) -> None:
132 | /         from qgis.PyQt.QtWidgets import (
133 | |             QDialog, QFileDialog, QTableWidget, QTableWidgetItem, QVBoxLayout,
134 | |         )
    | |_________^
135 |
136 |           path, _ = QFileDialog.getOpenFileName(
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> /tmp/controller_base.py:161:9
    |
160 |       def plot_cross_section(self) -> None:
161 | /         from qgis.PyQt.QtWidgets import (
162 | |             QComboBox, QDialog, QDialogButtonBox, QFileDialog,
163 | |             QFormLayout, QMessageBox, QVBoxLayout,
164 | |         )
    | |_________^
165 |
166 |           xs_path, _ = QFileDialog.getOpenFileName(
    |
help: Organize imports

F401 [*] `qgis.PyQt.QtWidgets.QVBoxLayout` imported but unused
   --> /tmp/controller_base.py:163:39
    |
161 |         from qgis.PyQt.QtWidgets import (
162 |             QComboBox, QDialog, QDialogButtonBox, QFileDialog,
163 |             QFormLayout, QMessageBox, QVBoxLayout,
    |                                       ^^^^^^^^^^^
164 |         )
    |
help: Remove unused import: `qgis.PyQt.QtWidgets.QVBoxLayout`

I001 [*] Import block is un-sorted or un-formatted
   --> /tmp/controller_base.py:224:9
    |
222 |       # ------------------------------------------------------------------
223 |       def build_case_from_osm(self) -> None:
224 | /         from qgis.PyQt.QtWidgets import (
225 | |             QButtonGroup, QDialog, QDialogButtonBox, QDoubleSpinBox,
226 | |             QFileDialog, QFormLayout, QHBoxLayout, QLabel, QLineEdit,
227 | |             QMessageBox, QPushButton, QRadioButton, QSpinBox, QWidget,
228 | |         )
    | |_________^
229 |
230 |           dlg = QDialog(self.host.main_window())
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> /tmp/controller_base.py:377:9
    |
376 |           # Show "in progress" + flush event loop so the message bar appears
377 | /         from qgis.PyQt.QtWidgets import QApplication
378 | |         from qgis.PyQt.QtGui import QGuiApplication
379 | |         from qgis.PyQt.QtCore import Qt
    | |_______________________________________^
380 |
381 |           self.host.message_bar().pushMessage(
    |
help: Organize imports

F401 [*] `qgis.PyQt.QtWidgets.QMessageBox` imported but unused
   --> /tmp/controller_base.py:415:54
    |
413 |     def run_case(self) -> None:
414 |         from qgis.PyQt.QtCore import QThread, pyqtSignal
415 |         from qgis.PyQt.QtWidgets import QFileDialog, QMessageBox
    |                                                      ^^^^^^^^^^^
416 |
417 |         # Re-entry guard. Without this, a second click while the first
    |
help: Remove unused import: `qgis.PyQt.QtWidgets.QMessageBox`

I001 [*] Import block is un-sorted or un-formatted
   --> /tmp/controller_base.py:528:13
    |
526 |       def _load_case_layers(self, case_dir: Path) -> list[str]:
527 |           try:
528 | /             from netCDF4 import Dataset
529 | |             import pandas as pd
530 | |             from qgis.core import (
531 | |                 QgsFeature, QgsField, QgsGeometry, QgsPointXY,
532 | |                 QgsProject, QgsVectorLayer,
533 | |             )
534 | |             from qgis.PyQt.QtCore import QVariant
    | |_________________________________________________^
535 |           except ImportError:
536 |               return []
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> /tmp/controller_base.py:699:9
    |
698 |       def activate_pick_tool(self, checked: bool) -> None:
699 | /         from qgis.core import (
700 | |             QgsCoordinateTransform, QgsFeatureRequest, QgsGeometry,
701 | |             QgsPointXY, QgsProject, QgsRectangle, QgsVectorLayer,
702 | |         )
703 | |         from qgis.gui import QgsMapTool
704 | |         from qgis.PyQt.QtWidgets import QFileDialog, QMessageBox
    | |________________________________________________________________^
705 |
706 |           canvas = self.host.map_canvas()
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> /tmp/controller_base.py:913:9
    |
912 |       def _plot_at_station(self, station: float) -> None:
913 | /         from qgis.PyQt.QtWidgets import (
914 | |             QComboBox, QDialog, QDialogButtonBox, QFormLayout, QMessageBox,
915 | |         )
    | |_________^
916 |
917 |           if not self._xs_parquet:
    |
help: Organize imports

Found 9 errors.
[*] 9 fixable with the `--fix` option.
--- current subset
N818 Exception name `MissingParquetBackend` should be named with an Error suffix
  --> src/openlimno/gui_core/controller.py:41:7
   |
41 | class MissingParquetBackend(RuntimeError):
   |       ^^^^^^^^^^^^^^^^^^^^^
42 |     """Neither pyarrow nor GDAL/OGR is importable.
   |

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:168:9
    |
167 |       def open_wua_q(self) -> None:
168 | /         from qgis.PyQt.QtWidgets import (
169 | |             QDialog, QFileDialog, QMessageBox, QTableWidget, QTableWidgetItem,
170 | |             QVBoxLayout,
171 | |         )
    | |_________^
172 |
173 |           path, _ = QFileDialog.getOpenFileName(
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:207:9
    |
206 |       def plot_cross_section(self) -> None:
207 | /         from qgis.PyQt.QtWidgets import (
208 | |             QComboBox, QDialog, QDialogButtonBox, QFileDialog,
209 | |             QFormLayout, QMessageBox, QVBoxLayout,
210 | |         )
    | |_________^
211 |
212 |           xs_path, _ = QFileDialog.getOpenFileName(
    |
help: Organize imports

F401 [*] `qgis.PyQt.QtWidgets.QVBoxLayout` imported but unused
   --> src/openlimno/gui_core/controller.py:209:39
    |
207 |         from qgis.PyQt.QtWidgets import (
208 |             QComboBox, QDialog, QDialogButtonBox, QFileDialog,
209 |             QFormLayout, QMessageBox, QVBoxLayout,
    |                                       ^^^^^^^^^^^
210 |         )
    |
help: Remove unused import: `qgis.PyQt.QtWidgets.QVBoxLayout`

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:279:9
    |
277 |       # ------------------------------------------------------------------
278 |       def build_case_from_osm(self) -> None:
279 | /         from qgis.PyQt.QtWidgets import (
280 | |             QButtonGroup, QDialog, QDialogButtonBox, QDoubleSpinBox,
281 | |             QFileDialog, QFormLayout, QHBoxLayout, QLabel, QLineEdit,
282 | |             QMessageBox, QPushButton, QRadioButton, QSpinBox, QWidget,
283 | |         )
    | |_________^
284 |
285 |           dlg = QDialog(self.host.main_window())
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:432:9
    |
431 |           # Show "in progress" + flush event loop so the message bar appears
432 | /         from qgis.PyQt.QtWidgets import QApplication
433 | |         from qgis.PyQt.QtGui import QGuiApplication
434 | |         from qgis.PyQt.QtCore import Qt
    | |_______________________________________^
435 |
436 |           self.host.message_bar().pushMessage(
    |
help: Organize imports

F401 [*] `qgis.PyQt.QtWidgets.QMessageBox` imported but unused
   --> src/openlimno/gui_core/controller.py:470:54
    |
468 |     def run_case(self) -> None:
469 |         from qgis.PyQt.QtCore import QThread, pyqtSignal
470 |         from qgis.PyQt.QtWidgets import QFileDialog, QMessageBox
    |                                                      ^^^^^^^^^^^
471 |
472 |         # Re-entry guard. Without this, a second click while the first
    |
help: Remove unused import: `qgis.PyQt.QtWidgets.QMessageBox`

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:583:13
    |
581 |       def _load_case_layers(self, case_dir: Path) -> list[str]:
582 |           try:
583 | /             from netCDF4 import Dataset
584 | |             import pandas as pd
585 | |             from qgis.core import (
586 | |                 QgsFeature, QgsField, QgsGeometry, QgsPointXY,
587 | |                 QgsProject, QgsVectorLayer,
588 | |             )
589 | |             from qgis.PyQt.QtCore import QVariant
    | |_________________________________________________^
590 |           except ImportError:
591 |               return []
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:754:9
    |
753 |       def activate_pick_tool(self, checked: bool) -> None:
754 | /         from qgis.core import (
755 | |             QgsCoordinateTransform, QgsFeatureRequest, QgsGeometry,
756 | |             QgsPointXY, QgsProject, QgsRectangle, QgsVectorLayer,
757 | |         )
758 | |         from qgis.gui import QgsMapTool
759 | |         from qgis.PyQt.QtWidgets import QFileDialog, QMessageBox
    | |________________________________________________________________^
760 |
761 |           canvas = self.host.map_canvas()
    |
help: Organize imports

I001 [*] Import block is un-sorted or un-formatted
   --> src/openlimno/gui_core/controller.py:974:9
    |
973 |       def _plot_at_station(self, station: float) -> None:
974 | /         from qgis.PyQt.QtWidgets import (
975 | |             QComboBox, QDialog, QDialogButtonBox, QFormLayout, QMessageBox,
976 | |         )
    | |_________^
977 |
978 |           if not self._xs_parquet:
    |
help: Organize imports

Found 10 errors.
[*] 9 fixable with the `--fix` option.

exec
/bin/bash -lc "ls -1 && sed -n '1,80p' CHANGELOG.md 2>/dev/null || true" in /mnt/data/openlimno
 succeeded in 0ms:
CHANGELOG.md
CODE_OF_CONDUCT.md
CONTRIBUTING.md
LICENSE
OpenLimnoStudio-x86_64.AppImage
README.md
SPEC-v0.1-archive.md
SPEC.md
benchmarks
build
containers
data
dist
docs
examples
mkdocs.yml
packaging
pixi.lock
pixi.toml
pyproject.toml
reviews
scripts
site
src
tests
tools
# Changelog

All notable changes documented here. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added
- M0 repository scaffolding: pixi.toml, pyproject.toml, CI matrix, governance documents, ADR templates, WEDM JSON-Schema initial drafts
- SPEC v0.5 frozen, Approved-for-M0 (unconditional)
- Lemhi sample data package (`data/lemhi/`): USGS 13305000 real discharge, public-value steelhead HSI (Bovee 1978 / Raleigh 1984), synthetic mesh + cross-sections + rating curve, all schema-validated
- `tools/build_lemhi_dataset.py` reproducible builder (live USGS fetch + offline fallback)
- ADR-0002 SCHISM integration: **Accepted**, LTS pin v5.11.0, OCI container distribution (conda-forge not feasible)
- ADR-0003 1D engine: **Accepted (build, minimal scope)** — in-house Manning + standard step; MASCARET / HEC-RAS / SWMM / SOBEK rejected after upstream survey
- **M1 vertical slice delivered**:
    - `openlimno.hydro.Builtin1D.solve_normal_depth` — MANSQ Manning normal-depth (PHABSIM/MANSQ equivalent)
    - `openlimno.habitat` — HSI evaluation, composite (geom/arith/min/weighted), cell-level WUA, hard `acknowledge_independence` guard
    - `openlimno.case.Case` — end-to-end orchestrator: YAML → validate → hydraulics → habitat → NetCDF/Parquet/CSV + provenance.json
    - `openlimno run` CLI command wired
    - `examples/lemhi/quickstart.py` produces a textbook unimodal WUA-Q curve (peak ~7 m³/s, 65 m² for steelhead spawning)
- **M2 progress (early delivery)**:
    - `openlimno.hydro.Builtin1D.solve_standard_step` — backwater profile from downstream-known WSE (PHABSIM/IFG4 equivalent)
    - `openlimno.habitat.classify_hmu` + `aggregate_wua_by_hmu` — Wadeson 1994 / Parasiewicz 2007 mesohabitat (cascade/step/riffle/run/glide/pool/backwater); ADR-0008
    - `openlimno.passage` — Culvert + SwimmingModel + `passage_success_rate` (η_P; deterministic + Monte Carlo); ADR-0007 attraction-passage decomposition explicit
    - `openlimno.habitat.regulatory_export.cn_sl712` — SL/Z 712-2014 four-tuple (monthly min / suitable / multi-year-avg% / 90% guarantee); ADR-0009
    - `openlimno.habitat.regulatory_export.us_ferc_4e` — FERC 4(e) flow regime by water-year type (wet/normal/dry)
    - `openlimno.habitat.regulatory_export.eu_wfd` — EU WFD ecological status (high/good/moderate/poor/bad) via WUA-based EQR proxy
    - `openlimno.studyplan` — IFIM Step 1-2 study planning module: problem statement, target species rationale, objective variable picker, TUF library/case merge (SPEC §4.4.1.1 priority rules), HSI source decision tree, uncertainty acknowledgment; CLI `openlimno studyplan validate|report`
    - `openlimno.habitat.drifting_egg` — 1D Lagrangian drift evaluation for pelagic-spawning carps (grass carp / silver carp / 圆口铜鱼); SPEC §4.2.6
    - `openlimno.qgis.openlimno_qgis_plugin` — QGIS plugin (M2 alpha read-only viewer), follows ADR-0005 Layer 1 strategy (no in-process OpenLimno deps)
    - **Bovee 1997 PHABSIM regression benchmark** (`benchmarks/phabsim_bovee1997/`) — closed-form rectangular Manning + analytic WUA matched within 1e-3
    - Lemhi data builder extended with `drifting_egg_params.parquet` (3 Chinese pelagic-spawning species)
    - `examples/lemhi/studyplan.yaml` — full IFIM study plan example with TUF override
- Total test count after M2 phase 1: **125 passing**

- **M3-M4 progress (additional early delivery, 2026-05-07)**:
    - `openlimno.preprocess` — production CSV/Excel cross-section reader, USGS QRev CSV ADCP reader (column-alias tolerant), GeoTIFF DEM reader (rasterio + GDAL fallback) with along-line sampling
    - CLI: `openlimno preprocess xs|adcp|dem-info` wired to real implementations
    - `openlimno.hydro.SCHISMAdapter` (M3 alpha) — subprocess wrapper, LTS-pin v5.11.0, container + native command builders, dry-run mode for CI without SCHISM
    - `openlimno.workflows.calibrate_manning_n` — scipy.optimize 1-parameter Manning's n calibration against rating curves; CLI `openlimno calibrate`; Snakefile template at `src/openlimno/workflows/snakefiles/calibrate.smk`
    - SPEC §7 verification suite expanded: MMS 1D (5 grid resolutions), Toro Riemann placeholder + geometry self-consistency, plus Bovee 1997 — all on the PR-required CI path
    - mkdocs user guide filled with real content: install, quickstart, concepts, preprocess, hydro, habitat, passage, studyplan, regulatory_export, qgis pages
    - mkdocstrings auto-doc API reference for 5 modules
- Test count after M3-M4 phase 1: **156 passing**

- **Honest-completion sweep (2026-05-07)** — addressed gaps identified in code audit:
    - **B. case.run() full integration** — studyplan / HMU multi-scale / regulatory_export auto-invoked; cell + HMU WUA tables both written; case YAML `regulatory_export: [...]` now drives outputs (`sl712.csv`, `ferc_4e.csv`, `eu_wfd.csv`)
    - **D. HydroSolver Protocol real implementation** — Builtin1D.prepare/run/read_results now do real work (pickled work-dir state); SCHISM backend wired into `case.run()` with dry-run fallback for environments without SCHISM
    - **C. HSI quality_grade watermarking** — wua_q.csv/wua_hmu.csv get a `# … grade B/C` header; quickstart PNG gets a red diagonal "C-GRADE — TENTATIVE" overlay; provenance.wua_quality_grade recorded
    - **A. CLI 6 stubs filled** — `init` (project skeleton), `wua` (computes + plots), `passage` (full η_P + η_A composition), `reproduce` (SHA-256 verification + optional rerun), `studyplan init` (templated YAML), `hsi upgrade` (interactive + bulk metadata edit)
    - **E. Provenance full SPEC §1 P7 fields** — added per-file SHA-256 of all referenced WEDM data, pixi.lock SHA, parameter fingerprint (case + studyplan + discharges hash), wua_quality_grade
    - **F. Biological observation readers** — `read_fish_sampling`, `read_redd_count`, `read_pit_tag_event`, `read_rst_count`, `read_edna_sample`, `read_macroinvertebrate_sample`, plus `validate_biological_table` with full WEDM schema validation (8 unit tests)
    - **G. SCHISM real input generation** — `_write_hgrid_from_ugrid` converts a UGRID-2D NetCDF into proper hgrid.gr3 ASCII; `_render_param_nml` produces a runnable param.nml skeleton; vgrid.in (depth-averaged) and bctides.in stub written
- **Final test count: 180 passing**, 1 skipped (post-1.0 unsteady SWE)
- Code: ~4,600 LoC src, ~2,500 LoC tests
- Remaining `NotImplementedError`s in code are intentional out-of-scope guards (PEST++ multi-parameter scoped to 1.x; unknown hydro backend rejected)

- **Final-completion sweep v2 (2026-05-07)** — last 8 honest gaps closed:
    - **33. Drift-egg auto-runs from `case.run()`**: `metric: drifting-egg` plus `habitat.drifting_egg` block now drives `evaluate_drifting_egg()` per discharge and writes `drift_egg.csv`; constant + CSV temperature forcing supported. Schema extended (case.schema.json `habitat.drifting_egg`).
    - **34. UGRID mesh validator**: `openlimno.preprocess.validate_ugrid_mesh` covers UGRID-1D and UGRID-2D; `case.run()` resolves `mesh.uri`, validates it, surfaces warnings, and forwards to SCHISMAdapter.prepare.
    - **35. Legacy importers (best-effort)**: `read_hecras_geometry` (HEC-RAS .g0X X1/GR records, fixed-width or comma-delimited bodies) + `read_river2d_cdg` (River2D bed-file NODES/ELEMENTS).
    - **36. Snakemake workflow**: `tests/integration/test_snakemake_calibrate.py` parses the calibrate.smk and dry-runs it under `snakemake` when available; skips cleanly otherwise.
    - **37. SCHISM dry-run e2e**: `tests/integration/test_case_schism_dry_run.py` proves backend=schism + dry_run produces real `hgrid.gr3` from a UGRID-2D mesh and falls back to Builtin1D for habitat post-processing. Adapter no longer raises when no SCHISM binary is on PATH (rc=127 + fallback path).
    - **38. examples/phabsim_replication**: full standalone Bovee 1997 example (build_data.py + case.yaml + quickstart.py + README.md) reproducing analytic WUA within 1.78e-4 relative error; CI-asserted by `tests/integration/test_phabsim_replication_example.py`.
    - **39. CLI subprocess tests**: `tests/integration/test_cli_subprocess.py` spawns `python -m openlimno` (new `__main__.py` shim) and exercises help/version/validate/init/run/studyplan paths via the actual entry-point.
    - **Final test count after v2: 205 passing**, 3 skipped (snakemake-not-installed × 2, post-1.0 unsteady SWE × 1)
    - Code: ~5,200 LoC src, ~3,200 LoC tests

### Changed
- N/A

### Deprecated
- N/A

### Removed
- N/A

### Fixed
- N/A

### Security

exec
/bin/bash -lc 'git status --short' in /mnt/data/openlimno
 succeeded in 0ms:
exec
/bin/bash -lc "git diff --unified=0 86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40 -- scripts/triple_review.sh | sed -n '1,220p'" in /mnt/data/openlimno
 succeeded in 0ms:
diff --git a/scripts/triple_review.sh b/scripts/triple_review.sh
index 477b0f5..dcf2ab8 100755
--- a/scripts/triple_review.sh
+++ b/scripts/triple_review.sh
@@ -22 +22,4 @@
-# Requires: codex, gemini, claude CLIs on PATH; git working tree clean.
+# Requires: codex, gemini, claude CLIs on PATH AND each logged in via
+# its subscription (ChatGPT / Google AI / Anthropic Console). API-key
+# auth is intentionally not used — see docs/triple_review.md for why.
+# Git working tree must be clean.
@@ -29,0 +33,12 @@ BASE="${1:-${BASE:-origin/main}}"
+# Round-3 review (Claude): defend against ``-``-prefixed BASE values
+# being interpreted as flags by downstream git commands. (``--``
+# after ``--verify`` doesn't work — it makes git treat the arg as a
+# pathspec — and ``--end-of-options`` isn't honored uniformly across
+# rev-parse modes. A simple upfront pattern check is the most
+# portable form of belt-and-suspenders.)
+case "$BASE" in
+    -*)
+        echo "ERROR: refnames starting with '-' are not allowed (flag-injection risk)" >&2
+        exit 2
+        ;;
+esac
@@ -36,11 +51,21 @@ BASE="${1:-${BASE:-origin/main}}"
-# Round-7 review (Codex+Gemini): the previous check rejected ALL hex
-# strings under 8 chars, including legitimate branches named "cafe",
-# "dead", "beef" (4 chars). It also missed UPPERCASE 7-char SHAs
-# like 'A72350F' since the regex was lowercase-only. Now reject
-# ONLY the git-default 7-char short SHA length, case-insensitive.
-# Branches with hex-shaped names of other lengths still allowed,
-# 8+ char SHAs allowed.
-base_lower="$(echo "$BASE" | tr 'A-F' 'a-f')"
-if [[ "$base_lower" =~ ^[0-9a-f]{7}$ ]]; then
-    echo "ERROR: 7-char short SHAs are collision-prone — pass full SHA or 8+ chars" >&2
-    exit 2
+# Round-1 review (Codex P2 + Gemini + Claude consensus, post-alpha.10):
+# the previous "reject only 7-char hex" rule still let 4-6 char
+# abbreviated SHAs (e.g. ``git rev-parse --short=6``) bypass collision
+# protection — they pass ``check-ref-format --allow-onelevel`` as
+# valid one-level refnames. Disambiguate by checking whether the
+# hex-shaped value is *actually* a real branch/tag in this repo. If
+# it is, allow (legit ``cafe`` branch). If it's not, reject as an
+# abbreviated SHA. Round-7's case-insensitive lowercasing preserved.
+# 8+ char SHAs and refnames-with-slashes unchanged.
+base_lower="$(printf '%s' "$BASE" | tr 'A-F' 'a-f')"
+if [[ "$base_lower" =~ ^[0-9a-f]{4,7}$ ]]; then
+    # Round-2 review (Claude P1 #4): the previous version checked
+    # ``show-ref --verify`` against the *original-case* $BASE while
+    # the regex tested $base_lower, so a user passing ``CAFE`` for a
+    # real ``cafe`` branch hit the false-reject path. ``rev-parse
+    # --symbolic-full-name`` is case-sensitive (correctly) and covers
+    # heads/tags/remotes/HEAD-relative in one call.
+    if [ -z "$(git rev-parse --symbolic-full-name "$BASE" 2>/dev/null)" ]; then
+        echo "ERROR: '$BASE' looks like an abbreviated SHA (collision-prone). Pass the full 40-char SHA or use the actual ref name." >&2
+        exit 2
+    fi
@@ -153,12 +178,16 @@ PID_GEMINI=$!
-        # Two modes:
-        #   * CI-style (ANTHROPIC_API_KEY set): --bare for hermetic, stateless
-        #     invocation. Skips keychain / hooks / plugin sync. Required for
-        #     reproducible review on a clean GitHub Actions runner.
-        #   * Local-style (no API key): rely on the user's logged-in session.
-        # Either way we feed the prompt + diff via stdin (NOT argv) so we
-        # don't hit ARG_MAX on big diffs.
-        if [ -n "${ANTHROPIC_API_KEY:-}" ]; then
-            CLAUDE_FLAGS=(-p --bare --add-dir "$REPO_ROOT")
-        else
-            CLAUDE_FLAGS=(-p --add-dir "$REPO_ROOT")
-        fi
+        # CLI-only: rely on the user's logged-in `claude` session
+        # (Anthropic Console subscription). API-key mode (--bare with
+        # ANTHROPIC_API_KEY) is intentionally not supported — see
+        # docs/triple_review.md "Why CLI subscription, not API". Feed
+        # prompt + diff via stdin (NOT argv) to dodge ARG_MAX on big
+        # diffs.
+        # Round-2 review (Codex P2): `claude -p` silently honors
+        # ANTHROPIC_API_KEY whenever it's set. A stale env var from
+        # an old API-based setup would either fail before reaching
+        # the subscription session, or worse, bill the API silently
+        # despite the "CLI-only" promise. Strip it for this invocation
+        # via `env -u`.
+        # Round-3 review (Claude): also strip ANTHROPIC_AUTH_TOKEN
+        # and ANTHROPIC_BASE_URL — claude-code honors the whole
+        # family. Stripping only one of the three would still let
+        # API auth sneak in.
@@ -172 +201,2 @@ PID_GEMINI=$!
-        } | claude "${CLAUDE_FLAGS[@]}" 2>&1
+        } | env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_BASE_URL \
+                claude -p --add-dir "$REPO_ROOT" 2>&1

exec
/bin/bash -lc 'git rev-parse --verify "$(git rev-parse --short=3 HEAD)''^{commit}" 2>/dev/null; echo rc=$?' in /mnt/data/openlimno
 succeeded in 0ms:
4c6ec27dd0d380f575fabf7ae6311e56a6ef111d
rc=0

exec
/bin/bash -lc "nl -ba scripts/triple_review.sh | sed -n '52,74p'" in /mnt/data/openlimno
 succeeded in 0ms:
    52	# the previous "reject only 7-char hex" rule still let 4-6 char
    53	# abbreviated SHAs (e.g. ``git rev-parse --short=6``) bypass collision
    54	# protection — they pass ``check-ref-format --allow-onelevel`` as
    55	# valid one-level refnames. Disambiguate by checking whether the
    56	# hex-shaped value is *actually* a real branch/tag in this repo. If
    57	# it is, allow (legit ``cafe`` branch). If it's not, reject as an
    58	# abbreviated SHA. Round-7's case-insensitive lowercasing preserved.
    59	# 8+ char SHAs and refnames-with-slashes unchanged.
    60	base_lower="$(printf '%s' "$BASE" | tr 'A-F' 'a-f')"
    61	if [[ "$base_lower" =~ ^[0-9a-f]{4,7}$ ]]; then
    62	    # Round-2 review (Claude P1 #4): the previous version checked
    63	    # ``show-ref --verify`` against the *original-case* $BASE while
    64	    # the regex tested $base_lower, so a user passing ``CAFE`` for a
    65	    # real ``cafe`` branch hit the false-reject path. ``rev-parse
    66	    # --symbolic-full-name`` is case-sensitive (correctly) and covers
    67	    # heads/tags/remotes/HEAD-relative in one call.
    68	    if [ -z "$(git rev-parse --symbolic-full-name "$BASE" 2>/dev/null)" ]; then
    69	        echo "ERROR: '$BASE' looks like an abbreviated SHA (collision-prone). Pass the full 40-char SHA or use the actual ref name." >&2
    70	        exit 2
    71	    fi
    72	fi
    73	if ! [[ "$base_lower" =~ ^[0-9a-f]{8,40}$ ]] \
    74	        && ! git check-ref-format --allow-onelevel "$BASE" 2>/dev/null; then

exec
/bin/bash -lc 'for n in 1 2 3 4; do echo -n "$n: "; git rev-parse --short=$n HEAD; done' in /mnt/data/openlimno
 succeeded in 0ms:
1: 4c6e
2: 4c6e
3: 4c6e
4: 4c6e

exec
/bin/bash -lc 'for x in 4 4c 4c6 4c6e; do git rev-parse --verify "${x}''^{commit}" >/tmp/out 2>/tmp/err; printf '"'%s rc=%s out=%s err=%s\\n' \""'$x" "$?" "$(cat /tmp/out)" "$(cat /tmp/err)"; done' in /mnt/data/openlimno
 succeeded in 0ms:
4 rc=128 out= err=fatal: Needed a single revision
4c rc=128 out= err=fatal: Needed a single revision
4c6 rc=128 out= err=fatal: Needed a single revision
4c6e rc=0 out=4c6ec27dd0d380f575fabf7ae6311e56a6ef111d err=

exec
/bin/bash -lc 'for x in 4c6; do git rev-parse "${x}''^{commit}" >/tmp/out 2>/tmp/err; printf '"'%s rc=%s out=%s err=%s\\n' \""'$x" "$?" "$(cat /tmp/out)" "$(cat /tmp/err)"; done' in /mnt/data/openlimno
 succeeded in 0ms:
4c6 rc=128 out=4c6^{commit} err=fatal: ambiguous argument '4c6^{commit}': unknown revision or path not in the working tree.
Use '--' to separate paths from revisions, like this:
'git <command> [<revision>...] -- [<file>...]'

exec
/bin/bash -lc "sed -n '40,95p' tests/unit/test_qgis_plugin_smoke.py && sed -n '385,415p' tests/integration/studio_workflows_smoke.py" in /mnt/data/openlimno
 succeeded in 0ms:
    assert "Apache-2.0" in text


def test_plugin_csv_helper_handles_openlimno_header(tmp_path: Path) -> None:
    """Helper should skip OpenLimno '#'-prefixed comment lines."""
    from openlimno.qgis.openlimno_qgis_plugin.plugin import OpenLimnoPlugin

    csv_path = tmp_path / "wua_q.csv"
    csv_path.write_text(
        "# OpenLimno output header\n"
        "# annual_avg=5.11\n"
        "discharge_m3s,wua_m2_oncorhynchus_mykiss_spawning\n"
        "1.0,18.0\n"
        "5.0,62.0\n"
    )
    rows = OpenLimnoPlugin._read_wua_csv(str(csv_path))
    # Comment-prefix rows skipped, data rows parsed
    assert len(rows) == 2
    assert "discharge_m3s" in rows[0]


def test_plugin_parquet_helper_returns_list(tmp_path: Path) -> None:
    """Helper should read parquet via pyarrow if available, list of dicts."""
    pa = pytest.importorskip("pyarrow")
    pq = pytest.importorskip("pyarrow.parquet")

    import pandas as pd

    df = pd.DataFrame({"discharge_m3s": [1.0, 2.0], "wua": [10.0, 20.0]})
    pq.write_table(pa.Table.from_pandas(df, preserve_index=False), str(tmp_path / "wua.parquet"))

    from openlimno.qgis.openlimno_qgis_plugin.plugin import OpenLimnoPlugin

    rows = OpenLimnoPlugin._read_wua_parquet(str(tmp_path / "wua.parquet"))
    assert len(rows) == 2
    assert rows[0]["discharge_m3s"] == 1.0
# ============================================================================
# TEST 10: Plot cross-section profile (matplotlib in Qt dialog)
# ============================================================================
def test_plot_profile():
    """Patch QFileDialog to feed in cross_section.parquet + hydraulics.nc,
    patch QDialog.exec to auto-accept the station+Q selector. Verify a
    profile dialog ends up in the project (renders matplotlib canvas)."""
    print("\n[TEST 10] Render cross-section profile dialog")
    win = reset_window()
    xs = OUT_ROOT / "case-bbox/data/cross_section.parquet"
    nc = OUT_ROOT / "case-bbox/out/hydraulics.nc"
    if not (xs.is_file() and nc.is_file()):
        record("T10.plot_profile", "SKIP", "no T1+T6 outputs")
        win.close(); return

    # Drive _render_profile_dialog directly with prepared rows
    from openlimno.gui_core.controller import _read_wua_parquet
    rows = _read_wua_parquet(str(xs))
    stations = sorted({float(r["station_m"]) for r in rows})
    # Patch QDialog.exec to auto-close — we just want the profile to render
    orig_exec = QDialog.exec
    QDialog.exec = lambda self_: 0  # simulate Cancel/dismiss
    try:
        win.ctl._hyd_nc = str(nc)  # so renderer picks up WSE
        win.ctl._render_profile_dialog(rows, stations, stations[len(stations)//2], 7.2)
        record("T10.plot_profile", "PASS",
                f"rendered station {stations[len(stations)//2]:g} at Q=7.2")
    except Exception as e:
        record("T10.plot_profile", "FAIL", f"{type(e).__name__}: {e}")
        traceback.print_exc()
    finally:

exec
/bin/bash -lc "grep -R \"_read_wua_csv\\|_read_wua_parquet\" -n src/openlimno/qgis/openlimno_qgis_plugin/plugin.py src/openlimno/qgis/openlimno_qgis_plugin/__init__.py src/openlimno/gui_core/__init__.py" in /mnt/data/openlimno
 exited 1 in 0ms:
exec
/bin/bash -lc "sed -n '140,260p' src/openlimno/qgis/openlimno_qgis_plugin/plugin.py" in /mnt/data/openlimno
 succeeded in 0ms:
exec
/bin/bash -lc 'tail -120 docs/reviews/v0.1.0-alpha.10/codex.md' in /mnt/data/openlimno
 succeeded in 0ms:
   189	        dlg.setWindowTitle("Plot cross-section")
   190	        form = QFormLayout(dlg)

exec
/bin/bash -lc "python - <<'PY'
import pyarrow as pa
for name in ['ArrowIOError','ArrowInvalid','ArrowNotImplementedError','ArrowTypeError','ArrowCapacityError','ArrowKeyError']:
 c=getattr(pa.lib,name,None)
 if c: print(name, c.__mro__)
PY" in /mnt/data/openlimno
 succeeded in 252ms:
ArrowIOError (<class 'OSError'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowInvalid (<class 'pyarrow.lib.ArrowInvalid'>, <class 'ValueError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowNotImplementedError (<class 'pyarrow.lib.ArrowNotImplementedError'>, <class 'NotImplementedError'>, <class 'RuntimeError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowTypeError (<class 'pyarrow.lib.ArrowTypeError'>, <class 'TypeError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowCapacityError (<class 'pyarrow.lib.ArrowCapacityError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)
ArrowKeyError (<class 'pyarrow.lib.ArrowKeyError'>, <class 'KeyError'>, <class 'LookupError'>, <class 'pyarrow.lib.ArrowException'>, <class 'Exception'>, <class 'BaseException'>, <class 'object'>)

exec
/bin/bash -lc 'git rev-parse --short=6 HEAD && git rev-parse --verify "$(git rev-parse --short=6 HEAD)''^{commit}"' in /mnt/data/openlimno
 succeeded in 0ms:
86ff7a
86ff7a7b9c1c3fe533baf4ef71e0d46c67baca40

exec
/bin/bash -lc 'git check-ref-format --allow-onelevel "$(git rev-parse --short=6 HEAD)"; echo $?' in /mnt/data/openlimno
 succeeded in 0ms:
0

exec
/bin/bash -lc "nl -ba scripts/triple_review.sh | sed -n '35,50p'; nl -ba src/openlimno/gui_core/controller.py | sed -n '50,75p'" in /mnt/data/openlimno
 succeeded in 0ms:
    35	# ``--allow-onelevel``.
    36	# Round-7 review (Codex+Gemini): the previous check rejected ALL hex
    37	# strings under 8 chars, including legitimate branches named "cafe",
    38	# "dead", "beef" (4 chars). It also missed UPPERCASE 7-char SHAs
    39	# like 'A72350F' since the regex was lowercase-only. Now reject
    40	# ONLY the git-default 7-char short SHA length, case-insensitive.
    41	# Branches with hex-shaped names of other lengths still allowed,
    42	# 8+ char SHAs allowed.
    43	base_lower="$(echo "$BASE" | tr 'A-F' 'a-f')"
    44	if [[ "$base_lower" =~ ^[0-9a-f]{7}$ ]]; then
    45	    echo "ERROR: 7-char short SHAs are collision-prone — pass full SHA or 8+ chars" >&2
    46	    exit 2
    47	fi
    48	if ! [[ "$base_lower" =~ ^[0-9a-f]{8,40}$ ]] \
    49	        && ! git check-ref-format --allow-onelevel "$BASE" 2>/dev/null; then
    50	    echo "ERROR: '$BASE' is not a valid ref name or SHA" >&2
    50	    act on them.
    51	    """
    52	    try:
    53	        import pyarrow.parquet as pq
    54	    except ImportError:
    55	        pq = None
    56	    if pq is not None:
    57	        t = pq.read_table(path)  # raises ArrowInvalid on torn footer, etc.
    58	        return [
    59	            dict(zip(t.column_names, row, strict=False))
    60	            for row in zip(*[c.to_pylist() for c in t.columns], strict=False)
    61	        ]
    62	    # No pyarrow: try GDAL OGR.
    63	    try:
    64	        from osgeo import ogr
    65	    except ImportError as e:
    66	        raise RuntimeError(
    67	            "neither pyarrow nor GDAL available to read parquet"
    68	        ) from e
    69	    ds = ogr.Open(path)
    70	    if ds is None:
    71	        raise OSError(f"GDAL/OGR failed to open {path}")
    72	    layer = ds.GetLayer(0)
    73	    defn = layer.GetLayerDefn()
    74	    field_names = [defn.GetFieldDefn(i).GetName() for i in range(defn.GetFieldCount())]
    75	    return [{fn: feat.GetField(fn) for fn in field_names} for feat in layer]

exec
/bin/bash -lc "python - <<'PY'
import tempfile, pathlib, pyarrow.parquet as pq
p=pathlib.Path(tempfile.mkdtemp())/'x.parquet'; p.write_text('v1')
try: pq.read_table(str(p))
except Exception as e: print(type(e), type(e).__mro__, e)
PY" in /mnt/data/openlimno
 exited 1 in 277ms:
Traceback (most recent call last):
  File "<stdin>", line 2, in <module>
  File "/usr/lib/python3.12/tempfile.py", line 512, in mkdtemp
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 265, in _sanitize_params
    dir = gettempdir()
          ^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 454, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 447, in _gettempdir
    tempdir = _get_default_tempdir()
              ^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 362, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/tmp', '/var/tmp', '/usr/tmp', '/mnt/data/openlimno']

codex
The patch introduces regressions in both the GUI parquet-read path and the review script's SHA validation. These are actionable issues that can cause unhandled GUI exceptions and allow the collision-prone short SHAs the script is trying to block.

Full review comments:

- [P2] Handle parquet failures outside the cache — /mnt/data/openlimno/src/openlimno/gui_core/controller.py:57-57
  When `pyarrow` is present, `pq.read_table()` failures now escape `_read_wua_parquet()`, but `open_wua_q()` and `plot_cross_section()` still call this helper directly rather than through `_read_xs_rows_cached()`. If a user selects a corrupt/partially-written parquet file, those GUI actions now raise an unhandled exception before reaching their empty-row or warning handling, so they need their own catch/report path or a non-throwing wrapper.

- [P2] Reject abbreviated SHAs shorter than seven chars — /mnt/data/openlimno/scripts/triple_review.sh:44-45
  This only rejects exactly seven hex characters, so a unique 4–6 character abbreviated commit such as `git rev-parse --short=6 HEAD` passes `git check-ref-format` and is then accepted by `git rev-parse --verify`, defeating the collision protection this validation is meant to provide. If short hex branch names need to be allowed, distinguish actual refs from commit abbreviations before allowing them.
The patch introduces regressions in both the GUI parquet-read path and the review script's SHA validation. These are actionable issues that can cause unhandled GUI exceptions and allow the collision-prone short SHAs the script is trying to block.

Full review comments:

- [P2] Handle parquet failures outside the cache — /mnt/data/openlimno/src/openlimno/gui_core/controller.py:57-57
  When `pyarrow` is present, `pq.read_table()` failures now escape `_read_wua_parquet()`, but `open_wua_q()` and `plot_cross_section()` still call this helper directly rather than through `_read_xs_rows_cached()`. If a user selects a corrupt/partially-written parquet file, those GUI actions now raise an unhandled exception before reaching their empty-row or warning handling, so they need their own catch/report path or a non-throwing wrapper.

- [P2] Reject abbreviated SHAs shorter than seven chars — /mnt/data/openlimno/scripts/triple_review.sh:44-45
  This only rejects exactly seven hex characters, so a unique 4–6 character abbreviated commit such as `git rev-parse --short=6 HEAD` passes `git check-ref-format` and is then accepted by `git rev-parse --verify`, defeating the collision protection this validation is meant to provide. If short hex branch names need to be allowed, distinguish actual refs from commit abbreviations before allowing them.

exec
/bin/bash -lc "python - <<'PY'
import tempfile, pathlib
from openlimno.gui_core.controller import _read_wua_parquet
p=pathlib.Path(tempfile.mkdtemp())/'x.parquet'
p.write_text('not parquet')
try:
 _read_wua_parquet(str(p))
except Exception as e:
 print(type(e), e, 'cause', type(e.__cause__) if getattr(e,'__cause__',None) else None)
PY" in /mnt/data/openlimno
 exited 1 in 132ms:
Traceback (most recent call last):
  File "<stdin>", line 3, in <module>
  File "/usr/lib/python3.12/tempfile.py", line 512, in mkdtemp
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 265, in _sanitize_params
    dir = gettempdir()
          ^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 454, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 447, in _gettempdir
    tempdir = _get_default_tempdir()
              ^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 362, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/tmp', '/var/tmp', '/usr/tmp', '/mnt/data/openlimno']

exec
/bin/bash -lc 'ls -ld /tmp /var/tmp /usr/tmp /mnt/data/openlimno | cat && touch /tmp/test_openlimno 2>&1 || true && ls -l /tmp/test_openlimno 2>&1 || true' in /mnt/data/openlimno
 succeeded in 0ms:
ls: cannot access '/usr/tmp': No such file or directory
drwxr-xr-x   23 user user   4096 May  9 14:23 /mnt/data/openlimno
drwxrwxrwt 1211 root root 552960 May  9 15:03 /tmp
drwxrwxrwt   22 root root   4096 May  9 15:03 /var/tmp
-rw-rw-r-- 1 user user 0 May  9 15:03 /tmp/test_openlimno

exec
/bin/bash -lc 'set -x; f=/tmp/test_openlimno_$RANDOM; touch "$f"; echo rc=$?; ls -l "$f"; rm "$f"; echo rm=$?; mktemp /tmp/review-diff.XXXXXX.patch; echo mktemp_rc=$?' in /mnt/data/openlimno
 succeeded in 0ms:
+ f=/tmp/test_openlimno_17409
+ touch /tmp/test_openlimno_17409
rc=0
+ echo rc=0
+ ls -l /tmp/test_openlimno_17409
-rw-rw-r-- 1 user user 0 May  9 15:03 /tmp/test_openlimno_17409
+ rm /tmp/test_openlimno_17409
rm=0
+ echo rm=0
+ mktemp /tmp/review-diff.XXXXXX.patch
/tmp/review-diff.xs3aWE.patch
mktemp_rc=0
+ echo mktemp_rc=0

codex
The patch largely hardens the parquet and review-script paths, but the CLI-only auth change is incomplete because Codex and Gemini can still consume inherited API-key environment variables.

Review comment:

- [P2] Strip API-key variables from all reviewer CLIs — /mnt/data/openlimno/scripts/triple_review.sh:201-202
  This only removes the Anthropic API environment for Claude; in a maintainer shell where `OPENAI_API_KEY` or `GEMINI_API_KEY` is still exported, the Codex and Gemini invocations above still inherit those variables. That means the new CLI-subscription-only mode can still use or bill API credentials despite the documented guarantee, so those invocations need equivalent `env -u` handling.
The patch largely hardens the parquet and review-script paths, but the CLI-only auth change is incomplete because Codex and Gemini can still consume inherited API-key environment variables.

Review comment:

- [P2] Strip API-key variables from all reviewer CLIs — /mnt/data/openlimno/scripts/triple_review.sh:201-202
  This only removes the Anthropic API environment for Claude; in a maintainer shell where `OPENAI_API_KEY` or `GEMINI_API_KEY` is still exported, the Codex and Gemini invocations above still inherit those variables. That means the new CLI-subscription-only mode can still use or bill API credentials despite the documented guarantee, so those invocations need equivalent `env -u` handling.
