# OpenLimno 对标 inSTREAM + 全功能 benchmark — 2026-05-26

> Live functional benchmark report: OpenLimno post-IBM-merge state
> exercised end-to-end against shipped inSTREAM 7.0.2 NetLogo
> reference output + the project's other reference-platform
> harnesses (PHABSIM Bovee 1997, MMS, Toro, HABBY/CASiMiR exchange,
> Lemhi end-to-end).

## Pass / fail summary

| Bench | Description | Tests | Status |
|---|---|---|---|
| **1. PHABSIM Bovee 1997** | Closed-form Δ ≤ 1e-3 against `benchmarks/phabsim_bovee1997/test_bovee_1997.py` | **9 / 9** | ✅ All pass |
| **2. MMS hydraulics** | Method of Manufactured Solutions; 5 grid resolutions | **6 / 6** | ✅ All pass |
| **3. Toro Riemann** | Shock-tube + geometry self-consistency | **2 / 2** | ✅ All pass |
| **4. HABBY/CASiMiR exchange** | Generic `benchmarks/_compare` harness | **15 / 15** | ✅ All pass |
| **5. PHABSIM real-Fortran scaffold** | `benchmarks/phabsim_real` (`--check-only` mode; binary not yet acquired) | **4 / 4** + 1 skip | ✅ pin + 1 expected skip |
| **6. Lemhi end-to-end** | `tests/integration/test_r5_r14_lemhi_end_to_end_audit.py` + `test_lemhi_end_to_end.py` | **17 / 17** | ✅ All pass |
| **7. Composite HSI example** | Per-cell + scalar overlay (real raster) | **3 / 3** | ✅ All pass |
| **8. PHABSIM replication example** | IFIM Step 4 reproducibility | **1 / 1** | ✅ Pass |
| **9. Drifting-egg case** | SPEC §4.2.6 Lagrangian | **3 / 3** | ✅ All pass |
| **10. Native IBM core** | inSTREAM-style daily simulation | **34 / 34** | ✅ All pass |
| **11. inSTREAM 7 official benchmark suite** | Parsers + NetLogo runner + comparison + acceptance | **18 / 18** | ✅ All pass |
| **opt-in: pestpp** | Real PEST++ GLM binary calibration | **1 / 1** (117s) | ✅ Pass |
| **opt-in: workflow** | Snakemake dry-run | **2 / 2** | ✅ All pass |
| **opt-in: qgis** | QGIS plugin shim | timed out (env install slow; tests themselves pass per prior session) | ⏸ env-install gate |
| **opt-in: osgeo** | GDAL/OSGeo bindings | 8 fail | env-drift (osgeo Python pkg not installed; tracked) |
| **default `pixi run check`** | **819 / 819** | — | **✅ All pass; ruff 0; mypy --strict 76 + 6 files clean** |

## Numerical results (this session's live runs)

### A. Lemhi PHABSIM-equivalent (1D Manning + WUA, HSI grade B)

```
discharge_m3s  wua_m2_oncorhynchus_mykiss_spawning  wua_m2_oncorhynchus_mykiss_fry
        5.0                            61.53                            0.28
       10.0                            48.47                            0.00
       20.0                             8.35                            0.00
       40.0                             0.00                            0.00
```

Reproducible across runs; identical to the audit reference numbers
documented in [`u4_prerequisite_lemhi_fixture_pipeline.md`](../u4_prerequisite_lemhi_fixture_pipeline.md).

### B. Regulatory exports (all 3 templates emit cleanly)

| File | Rows × Cols | Lead columns |
|---|---|---|
| `sl712.csv` | 12 × 6 | month, monthly_avg_q_m3s, min_eco_flow_m3s, suitable_eco_flow_m3s, multi_year_avg_pct |
| `ferc_4e.csv` | 12 × 5 | month, wet_year_q_m3s, normal_year_q_m3s, dry_year_q_m3s, suitable_eco_flow_m3s |
| `eu_wfd.csv` | 12 × 5 | month, observed_q_m3s, observed_wua_m2, eqr_monthly, status |

### C. Native IBM — 100-day simulation (200 fish, 10 cells, riffle-pool gradient)

```
day  abundance   biomass_g  mean_length_mm  survival_rate
  0        200    3061.30          110.00          1.000
 10        180    2870.00          111.51          0.900
 25        162    2745.38          113.79          0.810
 50        133    2495.71          117.71          0.665
 75        106    2200.51          121.73          0.530
100         82    1894.86          126.12          0.410
```

- Total mortality events: **69**
- Cells used (final day): **10 / 10** (all utilised)
- Growth: 110.00 → 126.12 mm mean length (+14.7% over 100 days)
- Survival: 41% at day 100 (realistic for unforced mortality model
  without recruitment in this short window)

### D. Reproducibility (seed=20260526)

Two independent runs with identical config + initial population
produce **bit-identical abundance trajectories**. Pinned by
`tests/unit/test_native_ibm_reproducibility.py`:

```
test_native_ibm_seed_produces_identical_outputs ............... PASSED
test_native_ibm_different_seed_produces_different_outputs ..... PASSED
```

### E. inSTREAM 7.0.2 brief reference comparison (live run)

Used the SHIPPED NetLogo 7.0.2 ExampleA `brief_summary.csv` from
`tests/fixtures/instream7/netlogo_702_short/` (generated by Cal
Poly Humboldt headless from the public inSTREAM 7.4 distribution).

Comparison machinery:

```
NetLogo : abundance=360, biomass=2457.2g, mean_length=68.98mm
Native  : abundance=360, biomass=2469.5g, mean_length=69.08mm

abundance_abs_delta            = 0
biomass_relative_delta_abs     = 0.005 (within 5% tolerance)
mean_length_abs_delta_mm       = 0.1 (within 1.0 mm tolerance)
passed                         = True  ✅
```

(One "At setup" snapshot; the shipped reference is a 2-day "short"
calibration fixture, not a long-run regulatory target. Full
multi-day comparison requires the inSTREAM 7.4 official archive
+ a real native run on the same input deck — pending R-PHABSIM-
REAL-style binary acquisition.)

The comparison FUNCTIONS work end-to-end. The parity report writer
also runs (`write_instream7_parity_report` produces comparison +
summary CSVs).

### E.1. IBM Studio default = real inSTREAM 7.4 ExampleA (2026-05-26 later)

The Studio's runtime ``/api/default`` route now serves the **real
Cal Poly Humboldt ExampleA case** when the GPL-3.0 archive is on
the local filesystem (env var / XDG cache / repo dev dir). When
absent, it transparently falls back to the synthetic Lemhi-Hayden
demo. Test-facing ``default_studio_scenario()`` still returns the
synthetic — only the live ``default_studio_scenario_resolved()``
prefers ExampleA.

Live state after the switch:

```
scenario_id      = instream7-example-a-real-archive
river.name       = inSTREAM 7.4 Example Project A
reach            = ExampleA (Cal Poly Humboldt official archive)
cells            = 1373 surveyed polygons
reach length     = 379.03 m (CRS EPSG:2225, ft→m)
time series      = 4384 days @ daily (Oct 1999 – Sep 2011)
median Q         = 6.06 m³/s
median T         = 12.0 °C
median NTU       = 2.0
initial pop      = 360 rainbow trout (300 age-0, 50 age-1, 10 age-2)
license          = GPL-3.0 (not bundled in this Apache-2.0 repo)
```

10-day IBM smoke result on the real cells (pre-fix, before the
2026-05-26 cm→mm correction):

```
day  abundance  biomass_g  mean_length_mm
  0       360       6.6           11.7    ← BUG: 11.7 was cm-as-mm
  1       359      95.7           28.0
  5       352      93.9           28.0
 10       343      91.5           28.0
```

Triple-review (Codex P1, Claude "cohort structure collapsed") caught
that the official ``Length mode`` column is in centimetres and was
being assigned directly to ``initial_length_mm``. The corrected path
now computes an abundance-weighted cm→mm mean (300×6.1cm + 50×12cm
+ 10×17cm) × 10 / 360 ≈ 72.2 mm, and preserves the full tri-cohort
structure under ``_provenance.initial_population_cohorts``.

45-day Studio run from the UI: 360 → 304 (survival 0.844). Plan-view
renders the convex envelope of the 1373 surveyed cells (rotated into
principal-axis frame) — that envelope is convex by construction and
does NOT trace the concave meander; the centerline (binned cell-
centroid lateral) does carry the along-reach shape. Triple-review
(2026-05-26 later) explicitly corrected the earlier "distinctive
meander curve" overclaim here.

Provenance auto-embedded in the scenario JSON under ``_provenance``:
``Cal Poly Humboldt inSTREAM 7.4 ExampleA``, archive filename,
license, CRS, real-cell count, time-series length, medians, and
``loaded_from`` filesystem path.

### E.2. Why mean length can shrink at median temperature

The 2026-05-26 pass-2 software test (Gemini) flagged a "starvation
bug" because mean fish length declined from 72.22 mm → 71.41 mm
over 30 days under the loaded ExampleA defaults at median T = 12 °C.
This is **expected biology, not a code defect**:

- The official ExampleA profile (`parameters-ExampleA.nls`) sets
  ``thermal_optimum_c = 22.0`` for rainbow trout. This is the
  Cal Poly Humboldt value and is preserved verbatim under
  ``profile.thermal_optimum_c`` — OpenLimno does not modify it.
- At T = 12 °C with ``thermal_min_c = 0.0`` and
  ``thermal_max_c = 30.0``, the ``triangular_factor`` in
  `runtime_submodels.py:90` returns about 0.55, so consumption is
  scaled to ~55 % of its optimum value while respiration is roughly
  unscaled. Net energy balance can therefore be negative.
- The official ExampleA profile explicitly allows negative growth:
  ``max_daily_shrinkage_mm = 0.08``. A 0.8 mm decline over 30 days
  is **inside** that envelope (0.027 mm/day vs 0.08 mm/day cap).
- The default scenario uses ``flow.median()`` + ``temperature.median()``
  + ``turbidity.median()`` of the 10-year time series. October–March
  values dominate the median, so the "representative day" is
  thermally suboptimal for trout by design. The Cal Poly Humboldt
  manual documents this case explicitly as a winter-stress scenario.

If you want a thermally favourable representative day, override the
config (``flow_m3s``, ``profile.thermal_optimum_c``, etc.) or load
a different ``--instream-fixture`` case.

### F. inSTREAM 7 input parsing (real fixtures)

From `tests/unit/test_instream7_benchmark.py` (real fixture parsing):

- `parse_instream7_case` reads inSTREAM 7 parameter files
- `discover_instream7_cases` discovers cases under an archive root
- `species_profile_from_official_case` extracts species params
  (incl. cmax_optimum_temperature, spawn temperatures)
- `read_official_initial_population` + `read_official_adult_arrivals`
  parse the official population formats
- `build_population_from_official_adult_arrivals` constructs the
  native initial population from arrival schedules

All 18 inSTREAM-7-related tests pass.

## Subsystem coverage matrix

| Subsystem | Tests | Real-data exercise | Reference comparison |
|---|---|---|---|
| Hydrodynamics (Builtin1D) | ✅ 29 tests | Lemhi 4 discharges | Bovee 1997 Δ ≤ 1e-3 |
| Hydrodynamics (SCHISM adapter) | ✅ 2 tests (dry-run + no-binary-raise) | SCHISM container available | n/a (M3+ scope) |
| Habitat (HSI / WUA / cell / HMU) | ✅ 191 tests | Lemhi end-to-end | PHABSIM Bovee 1997 |
| Habitat (composite per-cell overlay) | ✅ 3 example tests | composite_hsi example with real raster | n/a |
| Habitat (drifting-egg) | ✅ 3 case tests | drift_egg example | n/a |
| Passage (η_A × η_P) | ✅ 10 tests | Lemhi includes passage_criteria | FishXing exchange (`benchmarks/fishxing`) |
| Native IBM | ✅ 34 + 18 = 52 tests | 100-day, 200-fish demo (this session) | inSTREAM 7.0.2 NetLogo reference (this doc) |
| Preprocess (fetch) | ✅ 184 tests | Lemhi USGS rating curve fetched live (this session) | n/a |
| Preprocess (interop: HEC-RAS/MIKE/TELEMAC/HABBY/inSTREAM-Netlogo/River2D) | ✅ ~15 integration tests | Multiple `*_real_fixtures.py` paths | HABBY/CASiMiR exchange (15 pass) |
| Regulatory exports | ✅ Three templates emit cleanly | Lemhi 12-month tables | reviewer-of-record packages drafted (CN-SL712 + US-FERC + EU-WFD) |
| Workflows (PEST++ calibration) | ✅ 1 real PEST++ binary test | Lemhi calibration via `workflows.calibrate` | n/a |
| WEDM schema validation | ✅ 35 tests + 12 schemas validate | Lemhi case.yaml validates | n/a |
| Path-safety sandbox (R11-4) | ✅ Multiple R-tracks (R11-4, R15-7, R16-5, R17-1, R17-10, R18-1) | Lemhi inline raster paths sandbox-routed | n/a |
| Atomic-write + provenance | ✅ Pinned across audit chain | Lemhi provenance.json + IBM provenance.json (both schemas) | SHA-linked audit chain |
| Studio Path A (PyQt6) | ✅ 4 integration smoke tests | Studio headless smoke | n/a |
| IBM Studio (browser-based, dev-only) | ✅ 12 tests | Dev-only behind `--i-understand-this-is-experimental` flag | n/a |

## Net assessment

**对标 inSTREAM 维度**:
- ✅ Parser: 真实 inSTREAM 7 input format 全部解析(parameter file +
  reach shapefile + hydraulic time series + initial population)
- ✅ NetLogo reference runner: `run_instream7_netlogo_reference`
  + `write_instream7_netlogo_setup_file` 完整
- ✅ Brief population reader: NetLogo BriefPopOut format 已 wire
  through (parse + summarise + compare + parity report)
- ✅ Comparison + tolerance + parity: 4 容差(abundance ±N、biomass
  ±%、mean_length ±mm、综合 pass)+ 报告输出全部可工作
- ✅ Native IBM: 100-day 200-fish 实跑产出真实 mortality + growth
  曲线;reproducibility seed→identical 验证
- 🟡 真 inSTREAM 7.4 official 完整 case 跑通需要 USFWS-style
  binary 或 NetLogo headless 完整环境(R-PHABSIM-REAL-类外部依赖);
  当前用 shipped 2-day "short" reference 演示 comparison 路径

**对标 PHABSIM 维度**:
- ✅ Bovee 1997 closed-form 9/9 通过 Δ ≤ 1e-3
- 🟡 真 Fortran binary parity 待 USFWS 源 license + 容器 build(U3)

**对标 River2D / HABBY / FishXing 维度**:
- ✅ Reference table adapters 全部存在(`benchmarks/river2d`,
  `benchmarks/habby`, `benchmarks/fishxing` + `_compare` harness)
- ✅ HABBY/CASiMiR exchange 15/15 pass

**对标 WFD / FERC / SL712 监管维度**:
- ✅ 三模板均 emit 干净 CSV(Lemhi 12 行 × 5-6 列)
- 🟡 reviewer-of-record signature 待外部行动(三份 review package
  + 一份 outreach 信草稿就绪 in `docs/governance/`)

**净结论**: OpenLimno 12 个核心子系统全部 functional,876 tests
collected,819 默认通过 + 11 opt-in env tests 通过,inSTREAM 比较
machinery 端到端可工作。真 inSTREAM official 多日 long-run 比较 +
真 PHABSIM Fortran 比较 + 真流域 case study 这三件事仍是 external-
action 等待项(U3 / U4 / U5 — 详 ROADMAP)。

## See also

- [`u4_prerequisite_lemhi_fixture_pipeline.md`](../u4_prerequisite_lemhi_fixture_pipeline.md)
  — Lemhi end-to-end honest scope
- [`round_21_22_23_native_ibm_pivot_audit.md`](round_21_22_23_native_ibm_pivot_audit.md)
  — IBM merge audit trail
- [`ROADMAP.md`](../ROADMAP.md) — unfreeze gate U1-U6 status
- ADRs 0001-0016 — complete decision record
